-- Программа «роль Руководитель» (ТЗ 2026-08-17), PR-3: миграция данных.
-- Код разобран по трём шаблонам в PR-1/PR-2 и понимает обе модели, поэтому
-- переключение безопасно. Колонка managerRole сохраняется до PR-4 (переходная
-- совместимость: старые токены живут до 7 дней; откат = обратный UPDATE по ней).
-- Значение enum 'leader' добавлено отдельной миграцией 20260818090000 —
-- PostgreSQL запрещает использовать новое значение в той же транзакции.
UPDATE "User" SET "role" = 'leader'
WHERE "role" = 'manager' AND "managerRole" = 'leader';
