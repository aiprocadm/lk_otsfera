-- Этап 3 ТЗ 12.09.2026 (`У-214`): охват диалогов в профиле доступа.
--
-- До этой миграции видимость переписки держалась только на компании и общей
-- очереди ничейных диалогов: любой сотрудник компании видел любой её диалог.
-- Профиль доступа умел сужать заказы, организации, документы, финансы, лиды и
-- задачи — переписку не умел.
--
-- Колонка аддитивна и безопасна по умолчанию: `all` — это ровно прежнее
-- поведение, поэтому все уже заведённые профили после применения работают как
-- работали. Сужение включает человек, разметив профиль.
--
-- Обратный SQL (миграция обратима, ничего не теряется):
--   ALTER TABLE "AccessProfile" DROP COLUMN "dialogsScope";

-- AlterTable
ALTER TABLE "AccessProfile" ADD COLUMN     "dialogsScope" "ScopeLevel" NOT NULL DEFAULT 'all';
