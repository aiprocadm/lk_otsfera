-- Этап 2 ТЗ 12.09.2026 «Миграция из Битрикс24» (`У-190`): источник лида
-- `bitrix` — лиды, перенесённые из Битрикс24. Отдельным файлом, потому что
-- новое значение enum нельзя использовать в той же транзакции, где оно
-- добавлено (урок, записанный в схеме у `OneCPushStatus`); кодом значение
-- используется только с PR-3 этапа. Обратный SQL: значение enum в Postgres не
-- удаляется — при откате строки с `source = 'bitrix'` переводятся в `manual`.
ALTER TYPE "LeadSource" ADD VALUE 'bitrix';
