-- Сопровождение, хотфикс №45 (проверка `С-8`): ночная сверка обмена ищет
-- «зависшие претензии» — лиды, отправленные в 1С больше суток назад и не
-- получившие подтверждения. Теперь она берёт пачку (`take` + порядок по дате
-- претензии), и без индекса выборка всё равно перебирала бы таблицу лидов
-- целиком, чтобы отсортировать.
--
-- Миграция только добавляет индекс: ни одной строки не меняет.
CREATE INDEX "Lead_pushedToOneCAt_idx" ON "Lead"("pushedToOneCAt");
