# Реестр проверок сопровождения — что делать по «продолжай по ТЗ», когда этапов нет

Заведён 21.08.2026 требованием `У-80` ТЗ
[2026-08-21-tz-cabinets-documents-integrations.md](2026-08-21-tz-cabinets-documents-integrations.md)
(§9). Протокол — `CLAUDE.md` §14, блок «Режим сопровождения». Прогресс по
этапам — [STATUS.md](STATUS.md), сверка требований — [AUDIT.md](AUDIT.md).

**Зачем этот файл.** Между программами код никто не проверял: по фразе
«продолжай по ТЗ» агент отвечал «работы нет» и ждал. За это время накопились
дефекты `Д-1`…`Д-40` (§3.6 ТЗ) — все гейты зелёные, а счёт печатает
«В том числе НДС.» без суммы. Теперь «работы нет» означает **«выбери самую
старую проверку из таблицы и выполни её»**.

## Как пользоваться

1. **Когда.** Только если в `STATUS.md` нет этапов без `✅` и `gh pr list
   --state open` пуст. Пока есть открытый этап — работа идёт по нему, а не
   по этому файлу.
2. **Что выбрать.** Проверку с самой старой датой в колонке «Последний
   прогон». «Не выполнялась» старше любой даты. При равенстве — меньший номер.
3. **Что записать.** После прогона: дату, короткий хеш коммита `main`, на
   котором проверяли, итог одной строкой и ссылки на хотфикс-PR. Каждая
   находка — строка в «Журнале находок» с вердиктом `❌ дефект` · `⚠ вне
   объёма` · `ℹ шум`.
4. **Сколько чинить.** Не более трёх хотфикс-PR за прогон; остальное остаётся
   в очереди журнала и берётся следующим прогоном первым. Один дефект — один
   PR — один тест-страж, проверенный мутацией (`CLAUDE.md` §16).
5. **Чего не делать.** Границы режима — §9.4 ТЗ: никаких новых экранов,
   сущностей, миграций схемы (кроме индексов и ограничений целостности),
   правок требований, ослабления гейтов, массовых рефакторингов, мажорных
   обновлений. Нашёл такое — это `⚠`, вопрос заказчику, а не PR.

## Проверки

| № | Проверка | Процедура | Последний прогон | Итог |
|---|---|---|---|---|
| С-1 | **Гейты** | `npm run typecheck` · `npm run lint` · `npm run test:unit` · `npm run boundaries` · `npm run deadcode` · `npm run dup:check`; при доступном Docker — `npm run gate`. Любой красный — `❌` | 13.09.2026, `ac9fdc12` → `55d6ce95` | ✅ — typecheck 0 (оба проекта) · lint 0 · boundaries 0 (2730 модулей) · deadcode 0 · dup:check 2,77 % · format:check 0 · `tz:status` 18/18. **Полные прогоны локально** (CI GitHub Actions с 12.09.2026 не запускается — аккаунт заблокирован по оплате, канон local-evidence): модульный слой — 1183 файла, 12 728 тестов, 0 упавших (646 с); интеграционный слой против живого Postgres — 191 файл, 1423 теста, 0 упавших (163 с); `npm run build` зелёный (на PR-3b). Полный модульный прогон гонялся в фоне с записью кода выхода в файл, отдельно от `lint`/`typecheck` (вместе не помещаются в память) |
| С-2 | **Drift-аудит ТЗ** | Полный проход `CLAUDE.md` §16 по всем `У-N` (закрытые программы тоже — они описывают ожидаемое поведение): проверять цепочку поведения, не наличие файла. Обновить [AUDIT.md](AUDIT.md) | 13.09.2026, `ac9fdc12` → `55d6ce95` | ✅ — сверка по существу, а не по списку файлов. Требования этапа 1 (`У-178`…`У-187`) перепроверены по цепочке в PR-4 [#591](https://github.com/aiprocadm/lk_otsfera/pull/591): флаг `contacts` читают 19 файлов (страницы, меню, вкладки карточки организации, поиск, server actions, блоки лида/заказа/сделки), IDOR — `not_found` в скоупе и `notFound()` на страницах, `teamMode` обязателен, пять ПДн-контекстов субъекта `contact`. Попутная находка спеки этапа (`В-1-7`) оказалась расхождением с `У-96`: охват прав на заказы в редакторе ролей подписан «Заявки» (так интерфейс называет лиды) — **хотфикс №48 [#592](https://github.com/aiprocadm/lk_otsfera/pull/592)**, страж проверен мутацией. Зеркало: карточка лида есть только у менеджера (`/leader` и `/admin` её не имеют) — блок «Контакт» на ней зеркала не нарушает; вкладки «Контакты»/«Заметки» карточки организации отфильтрованы полем `cabinets`, как «Оплаты» и «История», исключений `mirrorExceptions.ts` не требуют |
| С-3 | **Живой обход §0** | По реестру меню (`lib/navigation/cabinet.ts`) и реестру вкладок карточки: заголовок = пункт меню, подзаголовок в одну строку, главная кнопка или пустой экран с кнопкой, крошки на вложенных, ширина ≤ 390 px без горизонтального скролла, ошибки на русском. Образец процедуры — записи 20–21.08.2026 в [AUDIT.md](AUDIT.md) | 13.09.2026, `ac9fdc12` → `55d6ce95` | ✅ — живой обход прогнан локально на стенде `:3100` (база `lk_otsfera_e2e` с чистого сида, флаги кабинетов + `FEATURE_TELEPHONY_MANGO=1` с фиктивными ключами + `FEATURE_INBOUND_MESSAGING=1` + `FEATURE_CONTACTS=1`): проекты `screen-rules-check` (заголовок = пункт меню, подзаголовок, крошки, ширина ≤ 390 px) и `mobile-shell-check` во всех шести ролях зелёные, включая новые экраны этапа 1 — разделы «Контакты» трёх кабинетов ЦО и вкладки «Контакты»/«Заметки» карточки организации (обход берёт адреса из реестров меню и вкладок, поэтому новые экраны попали под проверку сами). Падений по правилам экранов нет; расхождения — только у эталонов (`С-10`) |
| С-4 | **Доступ и изоляция** | Прогнать `security.role-access-matrix.guardrail`; для роутов и сервисов, появившихся с прошлого прогона, — ручные IDOR-пробы (чужая организация, чужая компания, чужой партнёр, `teamMode`, `canSee*`); убедиться, что у новых страниц кабинетов есть серверный `canSee*` (§4 `CLAUDE.md`) | 13.09.2026, `ac9fdc12` → `55d6ce95` | ✅ — `security.role-access-matrix`, `security.api-route-guard`, `server-actions.session-guard` зелёные. Новые точки этапа 1 проверены по цепочке: скоуп контактов — матрица 64 комбинаций (`contacts.scope.unit`); «Контакт заказа» — заказ чужой компании → `forbidden`, чужой заказ → `not_found`, контакт вне организации заказа или в архиве → `contact_not_found` (`services.orders.primaryContact`, 25 тестов, компания заказа сверяется с компанией сессии даже у администратора); контакт лида вне охвата сотрудника → `null` (`services.contacts.leadContact`); контакт сделки чужой компании или другой организации → `validation` (`services.deals.crud`); страницы «Контакты» ×3 и карточка — `notFound()` без флага или права `crm.contacts`; хотфиксы №48–№50 гардов и скоупа не меняют |
| С-5 | **Мутация стражей** | По кругу: взять следующие 5–10 тестов-стражей (`*.guardrail.test.ts` и стражи из `AUDIT.md`), сломать инвариант, убедиться, что тест падает, вернуть файл. Молчащий страж — `❌`. Вести список проверенных стражей ниже, чтобы круг не начинался заново | 13.09.2026, `ac9fdc12` → `55d6ce95` | ✅ 10 · ℹ 1 — круг 3 продолжен с долга прогона №26: `config.upload-formats` (копия расширений в роуте — поймана), затем по алфавиту после `deps.overrides`: `docs.commands-exist`, `docs.live-links`, `docs.onec-contract-fields`, `documents.document-template`, `documents.generate-panel-mirror`, `documents.requisites-request-reach`, `documents.send-button-mirror`, `documents.superseded-versions`, `e4.no-network` — **все десять пойманы** (11 поломок: копия форматов, несуществующая команда в документе, ссылка в никуда, поле пропало из образца контракта 1С, номер пункта договора не печатается, у руководителя не рендерится панель выпуска, кнопка «Запросить у клиента» переименована, у администратора пропал `canSend`, строитель канала без фильтра версии, тест с живым `fetch`). ℹ: первая проба `docs.commands-exist` легла в `docs/tz/MAINTENANCE.md`, который страж пропускает намеренно (реестры и архив планов описывают то, что было, — задокументировано в шапке стража) — повторена на `docs/ARCHITECTURE.md`, поймана. Стражи хотфиксов прогона проверены при создании: №48 — 1 поломка, №49 — 5, №50 — 2. Следующий по кругу — `email.templates` |
| С-6 | **Молчаливые ошибки** | `grep`/AST по `src/**` (кроме тестов): пустые `catch {}` и `.catch(() => {})` без `log`; `as unknown as`; `!` без комментария-обоснования; `TODO`/`FIXME`/`HACK`; `take:` без пагинации и счётчика; `process.env.` вне списка `У-122`; `console.*`; `revalidatePath`, не покрывающий все экраны объекта; свежие дубли `jscpd` | 13.09.2026, `ac9fdc12` → `55d6ce95` | ✅ · ℹ 1 — пустых `catch {}` нет, `.catch(() => {})` — 4 (все в `ALLOWED` стража `errors.no-silent-catch` с причиной, страж зелёный); `as unknown as` — 13 (без изменений с №26); `TODO`/`FIXME`/`HACK` — 0; `console.*` вне логгера — 0; `process.env` вне реестра — страж `config.env-registry` зелёный. Новые `take` этапа 1 — списки с постраничностью и «Показаны N из M» (`listContacts`, `listContactTab`, `orgHistory`), кандидаты объединения — кап с подписью. ℹ: селекты вариантов (`listContactOptions`, `listContactOrgOptions`, по 500) усекаются без подписи — как и прежний `listCompanyOrgOptions`; селект на полтысячи строк сам по себе непригоден, при таком объёме нужен поиск, а не список — вопрос объёма (`⚠` не ставлю: у справочника контактов поиск уже есть, форма сделки ведёт туда подсказкой) |
| С-7 | **Документация ↔ код** | `.env.example` и `.env.production.example` против `process.env` в коде; `docs/integrations/1c-contract.md` против `schemas.ts`/`rest-wire.ts`/`mock-1c`; `docs/glossary.md` против строк меню и вкладок; `CHANGELOG.md` за последние PR; команды README против `package.json`; `docs/feature-flags-matrix.md` против `featureFlags.ts` | 13.09.2026, `ac9fdc12` → `55d6ce95` | ✅ — стражи `.env.example`, реестра env, глоссария (пятнадцать терминов ТЗ есть в словаре кабинета и в `docs/glossary.md`), матрицы флагов, живых ссылок, команд README, полей контракта 1С и согласованности четырёх указателей на действующее ТЗ — зелёные. Строка `contacts` в матрице флагов переписана со всеми точками чтения (PR-4); CHANGELOG пополнен по PR-3b, PR-4 и хотфиксам №48–№50; снимок состояния в `CLAUDE.md` §14 обновлён на 13.09.2026 (этап 1 закрыт, текущий — 2) |
| С-8 | **Производительность** | Поиск `findMany`/`findFirst`/`count` внутри циклов (N+1); новые `where`/`orderBy` без индекса в `schema.prisma`; списки без `take` и без пагинации; тесты дольше 5 с (`vitest --reporter=verbose`) | 13.09.2026, `ac9fdc12` → `55d6ce95` | ❌ 2 — оба закрыты хотфиксами. (1) **Очередь прогона №25 закрыта**: обе ночные задачи после хотфикса №45 читали всю выборку без `take`, а сверка лидов порядком «сначала старые» отдала бы пачку неизлечимым претензиям — **хотфикс №49 [#593](https://github.com/aiprocadm/lk_otsfera/pull/593)** (перенос готового коммита `62b4aa70` из закрытого дублем #577: выборка возвратов от закрытых периодов ≤500/≤500, лиды — 200 самых свежих, индекс `Lead(pushedToOneCAt)` миграцией только с индексом, `log.warn` при полной пачке; пять мутаций стражей пойманы). (2) **Колонки этапа 1 без индексов**: карточка контакта на вкладках «Заказы» и «Сделки» фильтрует по `Order.primaryContactId` и `Deal.contactId`, у которых индексов не было — **хотфикс №50 [#594](https://github.com/aiprocadm/lk_otsfera/pull/594)** (миграция только с двумя индексами, страж `prisma.contact-lookup-indexes`, две мутации пойманы). Новых `findMany` в циклах в коде этапа 1 нет; тестов дольше 5 с нет (самый долгий файл полного прогона — 3,9 с) |
| С-9 | **Зависимости** | `npm audit --omit=dev` и `npm audit`; открытые PR renovate; уязвимые версии. Патч/минор с уязвимостью — чинить; мажор — только спекой (`⚠`) | 13.09.2026, `ac9fdc12` → `55d6ce95` | ✅ — `npm audit --omit=dev` = 0. Полный `npm audit` — прежние 6 записей, все dev (`vitest`, `@vitest/coverage-v8`, `@vitest/mocker`, `vite`, `vite-node`, `esbuild`), каждая закрывается только мажором → `Р-26`, §9.4. PR renovate нет |
| С-10 | **Визуальные эталоны** | При доступном стенде (`npm run dev` + seed): `npm run e2e:visual`, включая проекты `mobile-*`. **Стенд поднимать с `FEATURE_TELEPHONY_MANGO=1`** (и фиктивными `MANGO_API_KEY`/`MANGO_API_SALT` — иначе `R0.2` не даст приложению стартовать): эталоны сняты с пунктом меню «Звонки», а `.env.production` держит флаг выключенным, и без него пять снимков расходятся по высоте (прогон №25). **С прогона №27 — ещё и с `FEATURE_INBOUND_MESSAGING=1` и `FEATURE_CONTACTS=1`**: эталоны кабинетов ЦО сняты с пунктами «Мессенджеры» и «Контакты» и вкладками «Контакты»/«Заметки» карточки организации (флаг `contacts` в бою выключен до приёмки этапа 1, эталоны держат будущее состояние — тот же приём, что у «Звонков»). Красные **не по картинке** — `❌`; устаревшие эталоны — пересъёмка с объяснением в PR, что именно сдвинулось и почему это ожидаемо | 13.09.2026, `ac9fdc12` → `55d6ce95` | ✅ · пересъёмка 19 — стенд `:3100` (база `lk_otsfera_e2e` с чистого сида, `FEATURE_TELEPHONY_MANGO=1` + фиктивные ключи, `FEATURE_INBOUND_MESSAGING=1`, `FEATURE_CONTACTS=1`): полный прогон — **273 сверки пройдены, 216 пропущено по проектам, 19 падений — все по картинке, ни одного по правилам экранов** (53 мин). Причины: (1) эталоны кабинетов ЦО сняты до программы «Мессенджеры» и этапа 1 — в меню менеджера не было пунктов «Мессенджеры» и «Контакты» (+76 px), у руководителя и администратора — «Контакты» (+38 px), 16 снимков; (2) карточка организации администратора на мобильном — вкладки «Контакты» и «Заметки» удлинили полосу вкладок (+121 px), 1 снимок; (3) «Интеграции» руководителя — текст блока «Телефония (Mango Office)» с фиктивными ключами отличается от эталона, снятого без них (то самое «1 падение» прогонов №25–№26, 2 снимка). Все 19 пересняты по процедуре, затем **две сверки подряд с паузой больше минуты: 40/40 и 40 passed**. Процедура слева дополнена флагами `FEATURE_INBOUND_MESSAGING=1` и `FEATURE_CONTACTS=1` — эталоны держат состояние после приёмки этапа 1, как «Звонки» с прогона №25 |

## Журнал находок

Одна строка на находку. Вердикт ставится сразу (`❌ дефект` · `⚠ вне объёма` ·
`ℹ шум`); `✅` в колонке «Закрыто» — только после мержа хотфикса в `main`.

> Дефекты `Д-1`…`Д-40`, найденные аудитом 21.08.2026 при заведении ТЗ, сюда
> **не** записаны: они закрываются этапами 1–8 программы (у каждого указано
> требование `У-N` в §3.6 ТЗ), а не сопровождением. Режим сопровождения
> начинается, когда этапов не останется.

| Дата | Проверка | Находка | Вердикт | Закрыто |
|---|---|---|---|---|
| 13.09.2026 | С-10 | **19 эталонов устарели, ни одного падения по правилам экранов.** Эталоны кабинетов ЦО снимались до программы «Мессенджеры» (12.09) и этапа 1 (13.09): в меню не было «Мессенджеры» и «Контакты», в карточке организации — вкладок «Контакты»/«Заметки»; блок «Телефония (Mango Office)» у руководителя снят без фиктивных ключей (то самое «1 падение» прогонов №25–№26). Пересняты по процедуре, две сверки подряд зелёные; процедура `С-10` дополнена флагами стенда | ℹ шум (ожидаемый сдвиг) | ✅ [#595](https://github.com/aiprocadm/lk_otsfera/pull/595) |
| 13.09.2026 | С-6 | Селекты вариантов этапа 1 (`listContactOptions`, `listContactOrgOptions`) усекаются на 500 без подписи — как и прежний `listCompanyOrgOptions`. Селект на полтысячи строк непригоден сам по себе; у справочника контактов есть поиск, форма сделки ведёт туда подсказкой | ℹ шум | — |
| 13.09.2026 | С-5 | Первая проба `docs.commands-exist` легла в `docs/tz/MAINTENANCE.md`, который страж пропускает намеренно (реестры и архив планов описывают то, что было — это записано в шапке стража); повторена на `docs/ARCHITECTURE.md` — поймана | ℹ шум | — |
| 13.09.2026 | С-8 | **Колонки этапа 1 без индексов.** Карточка контакта на вкладках «Заказы» и «Сделки» (и счётчик сделок) фильтрует по `Order.primaryContactId` и `Deal.contactId`; в `schema.prisma` у обеих колонок индекса не было — каждый открытый контакт перебирал таблицы заказов и сделок целиком (у `Order` десять индексов, ни один не начинается с контакта). Миграция `20260913130000_maintenance_contact_lookup_indexes` — только два `CREATE INDEX`; страж `prisma.contact-lookup-indexes` (индексы в схеме + выборка в `get.ts` идёт по этим колонкам), две мутации пойманы | ❌ дефект | ✅ хотфикс №50 [#594](https://github.com/aiprocadm/lk_otsfera/pull/594) |
| 13.09.2026 | С-8 | **Очередь прогона №25 закрыта: фоновые задачи брали всю базу без `take`.** `detectLateRefundCorrections` и `reconcileStuckLeads` после хотфикса №45 перестали ходить в базу на каждую строку, но по-прежнему читали ВСЕ строки выборки без предела; у сверки лидов порядок «сначала старые» отдавал бы пачку неизлечимым претензиям. Перенесён готовый коммит `62b4aa70` из закрытого дублем #577: выборка возвратов от закрытых периодов (предел 500/500), лиды — 200 самых свежих, индекс `Lead(pushedToOneCAt)` (миграция только с индексом), предупреждение при полной пачке; стражи `oneCSync.reconcile`, `services.commission.corrections.unit`, `jobs.background-scan-batching` — пять мутаций пойманы | ❌ дефект | ✅ хотфикс №49 [#593](https://github.com/aiprocadm/lk_otsfera/pull/593) |
| 12.09.2026 | С-5 | **Страж границы суток снимал комментарии в одной своей проверке и не снимал в другой.** `dates.moscow-day-boundary` (дефект `Д-22`, из-за которого ночью удостоверение, истёкшее вчера, считалось действующим) требование «начало суток берут из `startOfMoscowDay`» проверял СЫРЫМ `readFileSync` (`expect(src).toContain('startOfMoscowDay(')`), а запрет `setHours(0,0,0,0)` — уже через `stripComments`. Мутация, которую он пропускал: в `manager/myDay.ts` убрать импорт и вызов, подставить `new Date(now.getFullYear(), now.getMonth(), now.getDate())` (это и есть полночь процесса) и оставить строку-пояснение со словом `startOfMoscowDay(` — страж зелёный, пять из пяти. «Дела на сегодня» могли молча вернуться к вчерашнему дню | ❌ дефект | ✅ закрыт в составе хотфикса №47 [#578](https://github.com/aiprocadm/lk_otsfera/pull/578) — правило усилено второй половиной: страж с проверкой НАЛИЧИЯ вообще не читает код сырым `readFileSync`, только через `readSource`; сырое чтение оставлено лишь для не-кода (`.md`, `.env`, `schema.prisma`, `package.json`). Переведены последние восемь сырых чтений в пяти стражах; всего на помощнике 30 стражей, долг — 11 |
| 12.09.2026 | С-5 / С-8 | **Шаблон стража границы суток видит только одну форму полуночи.** `MIDNIGHT = /setHours\(\s*0\s*,\s*0\s*,\s*0/` ловит `setHours(0,0,0,0)`, но не ловит сборку даты из частей — `new Date(y, m, d)`, — хотя это ровно та же полночь процесса, а не Москвы. Живых мест 7+ (`calculate-monthly-commissions`, `admin/dashboard`, `partner/dashboard`, `lib/calendar/month` и др.), и часть из них **законна** (границы месяца, где часовой пояс роли не играет). Расширять регулярку одним движением нельзя — нужен разбор по каждому месту | ❌ дефект | ⏳ очередь журнала — на прогон №27 вместе с `take` |
| 12.09.2026 | С-5 | Понижение версии пакета правкой одного `package.json`, без переустановки, страж `deps.overrides` не ловит: он смотрит наличие ключа `overrides` и то, что реально лежит в lock. Это не дефект — правду про установленное говорит lock, — но знать полезно, чтобы не считать пробу «страж молчит» находкой | ℹ шум | — |
| 12.09.2026 | С-5 | **Стражи считают комментарий кодом.** 26 стражей читали исходник продукта как есть и проверяли НАЛИЧИЕ строки. Мутацией подтверждено на четырёх (`config.settings-from-ui` — прайм снапшота флагов в `instrumentation.ts` и в воркере; `config.alerts` — токен телеграма в доставке тревог; `email.templates` — подстановка своего шаблона письма; `api.documents-list-scope` — подпись под усечённым списком): достаточно приписать `//` перед строкой, и страж остаётся зелёным, хотя для продукта комментирование и удаление — одно и то же. Удаление строки стражи ловят, комментирование — нет | ❌ дефект | ✅ хотфикс №47 [#578](https://github.com/aiprocadm/lk_otsfera/pull/578) — общий помощник `src/__tests__/helpers/source.ts` (`readSource` = `stripComments` + `readFileSync`), 27 стражей переведены, заведён страж-над-стражами `guards.source-read-strips-comments` с закрытым списком долга из 11 стражей, читающих не только код (`.md`/`.env`/`schema.prisma` — там наивное снятие `//` съело бы ссылки `https://…`); список может только уменьшаться |
| 12.09.2026 | С-5 | **Страж API-роутов засчитывал упоминание в комментарии за гард.** `security.api-route-guard` («каждый API-роут спрашивает права сам») признавал защищёнными три самых чувствительных роута продукта — вход и оба шага 2FA (`api/auth/login`, `api/auth/2fa/verify`, `api/auth/2fa/resend`) — только потому, что в пояснении внутри каждого встречалась строка `getSession()`: регулярка ловит `\bgetSession\(` и не отличает код от комментария. Настоящего гарда там нет и быть не может — все три работают ДО всякой сессии. То есть роуты публичны по замыслу, но исключение из проверки действовало **молча**, без записи и без причины, и любой четвёртый до-сессионный роут попал бы туда так же незаметно. Комментарий приехал вместе с починкой `Д-37` — страж ослеп не сразу, а в момент, когда рядом добавили пояснение | ❌ дефект | ✅ закрыт в составе хотфикса №47 [#578](https://github.com/aiprocadm/lk_otsfera/pull/578) — три роута внесены в `PUBLIC_BY_DESIGN` с объяснением, чем защищены на самом деле (вход — общий лимитер по IP и адресу плюс одинаковый ответ на существующий и несуществующий адрес; оба шага 2FA — pre-auth токен `purpose:"2fa"`, срок и предел числа попыток) |
| 12.09.2026 | С-8 (из очереди) | **Две фоновые задачи спрашивали базу на каждой строке.** `detectLateRefundCorrections` — четыре запроса на возврат (закрытый период, партнёр, история ставок партнёра, история ставок организации), `reconcileStuckLeads` — два на лид (подтверждение от 1С и «уже повторяли»). Беда не в числе запросов самом по себе, а в том, что часть строк из выборки **не уходит никогда**: возврат, чей период так и не закрыли, и лид, принятый 1С без своего номера (`oneCRequestId` в контракте необязателен, поэтому `externalIdInOneC` пуст, а претензия стоит). Каждый прогон перечитывал их заново — работа росла вместе с базой, а не с объёмом дел | ❌ дефект | ✅ хотфикс №45 [#575](https://github.com/aiprocadm/lk_otsfera/pull/575) — обе задачи берут справки одним запросом на прогон (замер стража 121 → 5 и 31 → 2), выборка возвратов отсекает те, у которых партнёра нет ни у заказа, ни у организации (`CommissionCorrection.partnerId` обязателен — корректировки они не получат никогда), выбор периода стал воспроизводимым; страж `jobs.background-scan-batching` считает **число запросов чтения** и падает, если оно снова начнёт расти со строками (три мутации) |
| 12.09.2026 | С-5 | **Два стража не видели файлов верхнего уровня.** `dates.moscow-day-boundary` и `services.no-inmemory-distinct` строили обход шаблоном `git ls-files 'src/lib/**/*.ts'`, а в pathspec гита `**/` требует **хотя бы одну подпапку**. Поэтому шесть файлов верхнего уровня не проверялись никогда: `lib/env.ts`, `lib/featureFlags.ts`, `lib/format.ts`, `lib/quickTasks.ts`, `worker/index.ts`, `worker/to-bull-processor.ts`. Среди них ровно те, ради которых стражи заведены (форматирование дат и «дела на сегодня» — их чинил хотфикс №21, на который ссылается docblock). Мутации `setHours(0, 0, 0, 0)` в `lib/format.ts` и `distinct: [...]` в `lib/quickTasks.ts` прошли зелёными; живых нарушений в этих файлах нет — дыра скрытая | ❌ дефект | ✅ хотфикс №46 [#576](https://github.com/aiprocadm/lk_otsfera/pull/576) — обход по каталогам + самопроверка «список обязан содержать `src/lib/format.ts` и `src/worker/index.ts`» |
| 12.09.2026 | С-8 | **Хвост находки №45: `take` у обеих задач так и нет.** Запрос на строку убран, но выборка по-прежнему не ограничена. Решение найдено и проверено, но не внесено — лимит §9.4 (три хотфикс-PR на прогон) выбран. **Готовое решение:** порядок у лидов меняется на ОБРАТНЫЙ (`orderBy: [{ pushedToOneCAt: 'desc' }, { id: 'desc' }] + take`) — неизлечимые строки имеют самую старую и больше не меняющуюся дату претензии, при `desc` они уходят в ХВОСТ окна, а свежая претензия всегда попадает в первую пачку; у возвратов `[{ paidAt: 'asc' }, { id: 'asc' }] + take`, второй ключ обязателен (иначе соседние заходы видят разные пачки); `log.warn` на полной пачке, чтобы переполнение окна не молчало; индекс `Lead(pushedToOneCAt)` (§9.4 индексы разрешает). Код написан и прогнан в закрытом [#577](https://github.com/aiprocadm/lk_otsfera/pull/577), коммит `62b4aa70`, ветка `fix/hotfix-45-background-jobs-batching` сохранена, **CI зелёный по всем шести job** | ❌ дефект | ⏳ очередь журнала — первой на прогон №27, брать код из `62b4aa70` |
| 12.09.2026 | С-5 | Первая попытка починить `С-10` в прошлом прогоне оставила урок и здесь: **нельзя переключать ветку в копии, где идёт прогон тестов** — файлы меняются под работающим vitest, и его результат перестаёт что-либо значить. Прогон пришлось остановить и повторить | ℹ шум | — |
| 12.09.2026 | С-1 | **Посторонняя копия репозитория внутри репозитория делает гейт не медленным, а МОЛЧАЩИМ.** Рабочая копия в `.claude/worktrees/**` попадает в обход eslint, и `npm run lint` линтует `src/` дважды — на этой машине это `exit 134` (heap OOM) ещё ДО вывода предупреждений. Падение по памяти неотличимо от «инструмента нет» и читается как шум окружения. Реальные последствия в этом же прогоне: параллельная сессия не увидела локально 25 предупреждений (неиспользуемый `readFileSync` в стражах, переведённых на `readSource`) — а гейт идёт с `--max-warnings=0`, то есть это красная сборка. Поймала только серверная проверка на [#578](https://github.com/aiprocadm/lk_otsfera/pull/578). Это тот же класс, что «CI выключен — он не падает, он молчит» (13.08–08.09). **Правило:** worktree удалять сразу после мержа — это не уборка, а починка гейта; локально проверять подпапкой (`npx eslint src/__tests__ --max-warnings=0`), а не обходом всего репозитория. Там же: `npm run typecheck` и полный `test:unit` просят по 4 ГБ и одновременно не помещаются | ℹ шум с последствием | ✅ предупреждения починены в [#578](https://github.com/aiprocadm/lk_otsfera/pull/578) (коммит `cf5493c1`); worktree удалён после мержа [#579](https://github.com/aiprocadm/lk_otsfera/pull/579) |
| 12.09.2026 | С-10 | Падение мобильного эталона карточки интеграций руководителя воспроизвелось с теми же цифрами, что в прогонах №23 и №24 (1274 против 1142, ровно 132 px): эталон снят на стенде БЕЗ ключей Mango. То есть снимок зависит не от вёрстки, а от того, настроена ли телефония — и **оба состояния стенда дают падение на разных снимках**: без ключей расходятся пять экранов из-за пропавшего пункта меню «Звонки», с ключами — этот один | ℹ шум | — |
| 11.09.2026 | С-2 | **«Запросить у клиента» решала по одному счёту.** ФТ-9.5 обещает рядом с погашенной кнопкой выпуска действие «Запросить у клиента» — письмо заказчику со списком недостающего. Пока набор реквизитов был общим на все документы, привязка кнопки к счёту совпадала с обещанием; `У-156` развёл наборы по типам, и договору дополнительно понадобились подписант заказчика, его должность и основание полномочий. У заказчика с полными реквизитами счёта, но без подписанта форма выпуска говорила «не хватает: подписант заказчика (ФИО)», а попросить их было нечем — кнопка не появлялась нигде. Само письмо (`requestRequisites`) собирает недостающее и по счёту, и по договору: механизм построен, но его ничто не запускало | ❌ дефект | ✅ хотфикс №43 [#572](https://github.com/aiprocadm/lk_otsfera/pull/572) — общий `clientRequestLabels` на письмо и на кнопку, отдельная строка «Для договора и доп. соглашения нужно дополнительно», страж `documents.requisites-request-reach` по каждому из четырёх типов |
| 11.09.2026 | С-5 | **Два стража сверяли список сам с собой.** У `config.login-sla` и `help.glossary` «эталонный список» брался ИЗ того же файла, который они проверяют, а сверялись только длина и наличие каждого элемента — такая проверка совпадает всегда. Мутация «заменить `login.inviteTtlDays` дублем `login.rateLimitMax`» прошла зелёной: длина семь, оба ключа в реестре настроек, а «срок действия приглашения» пропал бы с экрана «Политики входа» (`У-129`) и настраивался бы снова через `.env` вопреки `§0.3`. Тем же приёмом из обязательных терминов исчезло «Коммерческое предложение» (`У-76`/`У-175`) | ❌ дефект | ✅ хотфикс №44 [#573](https://github.com/aiprocadm/lk_otsfera/pull/573) — ожидаемые наборы написаны в самих тестах (эталон ТЗ, как у `config.alerts` и `config.onec-params`) + проверки на дубли ключей, имён полей формы и терминов |
| 11.09.2026 | С-8 | `detectLateRefundCorrections` выбирает ВСЕ возвраты без корректировки (без `take`) и делает запрос на каждую строку; пропущенные (возврат без партнёра или без закрытого периода) корректировку не получают никогда и перечитываются каждым прогоном. `reconcileStuckLeads` так же перебирает все зависшие лиды по два запроса на лид. Обе задачи фоновые, вход сегодня мал, но работа растёт вместе с базой. Починка меняет порционность фоновых задач — отдельная правка | ❌ дефект | ⏳ очередь журнала — первой на прогон №26 |
| 11.09.2026 | С-10 | Пять снимков разошлись **только по высоте** (8–38 px, ширина совпала): прод-стенд собран с `FEATURE_TELEPHONY_MANGO=0` из `.env.production`, поэтому в меню нет пункта «Звонки». Пересборка с флагом `1` и фиктивными `MANGO_API_KEY`/`MANGO_API_SALT` (без них `R0.2` намеренно не даёт стартовать) — все пять зелёные. Окружение, не вёрстка; требование к стенду внесено в процедуру `С-10` | ℹ шум | — |
| 11.09.2026 | С-1 | `npm run gate` на этой машине не поднимается: порт 5432 занят своим Postgres, контейнер из compose на него не встаёт, и `migrate deploy` уходит в чужую базу с чужими реквизитами (`P1000`). Не дефект кода — особенность машины; интеграционный слой прогнан напрямую против живого Postgres | ℹ шум | — |
| 11.09.2026 | С-1 | knip печатает `Duplicate exports (1)` (`canSeeOrganization|isOrgInScope` в `managerPolicy.ts`) и при этом выходит нулём — так настроено `rules.duplicates: "warn"` в `knip.json`. Гейт не врёт, решение осознанное | ℹ шум | — |
| 11.09.2026 | С-5 | Первая проба стража `config.onec-params` была вне его объёма: убрал `onec.apiToken`, которого нет в его шестёрке параметров. Повтор на `onec.mode` пойман. Записано, чтобы не искать заново | ℹ шум | — |
| 11.09.2026 | С-7 | **Контракт 1С отстал от кода с 14.06.2026.** `docs/integrations/1c-contract.md` — единственное, что видит команда 1С, а принимает код то, что описано в Zod-схемах `oneCSync/schemas.ts`. Схема заказа и платежа принимает `organizationInn` как второй ключ организации (и требует хотя бы один ключ), платёж — ещё `organizationExternalId`, `purpose`, `paymentOrderNumber`, `vatAmount`; в контракте этих полей не было — 1С не могла узнать, что платёж без `orderExternalId` можно адресовать по ИНН и что назначение платежа доедет до карточки. Стража на это не было: `docs.live-links` проверяет ссылки, а не поля | ❌ дефект | ✅ хотфикс №42 [#570](https://github.com/aiprocadm/lk_otsfera/pull/570) — разделы Orders/Payments дополнены, страж `docs.onec-contract-fields` (каждое поле входящих схем упомянуто в разделе своей операции; образец не обещает лишнего) |
| 11.09.2026 | С-6 | **Мусор в запросе к API ронял роут в 500 вместо 400.** `GET /api/partner/finance` читал `skip`/`take` через `parseInt` (`?skip=abc` → `NaN` в Prisma), `from`/`to` — через `new Date` (`?from=вчера` → `Invalid Date`), `status` — как есть; `POST /api/manager/certificates` принимал `issuedAt: z.string()` и тоже делал `new Date`. Всё это проходило до Prisma и валилось там с 500 `internal` — вместо 400 `invalid_request`, который обещает обвязка роутов (`lib/api/README.md`). Экраны сюда такого не шлют, дыра видна только клиентам API — но 500 значит «сломалось у нас», а не «вы прислали не то» | ❌ дефект | ✅ хотфикс №41 [#569](https://github.com/aiprocadm/lk_otsfera/pull/569) — `parseQuery` по Zod-схеме и общий помощник `dateString` в `lib/api/http.ts`; стражи: шесть видов мусора в строке запроса и три в теле дают 400, не доходя до сервиса |
| 11.09.2026 | С-5 | Страж `ui.empty-states` ловит самодельное пустое состояние только в файлах, где `EmptyState` **не** импортирован: в `calls-list.tsx` (импорт есть) дописанное руками `<p>Пока нет данных</p>` прошло. Страж рассуждает «файл знает про примитив — значит пользуется им»; по факту это сторожит только новые файлы. Живых нарушений нет (проверено гpепом по `components/**`) | ℹ шум | — (усиление стража — при следующем реальном случае) |
| 11.09.2026 | С-5 | Страж `ui.no-internal-jargon` сканирует разметку `src/components` и `src/app` (три пробы — текст, код требования, `placeholder` — пойманы), но не тексты в `src/lib` (`errors/messages.ts`, подписи уведомлений, письма). Утечек там сейчас нет (гpеп по «(этап», `У-`, `Р-`, `Д-` вне комментариев — 0) | ℹ шум | — |
| 11.09.2026 | С-5 | Страж `worker.processor-coverage` ищет имя процессора в тексте тестов: новый процессор без теста пойман, но **закомментированный** импорт (`// import … from '@/worker/processors/expire-proposals'`) считается покрытием. В `worker.expire-proposals.test.ts` такая строка есть, и процессор покрыт живым `expect.stringContaining('expire-proposals')` — дыры нет, но страж держится на совпадении строк | ℹ шум | — |
| 11.09.2026 | С-10 | Эталон `leader-settings-integrations` зависит от окружения стенда: снят без ключей Mango (телефония «не настроено»), а на прод-стенде ключи обязательны для старта, и карточка показывает «проверка не запускалась» — экран выше на 132 px. Не вёрстка и не регресс; пересъёмка сделала бы эталон неверным для dev-стенда. Запомнить при следующем прогоне `С-10` | ℹ шум | — |
| 11.09.2026 | С-6 | Очередь журнала, места 4–5: `admin/users/mutations.ts` (админ заводит пользователя) и `partner/team.ts` (приглашение в команду партнёра) спрашивают «почта занята?» до записи, а Read Committed чужих незакоммиченных строк не показывает — обе одновременные попытки проходят проверку, вторая получает `P2002`, и он улетал наружу. Хватает двойного щелчка по кнопке | ❌ дефект | ✅ хотфикс №38 [#565](https://github.com/aiprocadm/lk_otsfera/pull/565) |
| 11.09.2026 | С-6 | Очередь журнала, места 6–7: `organization/team.ts` и `manager/invite.ts` устроены как «найти пользователя или завести», поэтому текстом не чинятся — при гонке на новый адрес вторая транзакция падала на `P2002` и откатывала вместе с собой привязку к организации, которой человек и добивался. Исправлено одним повтором транзакции: второй заход находит уже заведённого человека | ❌ дефект | ✅ хотфикс №39 [#566](https://github.com/aiprocadm/lk_otsfera/pull/566) |
| 11.09.2026 | С-6 | Новое место того же класса, но с другого конца: `issueFromOrderItem` писал удостоверение **без единой проверки**, хотя `Certificate.orderItemId` уникален. Любой повтор (устаревшая вкладка, второе нажатие, прямой вызов API) давал 500 вместо «удостоверение уже выдано»; роут отвечал 500 там, где верный ответ — 409 | ❌ дефект | ✅ хотфикс №40 [#567](https://github.com/aiprocadm/lk_otsfera/pull/567) |
| 11.09.2026 | С-5 | Проба «рассылка проглочена молча, без `log.warn`» стражем `services.graceful-degrade` не ловится: он проверяет наличие `try`/`.catch` у вызова, а не то, что ошибка записана в журнал. Инвариант §3 («логируем и проглатываем») закрыт наполовину. Греп `С-6` такой случай поймает только при пустых скобках — комментарий внутри `catch { /* … */ }` его прячет | ℹ шум | — |
| 11.09.2026 | С-5 | Проба «мёртвая экспортируемая функция внутри живого модуля» стражем `services.no-test-only-modules` не ловится — он считает импортёров **модуля**, а не каждого экспорта (та же слепая зона, что замечена на `updateStudent` в прогоне №22). На своём объёме страж работает: отдельный модуль, который зовёт только тест, он ловит — проверено мутацией | ℹ шум | — |
| 10.09.2026 | С-6 | Одновременное создание партнёра с занятой почтой отдавало 500 вместо «Email уже зарегистрирован.»: проверка занятости стоит внутри транзакции, а Read Committed не показывает незакоммиченных строк другой транзакции — обе проходят проверку, вторая получает `P2002` на записи, и `catch` пробрасывал его дальше. Гонка достигается и двойным щелчком по кнопке «Создать». Данные целы — единственность держит база. Находка из очереди прогона №21, там воспроизвести на базе разработки не удалось; воспроизведена разбором кода и стражами на поведении | ❌ дефект | ✅ хотфикс №35 [#561](https://github.com/aiprocadm/lk_otsfera/pull/561) |
| 10.09.2026 | С-6 | Двух сотрудников с одной почтой в организации база не пускает (`@@unique([organizationId, email])`, `У-21`), а поиск дублей `У-22` сравнивает **ФИО + почту** — у однофамильца совпадения нет, и `P2002` улетал наружу: диалог «Новый сотрудник» падал вместо текста. Гонка не нужна — общий ящик отдела у двух людей обычное дело. Воспроизведено на живой базе (в транзакции с откатом): `P2002, target ["organizationId","email"]`. Та же дыра в правке сотрудника (`updateStudent`, `У-23`) | ❌ дефект | ✅ хотфикс №36 [#562](https://github.com/aiprocadm/lk_otsfera/pull/562) |
| 10.09.2026 | С-6 | Третье место того же класса: предпросмотр импорта сотрудников (`У-28`) не видел повтора почты (тот же ключ `У-22`), строка уходила в создание, а запись идёт одной транзакцией — `P2002` ронял **весь** импорт без номера строки. Человек не знал, какую из двухсот строк править | ❌ дефект | ✅ хотфикс №37 [#563](https://github.com/aiprocadm/lk_otsfera/pull/563) |
| 10.09.2026 | С-6 | Та же гонка «проверка занято → запись» ещё в четырёх точках приглашения людей: `admin/users/mutations.ts` (админ создаёт пользователя), `partner/team.ts` (`email_taken`), `organization/team.ts` и `manager/invite.ts` (там схема «найти или создать» — чинится не текстом, а одним повтором транзакции). Лимит трёх хотфиксов §9.4 исчерпан — в очередь журнала, прогон №23 начинает с неё | ❌ дефект | ✅ хотфикс №38 [#565](https://github.com/aiprocadm/lk_otsfera/pull/565) · №39 [#566](https://github.com/aiprocadm/lk_otsfera/pull/566) (прогон №23) |
| 10.09.2026 | С-6 | `POST /api/admin/training-directions` принимает `slug`, а `createDirection` не разбирает `P2002` по `TrainingDirection.slug` — повтор даёт 500 вместо 400. Экран направлений `slug` не посылает вовсе (поле остаётся `NULL`, их в Postgres можно много), поэтому человеку это не видно: дотянуться можно только прямым вызовом API от администратора | ℹ шум | — |
| 10.09.2026 | С-1 | Один раз в pre-commit (`vitest --changed --bail=1`) упал один тест из 79 на том же дереве; имя в перехваченном выводе не сохранилось. Одиннадцать повторов (3 × `--changed`, 8 × целевые файлы) зелёные, CI на этом же коде зелёный. Записано, чтобы при повторе не искать заново — кандидат в «плавающие», как хотфикс №27 | ℹ шум | — |
| 05.09.2026 | С-5 | Страж `components.upload-size-hint.guardrail` ловит только однострочную подсказку «Максимум 200 МБ.»; при переносе prettier (`Максимум 200{' '}` + новая строка + `МБ.`) поломка цифры проходит (2 passed) | ❌ дефект | ✅ хотфикс №2 — #499 (сверка файла целиком, 4 мутации пойманы) |
| 05.09.2026 | С-7 | `docs/feature-flags-matrix.md` описывает 19 флагов, в `featureFlags.ts` их 30: нет `leader_analytics`, `contacts`, `staff_chat`, `staff_calendar`, `global_search`, `client_requests` и др.; стража «документ ↔ реестр» нет | ❌ дефект | ✅ хотфикс №1 — #498 (страж `docs.feature-flags-matrix`, 5 мутаций пойманы) |
| 05.09.2026 | С-9 | `npm audit --omit=dev`: 14 уязвимостей (next 15.5.22, sharp 0.34.5, mailparser, nanoid, ip-address, fast-uri, browserslist, html-to-text, js-yaml, brace-expansion, postcss-selector-parser) — все чинятся патч/минор-обновлением без `--force` | ❌ дефект | ✅ хотфикс №3 — #500 (`npm audit fix` без `--force`: 14 → 7, только lock-файл; остаток — мажоры/откат, В-2) |
| 05.09.2026 | С-6 | Запись аудита `recordAudit(...).catch(() => {})` без `log` в потоке входа: `api/auth/login/route.ts:169`, `api/auth/2fa/verify/route.ts:70,94`, `api/auth/2fa/resend/route.ts:88`, `server-actions/staff/backupCodes.ts:25`, `lib/services/auth/sessions.ts:30`; там же `writeSyncLog(...).catch(() => {})` в `worker/processors/mango-backfill.ts:111`. Проглатывать нужно (вход не должен ломаться), но молча — нет: образец `.catch((e) => log.warn(...))` в `dlq/retry-all/route.ts:34` | ❌ дефект | ✅ решение `Р-25` (по поручению заказчика, 05.09.2026) — PR [#505](https://github.com/aiprocadm/lk_otsfera/pull/505): микро-спека, хелпер `bestEffort(label)` → `log.warn`, 8 мест + 2 `.catch(() => undefined)` в `clientRequests/attachments.ts`; страж `errors.no-silent-catch.guardrail`, 6 мутаций пойманы |
| 05.09.2026 | С-6 | Списки усекаются без счётчика «показано N из M»: `services/documents/generalList.ts:24` (`take: 200`), `services/commission/corrections.ts:139` (200), `services/admin/pendingRecords.ts:52` (100) | ❌ дефект | ✅ хотфикс №4 — [#501](https://github.com/aiprocadm/lk_otsfera/pull/501) (прогон №2; сервисы отдают `total`, примитив `ListCapNotice`, 8 мутаций пойманы) |
| 05.09.2026 | С-6 | `/partner/finance` показывает 30 последних комиссионных отчётов (`app/partner/finance/page.tsx`, `listStatements(…, { take: 30 })`) без подписи и без пути к остальным — старые только в выгрузке Excel; у сервиса уже есть `skip`/`take`, примитив `Paginator` есть | ❌ дефект | ✅ хотфикс №5 — [#502](https://github.com/aiprocadm/lk_otsfera/pull/502) (прогон №2; `countStatements` по общему `statementsWhere`, `Paginator` на странице, 5 мутаций пойманы) |
| 05.09.2026 | С-6 | Доски сделок / воронки / задач (`deals/board.ts:76`, `funnel/board.ts`, `tasks/board.ts:133`) берут 500 новейших карточек **включая закрытые и выполненные** и молчат; при переполнении старые «живые» карточки исчезнут с доски. Диалог задачи (`tasks/board.ts:198`, `task-dialog.tsx:155`) — селекты организаций (200 по алфавиту) и заявок (100 новейших) без поиска | ⚠ вне объёма | ✅ решение `Р-27` (по поручению заказчика, 05.09.2026) — PR [#507](https://github.com/aiprocadm/lk_otsfera/pull/507): доски сортируют `status asc, createdAt desc` (терминальные значения enum последними — в предел 500 первыми попадают открытые), рядом честный `count` и подпись «Показаны первые N из M» на 6 страницах; справочники диалога задачи — по последней активности со счётчиком, текущая привязка не теряется. Стражи `prisma.enum-terminal-last.guardrail` и `boards.status-order.integration`, 9 мутаций пойманы. Комбобокс с поиском — отдельная спека по запросу |
| 05.09.2026 | С-6 | Прочие `take` без счётчика ограничены самой моделью: `admin/alerts.ts:39` (100, ключ = правило), `deals/notes.ts:73` (200 на сделку), `manager/orderDetail.ts:39` (50 записей аудита), `staffChat/mentions.ts:59` (200), `calendar/items.ts` (окно дат), точечные `take: 1/2/5`; `api/enrollments/students` отдаёт свой предел в `meta.take` | ℹ шум | — |
| 05.09.2026 | С-7 | Рабочие чеклисты зовут несуществующее: `npm run worker:start` в `docs/qa-staging-smoke-manager.md:23`, `docs/qa-staging-smoke-organization.md:19`, `docs/runbook-staged-rollout-cabinets.md:65` (в `package.json` — `worker`); `dist/scripts/backfill-order-organization-id.js` в `qa-staging-smoke-organization.md:18` — скрипт удалён после миграции `NOT NULL` | ❌ дефект | ✅ хотфикс №6 — [#503](https://github.com/aiprocadm/lk_otsfera/pull/503) (прогон №2; команда поправлена, шаг бэкфилла заменён пояснением, страж `docs.commands-exist.guardrail`, 3 мутации пойманы) |
| 05.09.2026 | С-7 | В документе заказчика `docs/tz/2026-08-04-tz-fix-1c-import.md` скрипты названы `.mjs` (`inspect-1c-xlsx`, `backfill-orphan-companies`), реализованы как `.ts`; `npm run report:legacy-enrollments` в ТЗ/AUDIT/STATUS — одноразовый отчёт `У-34а`, снят после разбора (`ab2a0ae6`). Документы заказчика и реестры не правятся — страж их не сканирует | ℹ шум | — |
| 05.09.2026 | С-9 | Мажорные обновления: `next` 15.5 → 16.3.4, `vitest` 4 → 5; у `prisma`/`exceljs` у `npm audit` нет фикса, только откат | ⚠ вне объёма | ✅ решение `Р-26` (по поручению заказчика, 05.09.2026) — хотфикс №7 PR [#506](https://github.com/aiprocadm/lk_otsfera/pull/506): дырявы были транзитивные `postcss@8.4.31`/`uuid@8.3.2`/`deepmerge-ts@7.1.5`, а не сами пакеты; `overrides` в `package.json`, `npm audit --omit=dev` = 0; мажоры не тронуты. Dev-хвост (`vitest`/`vite`/`esbuild`, 6 записей) — принятый риск, пересмотр на каждом `С-9`; страж `deps.overrides.guardrail`, 3 мутации пойманы |
| 05.09.2026 | С-6 | `Sentry.flush(2000).catch(() => {})` при аварийном выходе воркера; `twoFactorChallenge.delete(...).catch` (челлендж уже мог истечь); `recordLastLogin` (комментарий объясняет); 13 × `as unknown as` и 112 × `!` — все с обоснованием | ℹ шум | — |
| 05.09.2026 | С-8 | Циклы с запросами: `oneCSync/corrections.ts:38`, `oneCSync/invoicePaidNotice.ts:58`, `import/matcher.ts:41`, `oneCSync/reconcile.ts:164` — наборы ограничены (строки одной выписки, документы одного заказа) | ℹ шум | — |
| 05.09.2026 | С-1 | `npm run deadcode` (knip) на `12c3078a`: новый мёртвый экспорт `BOARD_CAP` в `services/deals/board.ts:76` — след PR [#507](https://github.com/aiprocadm/lk_otsfera/pull/507): тест доски сделок проверял `take: 500` числом, а не константой (у воронки и задач константа импортирована тестом `boards.cap-and-order.unit`) | ❌ дефект | ✅ хотфикс №8 — [#508](https://github.com/aiprocadm/lk_otsfera/pull/508) (прогон №3; тест `services.deals.board` импортирует `BOARD_CAP`, проверяет `take` и само значение; knip снова 5 экспортов = известный долг; 3 мутации пойманы) |
| 05.09.2026 | С-5 | `navigation.breadcrumbs-coverage` не замечает переименование пропа `breadcrumbs=` на странице (вьюха всё равно рендерит `<Breadcrumbs>`), фолбэк `menuGroupRank → 0` не ловится никем (ветка недостижима — чужие группы запрещает сам страж) | ℹ шум | — (чужой проп ловит `tsc`, мутацию во вьюхе страж поймал) |
| 05.09.2026 | С-6 | Шесть `.catch(() => undefined)` в `src/e2e/screen-rules-check.ts` — обход экранов Playwright глотает ошибки проб нарочно, вне охвата стража `errors.no-silent-catch` (`src/**` без e2e) | ℹ шум | — |
| 05.09.2026 | С-8 | Справочник организаций диалога задачи (`getTaskFormOptions`, `Р-27`) сортируется по `updatedAt` внутри `companyId` без составного индекса `[companyId, updatedAt]` — Postgres сортирует организации компании в памяти | ℹ шум | — (сотни–тысячи строк сортируются за миллисекунды; индекс = миграция, §9.4 — вернуться, если у компании станет больше ~10 000 организаций) |
| 05.09.2026 | С-10 | Доски сделок/воронки/задач не входят в визуальные эталоны (`src/e2e/snapshots/` снимает дашборды и карточки); после `Р-27` их держат обход §0 (`С-3`, 6 из 6 на 390 px) и unit-тесты страниц | ℹ шум | — (новый эталон — не хотфикс; при следующей пересъёмке эталонов добавить доски) |
| 05.09.2026 | С-1 | `npm run gate` не запустить: на сервере нет Docker; integration-слой прогнан напрямую против живого Postgres (`lk_otsfera_e2e`) | ℹ шум | — |
| 05.09.2026 | С-3, С-10 | Dev-сервер стенда `:3100` (`next dev`) через ~3 ч работы упал с `JavaScript heap out of memory` (heap 19 ГБ): 74 проверки получили `ERR_CONNECTION_REFUSED`. После перезапуска `--last-failed` 80/80 зелёные, диффов по картинке нет. Перед полным прогоном стенд перезапускать; при повторе — стенд на `next build && next start` | ℹ шум | — |
| 05.09.2026 | С-10 | `organization-team-modal-focus-trap` › «background is inert» на `mobile-organization`: `dialog[open] input[name=email]` не найден после клика (клик до гидратации в dev-режиме); соседний тест с тем же кликом прошёл, перегон прошёл | ℹ шум | — |
| 06.09.2026 | С-5 | Страж `navigation.page-title-matches-menu.guardrail` молчит: читает только буквальные `<h1>`, а 89 страниц разделов ставят заголовок через `<PageHeader title=…>` — заголовков он не находил и проходил вхолостую (мутация «Заказы» → «Заявки» в `manager/orders/page.tsx` не поймана) | ❌ дефект | ✅ хотфикс №9 — [#510](https://github.com/aiprocadm/lk_otsfera/pull/510) (прогон №4; страж читает `title="…"`, `title={'…'}`, `title={sectionLabel('…')}` — 69 из 89 разделов, и падает, если прочитал меньше 50; 3 мутации пойманы) |
| 06.09.2026 | С-6 | Карточка организации (`services/manager/organizationCard.ts`): 12 вкладок-списков по `take: 20` без счётчика и подписи — во всех пяти кабинетах; плитка «Задолженность» (`У-102`) считалась `reduce` по показанным 20 заказам, а не по всем | ❌ дефект | ✅ хотфикс №10 — [#511](https://github.com/aiprocadm/lk_otsfera/pull/511) (прогон №4; `ORG_CARD_TAB_CAP`, `count` по тем же `where` → `tabTotals`, `ListCapNotice` под вкладками, задолженность — `order.aggregate`; страж `services.organizationCard.tab-caps.guardrail`, 7 мутаций пойманы) |
| 06.09.2026 | С-6 | Список переписок «Сообщения» (`services/chat/threads.ts:91`, `listThreads`, `take: 50`, «No cursor pagination in v1») без счётчика и подписи — в пяти кабинетах | ❌ дефект | ✅ хотфикс №11 — [#512](https://github.com/aiprocadm/lk_otsfera/pull/512) (прогон №4; `THREADS_CAP`, `count` по тому же `where` → `total`, `ListCapNotice` в `OrderThreadInbox`, страницы передают `total`; 6 мутаций пойманы) |
| 06.09.2026 | С-6 | Списки без счётчика, ограниченные самой моделью: колокольчик `notifications/inbox.ts` (50 новейших — старые уведомления не нужны, есть «прочитать все»), staff-чат `staffChat/conversations.ts` (100 бесед / 200 сообщений в беседе — объёмы компании кратно меньше). Подпись усечения тут не нужна | ℹ шум | — |
| 06.09.2026 | С-6 | `partner/orgComments.ts` (`listOrgOrderComments`) в проде не используется — экран партнёра ушёл на вкладки карточки (`organizationCard.ts`), сервис жив только тестом `security.idor-comments.integration`; knip его не видит (импорт из теста считается использованием). Убрать сервис вместе с тестом или перевести тест на карточку — хотфикс прогона №5 (лимит §9.4 в прогоне №4 исчерпан) | ❌ дефект | ✅ хотфикс №12 — [#514](https://github.com/aiprocadm/lk_otsfera/pull/514) (прогон №5): оба остатка партнёрской карточки удалены (`orgHistory.ts` — тот «1 файл» долга knip), тест изоляции переведён на `getOrganizationCard`, страж `services.no-test-only-modules.guardrail` |
| 06.09.2026 | С-6 | `req.json().catch(() => null)` / `req.formData().catch(() => null)` в роутах разбора тела (`partner/team`, `company/branding`, `2fa/verify` и др.): `null` дальше не проходит схему и превращается в `400` с кодом ошибки — не тихое глотание, страж `errors.no-silent-catch` держит это в allow-list по смыслу | ℹ шум | — |
| 06.09.2026 | С-8 | `EnrollmentRequest` не имеет индекса по `organizationId`; после хотфикса №10 по нему идёт `count` карточки организации. Таблица — сотни строк, seq scan дешевле индекса; при росте до десятков тысяч — индекс отдельной миграцией (миграции хотфиксам запрещены §9.4) | ℹ шум | — |
| 06.09.2026 | С-10 | `organization-team-modal-focus-trap` › «focus restores to trigger after Escape» на `org-desktop`: фокус не вернулся к кнопке за отведённое время при первом заходе (dev-сервер ещё собирал маршрут, 8 с); `--last-failed` зелёный. Тот же класс, что запись 05.09 (гидратация) | ℹ шум | — |
| 06.09.2026 | С-10 | `organization-team-modal-focus-trap` › «focus restores to trigger after Escape» упал ВТОРОЙ прогон подряд (№4 — `org-desktop`, №5 — `mobile-organization`), оба раза зеленея на `--last-failed`: тест открывал модалку `focus()` + `Enter`, а обработчик на кнопке появляется только после гидратации — нажатие уходило в никуда. Не шум, а нестабильный страж: полный прогон краснеет на ровном месте | ❌ дефект | ✅ хотфикс №13 — [#515](https://github.com/aiprocadm/lk_otsfera/pull/515) (прогон №5): открытие через `openModal()` с `expect(...).toPass()`, 3 мутации `dialog.tsx` пойманы |
| 06.09.2026 | С-6 | Списки фильтров админского зеркала (`admin/auditLog.ts:106`, `admin/piiAccess.ts:181`, `admin/users/queries.ts:221` — `distinct` с `take: 200`) счётчика «показано N из M» не требуют: это варианты для выпадающего списка, а не выдача пользователю | ℹ шум | — |
| 06.09.2026 | С-10 | При частых прогонах Playwright подряд падает `auth.setup` («authenticate as leader») — упирается в лимит входа 10/мин (`У-133`), а не в дефект. Между прогонами одного файла держать паузу ~1,5 мин | ℹ шум | — |
| 06.09.2026 | С-6 | `revalidatePath` покрывает экраны объекта неравномерно: действия сделок (`server-actions/deals/index.ts`) обновляют доски и список лидов, но не воронку (`/manager|leader/funnel`), хотя `convertLeadToDeal` меняет лиду `status` и `promotedDealId`. Практического эффекта нет: все затронутые страницы динамические (`force-dynamic` либо чтение `cookies()` в `requireSession`), серверного кэша у них не бывает, а клиентский `staleTimes` не настроен — вызовы здесь декларация намерения, а не механизм корректности | ℹ шум | — |
| 06.09.2026 | С-6 | Все 38 non-null `!` в боевом коде проверены поимённо: каждый прикрыт проверкой рядом — `length > 0`, `filter(Boolean)`, гарантированная группа регулярного выражения, `split` всегда с одним элементом. Опасных нет; у части (`rateResolve`, `parser`, `scan/backfill`, `chat/threads`) обоснование записано комментарием | ℹ шум | — |
| 06.09.2026 | С-5 | Страж `shell.command-palette` сторожит проп `searchEnabled`, но соседний `searchHref` (ссылка «Показать все результаты») не проверяет — первая проба мутацией прошла мимо инварианта. Дыры нет: ссылка отрисовывается только внутри `{wantSearch && …}`, то есть при `searchEnabled`; правильная мутация (`searchEnabled` заказчику) стражем поймана | ℹ шум | — |
| 06.09.2026 | С-7 | Три действующих рабочих документа (`qa-staging-smoke-manager.md`, `qa-staging-smoke-organization.md`, `runbook-staged-rollout-cabinets.md`) в шаге «обновить memory-референс» ссылаются на `../../../C:/Users/<имя>/.claude/…` — путь с машины автора: файла нет ни в репозитории, ни на сервере, по ссылке не пройти никому. Ссылки в документах, кроме `MAINTENANCE.md`, никто не сторожил | ❌ дефект | ✅ хотфикс №14 — [#518](https://github.com/aiprocadm/lk_otsfera/pull/518) (прогон №7): шаг описан словами, новый страж `docs.live-links.guardrail` на все действующие `docs/**` |
| 06.09.2026 | С-7 | Ещё две ссылки в никуда — в разделе `STATUS.md` «Разведка по коду» (`partner/orgEmployees.ts`, `org-employees-tab.tsx`, удалены при переезде партнёрской карточки). Раздел помечен датой и коммитом `92c683e`, это снимок прошлого — править его §9.4 запрещает; страж ссылок исключает его осознанно | ℹ шум | — |
| 06.09.2026 | С-2 | `Company.proposalValidDays` (умолчание срока действия КП, `У-162`) правится только в базе: соседняя настройка той же модели `defaultVatRate` есть в форме «Налоги и нумерация», а этой в интерфейсе нет. Требование выполнено — умолчание берётся из настройки, а не из константы, и срок каждого КП менеджер задаёт руками; экрана для правки умолчания требование не обещает. Понадобится — это микро-спека (поле в существующую форму), не хотфикс | ℹ шум | — |
| 06.09.2026 | С-5 | Проба «со страницы `/manager/calls` снят `requireManager()`» не поймана ни одним стражем — дыры нет: префиксы кабинетов закрывает `middleware.ts` по реестру `protectedPrefixes`, а он под стражем `security.role-model-inventory` («точный снимок префикс → роли»). Страж телефонии сторожит гард ФЛАГА, роль — не его инвариант | ℹ шум | — |
| 07.09.2026 | С-4 | Серверные действия — открытые HTTP-эндпоинты (идентификатор экспорта лежит в клиентском бандле, `middleware.ts` смотрит на путь страницы и такой POST не остановит), но автоматической проверки «у действия есть гард» не было: `server-actions.async-exports` сторожит только форму экспорта. Разобраны все 189 действий — дыр нет (прямой гард, `getSession()` с проверкой или делегирование в соседа), правило держалось на дисциплине | ❌ дефект | ✅ хотфикс №15 — [#520](https://github.com/aiprocadm/lk_otsfera/pull/520) (прогон №8): страж `server-actions.session-guard.guardrail`, 4 мутации |
| 07.09.2026 | С-4 | Тот же класс, что №15, но для `/api/**`: `matcher` middleware начинается с `(?!api|…)` — по роли роуты не закрыты вовсе, дверь только внутри обработчика, и автоматической проверки не было. Разобраны все 83 обработчика — дыр нет: вебхуки сверяют секрет и подпись (fail-closed 401), сброс пароля идёт по одноразовому токену и ограничен по IP и адресу, `health/live` отдаёт только `{status:"ok"}` | ❌ дефект | ✅ хотфикс №16 — [#522](https://github.com/aiprocadm/lk_otsfera/pull/522) (прогон №9): страж `security.api-route-guard.guardrail`, 6 мутаций |
| 07.09.2026 | С-8 | Списки значений для фильтров собирались `findMany({ distinct })`: Prisma считает `distinct` в памяти приложения — в базу уходит выборка БЕЗ `DISTINCT` и БЕЗ `LIMIT` даже при указанном `take` (проверено логом запросов на стенде). Страница «Аудит» дважды тянула в процесс весь журнал аудита (самая быстрорастущая таблица), страница ПДн — журнал просмотров; вдобавок `take: 200` обрезал результат ПОСЛЕ выборки, и в фильтр попадали не все исполнители | ❌ дефект | ✅ хотфикс №17 — [#524](https://github.com/aiprocadm/lk_otsfera/pull/524) (прогон №10): оба места на `groupBy`, страж `services.no-inmemory-distinct.guardrail`, 6 мутаций |
| 07.09.2026 | С-8 | `/api/documents` отдавал все документы платформы: ни `take`, ни счётчика, а карточка заказа в админке отбирала нужные три строки уже в браузере (`docs.filter(...)`). Роль не при чём — роут admin-only (Model A), но на каждый заход на карточку уезжали метаданные всех документов системы | ❌ дефект | ✅ хотфикс №18 — [#526](https://github.com/aiprocadm/lk_otsfera/pull/526) (прогон №11): фильтр по заказу в запросе, `take` + `count` по тому же условию, ответ `{ rows, total }`, подпись усечения, страж `api.documents-list-scope.guardrail` |
| 07.09.2026 | С-8 | Многошаговые записи без транзакции: 12 функций пишут в 2+ модели подряд. Разобраны все — критичных две. Смена статуса заказа меняла `Order.statusId` отдельно от записи в `OrderStatusChange` (заказ мог остаться в новом статусе без строки в истории); снятие участника партнёра меняло `PartnerUser.isActive` отдельно от `User.sessionVersion++` (снятый работал бы до истечения токена). Остальные десять — фоновые задачи и импорт, где частичная запись переигрывается повторным заходом | ❌ дефект | ✅ хотфикс №19 — [#530](https://github.com/aiprocadm/lk_otsfera/pull/530) (прогон №13): оба места в `prisma.$transaction`, рассылки и аудит снаружи, страж `services.write-atomicity.guardrail` |
| 08.09.2026 | С-8 | Границы суток считались `setHours(0, 0, 0, 0)` — полночь в зоне процесса, а серверы живут в UTC (`TZ` не задан, `date +%Z` = UTC). С 00:00 до 03:00 МСК граница указывала на предыдущие сутки московского календаря: удостоверение, истёкшее вчера, ночью считалось действующим. Наружу всё рендерится в `Europe/Moscow` — тот же дефект `Д-22`, что уже решён для года в номере документа | ❌ дефект | ✅ хотфикс №20 — [#532](https://github.com/aiprocadm/lk_otsfera/pull/532) (прогон №14): общий `startOfMoscowDay`, переведены удостоверения и выгрузка слушателей, страж `dates.moscow-day-boundary.guardrail` |
| 08.09.2026 | С-8 | Те же границы суток остались в «делах на сегодня» (`manager/myDay.ts`) и на дашбордах заказчика и партнёра — правка однотипная, но в лимит трёх файлов бизнес-логики одного PR (§9.4) не вошла. Места перечислены в страже `dates.moscow-day-boundary` поимённо с причиной, страж следит, чтобы список не рос молча | ❌ дефект | ✅ хотфикс №21 — [#533](https://github.com/aiprocadm/lk_otsfera/pull/533) (тот же прогон №14): все три места на `startOfMoscowDay`, очередь стража пуста, он сверяет все пять мест |
| 08.09.2026 | С-6 | Сохранение шаблона письма (`saveTemplateAction`) принимало тему и тело любой длины: колонки `NotificationTemplate` — `text` без границы, проверялись только подстановки. Вставленная страница уезжала в базу и в письмо, а почтовый шлюз такое отвергает. У шаблонов документов предел есть (`SLOT_TEXT_MAX`), у писем не было | ❌ дефект | ✅ хотфикс №22 — [#535](https://github.com/aiprocadm/lk_otsfera/pull/535) (прогон №15): `EMAIL_SUBJECT_MAX`/`EMAIL_BODY_MAX` и `checkTemplateLength` в реестре писем, отказ до записи с русским объяснением, страж |
| 08.09.2026 | С-10 | Падение `auth.setup` останавливает ВЕСЬ прогон: 474 теста получают «did not run». В прогоне №16 дашборд заказчика на холодном стенде компилировался дольше таймаута 60 с — повторный запуск на прогретом стенде прошёл. Не дефект приложения, но стоит помнить: первый прогон после подъёма стенда может оборваться на входе | ℹ шум | — |
| 08.09.2026 | С-10 | Браузерные проверки гонялись только руками: регресс интерфейса мог доехать до `main` незамеченным. В CI добавлен job `e2e · правила экранов` — 290 структурных проверок на **прод-сборке** за ~4 минуты (в dev-режиме тот же объём занимал больше часа: там маршруты собираются на первом заходе, отсюда и таймауты предыдущих прогонов) | ❌ дефект | ✅ [#539](https://github.com/aiprocadm/lk_otsfera/pull/539) |
| 08.09.2026 | С-1 | **CI был выключен в настройках репозитория** (`disabled_manually`) с 13.08.2026 — почти месяц. Все PR-ы этого времени, включая 40+ моих, уезжали в `main` без единой серверной проверки, хотя шапка `ci.yml` и `CLAUDE.md` §6 называют CI обязательным гейтом, который «обойти нельзя». Заметить было нечем: отключённый workflow не падает, он молчит, а `gh pr checks` отвечает «no checks reported» — то же, что при ещё не запустившемся прогоне | ❌ дефект | ✅ workflow включён 08.09.2026; добавлен ручной запуск `workflow_dispatch`, чтобы состояние гейта можно было проверить в любой момент — [#544](https://github.com/aiprocadm/lk_otsfera/pull/544) |
| 08.09.2026 | С-1 | Первый прогон CI после включения показал накопленное за месяц. `schema.prisma` разошёлся с миграциями: в базе есть уникальный ключ `Order(id, companyId)` и составной внешний ключ `Document(orderId, companyId)` — защита «компания документа не может разойтись с компанией заказа» (этап 6), — а в схеме их не было. Следующая `prisma migrate dev` молча снесла бы обе | ❌ дефект | ✅ [#540](https://github.com/aiprocadm/lk_otsfera/pull/540): ключ описан в схеме; внешний Prisma выразить не умеет (`ON DELETE SET NULL ("orderId")` по одной колонке пары) — заведён `npm run schema:drift` с поимённым списком таких исключений |
| 08.09.2026 | С-1 | `npm run verify` (полная лестница) был красным на двух шагах, и заметить это было нечем: локально я гоняю шаги по отдельности, сервер молчал. `format:check` — 138 файлов (prettier обновился внутри мажора до 3.9.6 и изменил перенос union-типов; последний общий прогон был 31.07). `deadcode` — 5 мёртвых экспортов и 19 типов | ❌ дефект | ✅ [#541](https://github.com/aiprocadm/lk_otsfera/pull/541): общий прогон prettier, экспорты разобраны по существу (снят `export` там, где сущность нужна только внутри файла; лишний реэкспорт убран из бареля) |
| 08.09.2026 | С-10 | Браузерные проверки в CI поднимались, но job висел 30 минут на `prisma:seed`: seed транзитивно тянет прод-код с клиентами очередей, и заданный, но мёртвый `redis://localhost:6379` заставлял его бесконечно переподключаться. Затем четыре админских экрана («Здоровье системы», «Обмен», дашборд) не открывались даже за 120 с — по той же причине | ❌ дефект | ✅ [#540](https://github.com/aiprocadm/lk_otsfera/pull/540): заглушки перенесены на шаг запуска сервера, Redis поднят service-контейнером. Итог: 118 проверок за 2 минуты, весь CI зелёный |
| 08.09.2026 | С-9 | **Разведка мажорного обновления `vitest` 2 → 5** (единственный способ закрыть dev-хвост `Р-26`). Обновление ставится: `npm audit` = **0 уязвимостей**, простые тесты идут. Но vitest 5 перешёл на бандлер Rolldown, и тот не разбирает JSX в `.tsx`: **442 файла из 1109 падают** с `RolldownError: Unexpected JSX expression`, плюс изменилось поведение моков (`is not a constructor` у фабрик без `new`). Настройка `esbuild.jsx` не помогает — у Rolldown своя. Это не доводка, а работа с полной перепроверкой 11 952 тестов: §9.4 прямо называет мажоры отдельной спекой. Откачено, `package-lock.json` восстановлен | ⚠ вне объёма | Риск `Р-26` остаётся принятым: в проде этих пакетов нет, `npm audit --omit=dev` = 0, CI проверяет только прод-зависимости. Спека — по запросу заказчика |
| 08.09.2026 | С-10 | `/organization/team` (шлюз для писем-приглашений) не доводил до управления участниками: страница с `redirect()` при стриминге RSC отдаёт 200 и уводит на клиенте, параметр вкладки терялся — человек попадал на «Обзор» без списка участников и кнопки «Пригласить». Страж `organization-team-modal-focus-trap` падал СТАБИЛЬНО, а не флейком: перегон `--last-failed` не помогал | ❌ дефект | ✅ хотфикс №23 — [#544](https://github.com/aiprocadm/lk_otsfera/pull/544) (прогон №17): заведён список `LEGACY_REDIRECTS` в `next.config.mjs` (кабинет заказчика в реестр хаба настроек не входит), честный `307` на `?tab=settings`, тест карты дополнен |
| 08.09.2026 | С-10 | Эталоны картинок в CI НЕ переносятся: сняты на dev-сервере, а прод-сборка даёт высоту страницы на 8 px меньше (нет dev-оверлея Next) — четыре снимка менеджера разошлись при проверке. Плюс шрифты CI-образа отличаются от серверных. Сравнение снимков остаётся локальной проверкой `С-10`; в CI ушла только структура, которая от шрифтов не зависит | ℹ шум | — |
| 08.09.2026 | С-10 | Полезное следствие: локальный прогон структурных проверок тоже стоит гонять на `next build && next start` — 3,6 мин против часа, и исчезает главный источник флейков (компиляция маршрута на первом заходе). Снимки при этом снимаются и сверяются на dev-стенде, как раньше | ℹ шум | — |
| 08.09.2026 | С-10 | Полный прогон эталонов на dev-стенде дважды встал: 143 и 189 проверок из 196, падений 0, дальше движения нет (в первый раз сервер упал совсем). Это известная просадка `next dev`, на прод-сборке её нет, но эталоны сняты в dev и туда не переносятся. Вывод для процедуры: гонять эталоны порциями, перезапуская стенд, либо мириться с остановкой у самого конца | ℹ шум | — |
| 08.09.2026 | С-1 | Прогон впервые проверен **сервером**: CI зелёный на всех шести job, включая браузерные проверки. До этого `С-1` опиралась только на мою машину — и месяц никто не видел, что серверные проверки выключены | ℹ шум | — |
| 09.09.2026 | С-9 | `npm audit --omit=dev` впервые с 05.09.2026 не нулевой: `imapflow` (чтение почтового ящика «Входящих») держит у себя копию `nodemailer@9.0.3`. Опасна GHSA-2x7j-588g-ccc2 — разбор списка адресов квадратичен по длине: одно входящее письмо с длинным составленным списком вешает разбор почты, а отправить его может кто угодно. Рядом два обхода проверки домена получателя. Безопасная версия `9.1.1` — патч, чинится `overrides` по `Р-26` | ❌ дефект | хотфикс №24 [#546](https://github.com/aiprocadm/lk_otsfera/pull/546) |
| 09.09.2026 | С-8 | **Постраничные списки листаются неустойчиво.** PostgreSQL при равных ключах сортировки порядок не обещает, а `ORDER BY … LIMIT n OFFSET m` считает top-N heapsort — соседние страницы сортируются независимо. Проверено на живой базе: из 60 показанных строк уникальных 58, одна показана трижды, две не показаны вовсе. Совпадения ключа — норма: умолчание `createdAt` = `CURRENT_TIMESTAMP` = время начала транзакции, поэтому пачка из одного импорта имеет одинаковый `createdAt`. Страж нашёл **16** таких списков; закрыты три самых уязвимых, остальные 13 — в очереди стража | ❌ дефект | хотфикс №25 [#547](https://github.com/aiprocadm/lk_otsfera/pull/547) |
| 09.09.2026 | С-8 | Продолжение: три самых посещаемых списка кабинета заказчика (заказы, документы, слушатели) — из той же очереди. Заказы приезжают пачкой из импорта 1С и принятых КП, документы одного заказа выпускаются одной транзакцией, слушатели заводятся списком из файла: у всей пачки `createdAt` совпадает | ❌ дефект | хотфикс №26 [#548](https://github.com/aiprocadm/lk_otsfera/pull/548) |
| 09.09.2026 | С-8 | Продолжение по очереди: три списка кабинета партнёра — заявки, документы и комиссионные отчёты. Отчёты показательнее всего: сортировка по началу периода, а у всех отчётов одного месяца это одна дата по определению — порядок страниц не был определён вовсе. Заодно уточнён масштаб: постраничных выборок в проекте **33**, неустойчивых было **19** (в прогоне №18 записано «16» — недосчёт: тогда сравнивались файлы, а не выборки) | ❌ дефект | хотфикс №28 [#551](https://github.com/aiprocadm/lk_otsfera/pull/551) |
| 09.09.2026 | С-8 | Продолжение по очереди: обращения заказчиков, лиды менеджера, входящие письма. Лиды серьёзнее прочих — там листание курсором, и при равных `createdAt` соседи встают вокруг курсора как попало. Очередь: 7 мест | ❌ дефект | хотфикс №29 [#552](https://github.com/aiprocadm/lk_otsfera/pull/552) |
| 09.09.2026 | С-8 | Продолжение по очереди: админские списки организаций, партнёров и людей. Очередь: 4 места | ❌ дефект | хотфикс №30 [#554](https://github.com/aiprocadm/lk_otsfera/pull/554) |
| 09.09.2026 | С-8 | Продолжение по очереди: сотрудники в карточке организации, звонки, комиссионные отчёты администратора. Очередь: 1 место | ❌ дефект | хотфикс №31 [#555](https://github.com/aiprocadm/lk_otsfera/pull/555) |
| 09.09.2026 | С-1 | **Тест «протух» по календарю.** Утром полный прогон зелёный, днём тот же тест на том же коде красный: в фикстурах `services.import.history` стояли абсолютные даты августа, и разница до «сегодня» перевалила за окно отката импорта (30 дней, `Т-40`). Продукт исправен — сломал календарь. Даты переведены в «дней назад»; соседние тесты с окнами разобраны поимённо, мина была одна. Приём на будущее: в `С-1` смотреть не только «зелено ли сейчас», но и нет ли в фикстурах абсолютных дат рядом с проверкой срока | ❌ дефект | хотфикс №33 [#557](https://github.com/aiprocadm/lk_otsfera/pull/557) (сверх лимита §9.4 — `main` был красным) |
| 09.09.2026 | С-8 | **НДС при разборе выписки терял копейку.** Когда в назначении указана только ставка, сумма налога считалась в рублях (`amount * rate / (100 + rate)`): произведение в двоичной дроби чуть-чуть не дотягивает до половины копейки, и округление уводит вниз. Перебор 200 000 сумм: ошибается каждый 35-й платёж (2,8 %). Бухгалтер сверяет НДС с 1С по копейке. Расчёт переведён в целые копейки — перебор 300 000 сумм по четырём ставкам расхождений не нашёл. Единственное место, где деньги считались в плавающей точке: комиссии и заказы идут через `Decimal`, сверка оплат — уже в копейках | ❌ дефект | хотфикс №34 [#559](https://github.com/aiprocadm/lk_otsfera/pull/559) |
| 09.09.2026 | С-4 | При одновременном создании партнёра с одинаковым адресом почты проверка «уже есть» внутри транзакции не спасает: база отдаст `P2002`, а `catch` пробрасывает всё, кроме своих ошибок, — человек увидит 500 вместо «такой адрес занят». Данные при этом целы (уникальность держит база). Воспроизвести на базе разработки не удалось: она замусорена прогонами, и подготовка данных падает раньше на внешнем ключе. Записано как наблюдение — вред ограничен видом ошибки | ℹ шум | очередь журнала |
| 09.09.2026 | С-8 | Проверено делом, не ухудшили ли хвосты `id` (хотфиксы №25–№32) работу с базой: на таблице в 200 000 строк с индексом по `createdAt` план остался Index Scan, добавился лишь Incremental Sort по группам с равными ключами — прочитано 202 строки вместо 120. Новых индексов не требуется | ℹ шум | — |
| 09.09.2026 | С-8 | **Очередь закрыта полностью.** Последний пункт — разбор карточек 1С: порядок собирался функцией по выбору человека, и неустойчивы были все четыре варианта. Для очереди разбора это дороже обычного списка: строку разобрали бы дважды, а другую не увидели бы вовсе, и платёж остался бы непривязанным. Итог направления: все 19 неустойчивых выборок закрыты, список исключений стража пуст | ❌ дефект | хотфикс №32 [#556](https://github.com/aiprocadm/lk_otsfera/pull/556) |
| 09.09.2026 | С-8 | Смежное направление: выборки с **обрезкой (`take`) без листания** — их 121, у 101 порядок неустойчив. В отличие от страниц, строки тут не теряются: список показывается целиком до обрезки, а на её границе состав «дрожит» между обновлениями. У пользовательских списков рядом стоит счётчик «показаны N из M» (хотфиксы №4 и №5), у досок действует `Р-27` («открытые первыми»). Отдельный случай — `resolveContactByChannel` с `take: 2` без сортировки — умышленный приём: «ровно один или ничего, никогда не угадывать». Чинить это как дефект не нужно; аккуратный хвост стоит добавлять по мере правок соседнего кода | ℹ шум | — |
| 09.09.2026 | С-7 | Записи хотфиксов №25–№28 от 09.09.2026 дописывались в раздел **уже выпущенного** релиза 0.11.0 (08.09.2026) — читается как «это было в релизе». Перенесены в «Не выпущено» и упорядочены. Причина проста: скрипт вставлял по первому заголовку «Исправлено», а он принадлежит разделу релиза | ℹ шум | исправлено в [#551](https://github.com/aiprocadm/lk_otsfera/pull/551) |
| 09.09.2026 | С-1 | `components.team-visibility-toggle` — **плавающий тест**: «повторное нажатие убирает прежнюю ошибку» упал в CI по таймауту `waitFor`, локально трижды подряд прошёл, перезапуск того же job — зелёный. Второе нажатие приходит, пока кнопка ещё в состоянии отправки, и на медленной машине не срабатывает. Красит CI без причины; чинить — в очередь (лимит §9.4 на прогон исчерпан) | ℹ шум | очередь журнала |
| 09.09.2026 | С-1 | Причина того «плавания» найдена и воспроизведена локально (1 падение на 12 прогонов под нагрузкой): кнопка блокируется на время отправки, а сообщение об ошибке и снятие блокировки — два разных коммита React; клик в промежуток между ними теряется. Экран правильный, тест торопился | ❌ дефект | хотфикс №27 [#550](https://github.com/aiprocadm/lk_otsfera/pull/550) |
| 09.09.2026 | С-10 | Прод-стенд для обхода экранов надо поднимать на базе `lk_otsfera_e2e`, а не на базе разработки из `.env`: в базе разработки нет демо-пользователя партнёра (её перетирают integration-прогоны, 41 пользователь вида `acc-*@t.local`), и вход падал 2 минуты, выглядя как зависший дашборд партнёра. Пять остальных ролей входили за секунду — это и подсказало, что дело в данных | ℹ шум | — |
| 09.09.2026 | С-5 | Мутация «подменить импорт `allowedChannels` локальной функцией с тем же именем» стражем `notifications.rules` не ловится: он ищет вызов по имени. Инвариант при этом цел (никто так не пишет), проба вне его — записано, чтобы не искать заново | ℹ шум | — |
| 08.09.2026 | С-7 | Версия проекта стояла 0.10.0 с 12.07.2026, в разделе «Не выпущено» накопился 271 пункт за 757 коммитов и пять закрытых программ ТЗ. По поручению заказчика (08.09.2026) нарезан релиз **0.11.0** — минорный, а не 1.0.0: продукт внутренний, «единица» означала бы заявление о стабильности внешнего контракта | ❌ дефект | ✅ [#537](https://github.com/aiprocadm/lk_otsfera/pull/537) |
| 08.09.2026 | С-4 | Сценарная приёмка §7 п.3 («КП из сделки для организации без реквизитов → отправка → принятие → заказ») проходилась только руками, отметки о проходе в документах нет; каждый шаг покрыт по отдельности, принятие — на моках. По поручению заказчика живая приёмка заменена воспроизводимой | ❌ дефект | ✅ [#537](https://github.com/aiprocadm/lk_otsfera/pull/537): тест `acceptance.proposal-to-order.integration` проходит путь целиком на живом Postgres, включая проверку «повторное принятие второй заказ не плодит» |
| 07.09.2026 | С-8 | `listOrderStudentOptions` (селект слушателей на карточке заказа) выбирает без `take`, а защитная ветка `organizationId === null` даёт пустой `where` — то есть всех слушателей платформы. Ветка недостижима в реальных данных (`Order.organizationId` не бывает null, это записано в самом коде), список одной организации ограничен по природе. Взять на заметку, если появятся организации с тысячами слушателей | ℹ шум | — |
| 07.09.2026 | С-5 | `navigation.breadcrumbs-coverage`: проба «у страницы убран проп `breadcrumbs=`» снова не поймана — страж считает цепочку импортов, и крошки рисует любой компонент в ней (`page-header.tsx`). Это та же ℹ, что записана 05.09 в прогоне №3, а не новый регресс | ℹ шум | — |
| 07.09.2026 | С-1 | Полный unit после мержа хотфикса №18 упал: в моке `services.documents.legacyApi` не было `count`, добавленного в сервис. Прогонял тесты роута, панели и страниц, но не весь список потребителей — доводка [#527](https://github.com/aiprocadm/lk_otsfera/pull/527) влита сразу, `main` был красным ~15 минут | ❌ дефект | ✅ доводка [#527](https://github.com/aiprocadm/lk_otsfera/pull/527) |
| 07.09.2026 | С-5 | Проба «панель выпуска документов собрана в мёртвую переменную» стражем `documents.generate-panel-mirror` не поймана — он сторожит монтирование компонента, а не присваивание. Гейт держит: `npm run lint` даёт красный `prefer-const` (и предупреждение `no-unused-vars`) | ℹ шум | — |

## Стражи, проверенные мутацией (для С-5)

Полный проход 19 стражей — 15.08.2026 (журнал в [AUDIT.md](AUDIT.md)). Круг
С-5 идёт по алфавиту именем файла `src/__tests__/*.guardrail.test.ts`;
первый круг замкнут 06.09.2026 (прогон №6): все 66 стражей проверены
мутацией. Второй круг замкнут 11.09.2026 (прогоны №7–№24); стражи,
заведённые хотфиксами по ходу, проверялись мутацией при создании (строки
ниже). Третий круг идёт с 11.09.2026 (прогон №24 — первые четыре по алфавиту,
прогон №25 — `config.alerts`, `config.env-example`, `config.env-registry`,
`config.login-sla`, `config.onec-params`, плюс `help.glossary` сверх круга;
прогон №26 — `config.settings-from-ui`, `config.telephony-webhooks`,
`config.upload-formats`, `dates.moscow-day-boundary`, `deps.overrides`, плюс
`services.no-inmemory-distinct` сверх круга, и параллельной сессией —
`config.settings-from-ui`, `config.alerts`, `email.templates`,
`api.documents-list-scope`, `security.api-route-guard`,
`config.telephony-webhooks`, `deps.overrides`). **`config.upload-formats`
мутацией не проверялся** — прогон №27 начинает с него, затем продолжает со
стража после `deps.overrides`.
Новые стражи и дата их мутационной проверки:

| Страж | Проверен мутацией |
|---|---|
| `docs.tz-program` (якоря реестра, сводка) | 19.08.2026 |
| правило ширины экранов / §0 живой обход | 20–21.08.2026 |
| `docs.tz-program` → `режим сопровождения` (колонка «Последний прогон», строки `С-N`, ссылки) | 21.08.2026 (три поломки, все пойманы) |
| `acceptance.audit-registry` (разбор строк AUDIT.md, отметка даты) | 05.09.2026 (этап 9, PR-3) |
| `auth.teamMode-required` | 05.09.2026 ✓ поймана |
| `components.no-db-import` | 05.09.2026 ✓ поймана |
| `components.upload-size-hint` | 05.09.2026 ✓ однострочная поймана · ✗ перенос prettier не пойман → хотфикс №2 #499: ✓ пойманы 4 (перенос prettier с цифрой, однострочная, перенос перед цифрой, голый перенос JSX-текста) |
| `config.alerts` | 05.09.2026 ✓ поймана |
| `config.env-example` | 05.09.2026 ✓ поймана |
| `config.env-registry` | 05.09.2026 ✓ поймана |
| `config.login-sla` | 05.09.2026 ✓ поймана |
| `config.onec-params` | 05.09.2026 ✓ поймана |
| `config.settings-from-ui` | 05.09.2026 ✓ поймана |
| `config.telephony-webhooks` | 05.09.2026 ✓ поймана |
| `config.upload-formats` | 05.09.2026 ✓ поймана (прогон №2) |
| `documents.document-template` | 05.09.2026 ✓ поймана (прогон №2) |
| `documents.generate-panel-mirror` | 05.09.2026 ✓ поймана (прогон №2) |
| `documents.send-button-mirror` | 05.09.2026 ✓ поймана (прогон №2) |
| `documents.superseded-versions` | 05.09.2026 ✓ поймана (прогон №2) |
| `e4.no-network` | 05.09.2026 ✓ поймана (прогон №2) |
| `email.templates` | 05.09.2026 ✓ поймана (прогон №2) |
| `errors.codes-covered` | 05.09.2026 ✓ поймана (прогон №2) |
| `featureFlags.route-gated` | 05.09.2026 ✓ поймана (прогон №2) |
| `featureFlags.third-gate` | 05.09.2026 ✓ поймана (прогон №2) |
| `help.glossary` | 05.09.2026 ✓ поймана (прогон №3) |
| `import.no-second-writer` | 05.09.2026 ✓ поймана (прогон №3): второй писатель в `matcher.ts` |
| `import.org-name-key` | 05.09.2026 ✓ поймана (прогон №3) |
| `navigation.breadcrumbs-coverage` | 05.09.2026 ✓ поймана (прогон №3): крошки убраны из вьюхи `manager-order-detail-view`; переименование пропа на странице — вне инварианта, ловит `tsc` (ℹ) |
| `navigation.icons` | 05.09.2026 ✓ поймана (прогон №3) |
| `navigation.menu-groups` | 05.09.2026 ✓ поймана (прогон №3): чужая группа `'Сбыт'` в `cabinet.ts`; фолбэк `menuGroupRank → 0` недостижим (ℹ) |
| `navigation.mirror` | 05.09.2026 ✓ поймана (прогон №3) |
| `navigation.org-card-registry-usage` | 05.09.2026 ✓ поймана (прогон №3) |
| `navigation.org-card-tabs` | 05.09.2026 ✓ поймана (прогон №3) |
| `navigation.org-card-tiles` | 05.09.2026 ✓ пойманы 2 (прогон №3) |
| `navigation.org-settings-sections` | 06.09.2026 ✓ поймана (прогон №4) |
| `navigation.page-title-matches-menu` | 06.09.2026 ✗ не поймана (прогон №4): переименование заголовка страницы через `PageHeader title=` страж не читал — только буквальные `<h1>` → хотфикс №9 (строка хотфикса ниже) |
| `navigation.partner-orders-rename` | 06.09.2026 ✓ поймана (прогон №4) |
| `navigation.partner-org-card` | 06.09.2026 ✓ поймана (прогон №4) |
| `navigation.personal-settings` | 06.09.2026 ✓ поймана (прогон №4) |
| `navigation.quick-tasks` | 06.09.2026 ✓ поймана (прогон №4) |
| `navigation.same-section-same-name` | 06.09.2026 ✓ поймана (прогон №4) |
| `navigation.without-inn-filter` | 08.09.2026 ✓ поймана (прогон №17, круг 2): из списка организаций убран фильтр «без ИНН» |
| `notifications.registry` | 08.09.2026 ✓ поймана (прогон №17, круг 2): у вида уведомления пропал текст-образец |
| `notifications.rules` | 09.09.2026 ✓ поймана (прогон №18, круг 2): рассылка организации перестала спрашивать правила маршрутизации |
| `oneCSync.pushable-types` | 09.09.2026 ✓ пойманы 2 (прогон №18, круг 2): в список типов для 1С добавлен `proposal` только в схеме — умолчание Prisma и CHECK базы разошлись |
| `pages.subtitles` | 09.09.2026 ✓ поймана (прогон №18, круг 2): экран заказов заказчика нарисовал `<h1>` мимо `PageHeader` |
| `pii.capture-coverage` | 09.09.2026 ✓ поймана (прогон №19, круг 2): у списка слушателей подменён контекст записи доступа к ПДн — `manager_students_list` больше не пишется. Две пробы до этого оказались вне инварианта: страж смотрит только файлы реестра и только наличие вызова, а в файле их два |
| `prisma.enum-terminal-last` | 09.09.2026 ✓ поймана (прогон №19, круг 2): живое значение `negotiating` дописано после терминальных в `DealStatus` |
| `prisma.migrations-plain-sql` | 09.09.2026 ✓ пойманы 2 (прогон №19, круг 2): в файл миграции дописан цветной баннер `prisma` с управляющими символами |
| `security.api-route-guard` | 09.09.2026 ✓ поймана (прогон №20, круг 2): заведён API-роут без проверки вызывающего. Первая проба была вне инварианта — страж берёт список роутов через `git ls-files`, поэтому неотслеживаемый файл не виден (в PR такого не бывает) |
| `security.client-visibility` | 09.09.2026 ✓ пойманы 2 (прогон №20, круг 2): внутреннее поле `assignedManagerId` в клиентском сервисе; воскрешён раздел лидов у партнёра. Проба «импорт типа `Lead`» — вне инварианта: страж следит за полями и разделами, а не за импортами |
| `security.document-download-name` | 09.09.2026 ✓ поймана (прогон №21, круг 2): роут скачивания подписал ссылку без имени файла. Первая проба была вне инварианта — мутировал роут выгрузки отчёта партнёра, а страж следит за тремя роутами документов |
| `security.document-read-fields` | 09.09.2026 ✓ поймана (прогон №21, круг 2): из выборки документа убран `type` перед вызовом `canReadDocument` |
| `security.document-status` | 09.09.2026 ✓ поймана (прогон №21, круг 2): статус документа записан прямым `update` мимо матрицы переходов |
| `lib.navigation.settings-redirect` (хотфикс №23) | 08.09.2026 ✓ пойманы 3: переезд ведёт на раздел без вкладки; адрес выпал из карты; один и тот же адрес в двух списках |
| `navigation.without-inn-filter` | 06.09.2026 ✓ поймана (прогон №4) |
| `notifications.registry` | 06.09.2026 ✓ поймана (прогон №4) |
| `notifications.rules` | 06.09.2026 ✓ поймана (прогон №4) |
| `oneCSync.pushable-types` | 06.09.2026 ✓ поймана (прогон №5): коммерческое предложение в списке выгружаемых типов |
| `pages.subtitles` | 06.09.2026 ✓ поймана (прогон №5): проп `subtitle` стал необязательным |
| `pii.capture-coverage` | 06.09.2026 ✓ поймана (прогон №5): контекст переименован мимо реестра |
| `prisma.enum-terminal-last` | 06.09.2026 ✓ поймана (прогон №5): доска сделок сортирует только по дате |
| `prisma.migrations-plain-sql` | 06.09.2026 ✓ поймана (прогон №5): баннер CLI в SQL-миграции |
| `security.client-visibility` | 06.09.2026 ✓ поймана (прогон №5): внутреннее поле `assignedManagerId` в партнёрском сервисе |
| `security.document-download-name` | 06.09.2026 ✓ поймана (прогон №5): ссылка подписана без имени файла |
| `security.document-read-fields` | 06.09.2026 ✓ поймана (прогон №5): выборка документа без `type` |
| `security.document-status` | 06.09.2026 ✓ поймана (прогон №5): прямой `document.update` со `status` мимо двери |
| `security.order-deal-visibility` | 06.09.2026 ✓ поймана (прогон №5): панель сделки в кабинете заказчика |
| `security.role-access-matrix` | 06.09.2026 ✓ поймана (прогон №6): руководителем считается и менеджер |
| `security.role-model-inventory` | 06.09.2026 ✓ поймана (прогон №6): седьмая роль в модели |
| `security.settings-matrix` | 06.09.2026 ✓ поймана (прогон №6): админский раздел настроек открыт руководителю |
| `server-actions.async-exports` | 06.09.2026 ✓ поймана (прогон №6): синхронный экспорт в файле `use server` |
| `services.graceful-degrade` | 06.09.2026 ✓ поймана (прогон №6): рассылка без `try`/`catch` роняет основное действие |
| `services.no-test-only-modules` | 06.09.2026 ✓ поймана (прогон №6, свежий круг): модуль сервисов без импортёров |
| `services.organizationCard.tab-caps` | 06.09.2026 ✓ поймана (прогон №6): голое `take: 20` вместо константы |
| `services.requisites.partner-org` | 06.09.2026 ✓ поймана (прогон №6): право на запись реквизитов подменено правом на чтение |
| `services.students` | 06.09.2026 ✓ поймана (прогон №6): слушателя заводит тот, у кого только право чтения |
| `shell.command-palette` | 06.09.2026 ✓ поймана (прогон №6): заказчику включён поиск по данным (`searchEnabled`); проба через `searchHref` — вне инварианта, см. журнал |
| `ui.empty-states` | 06.09.2026 ✓ поймана (прогон №6): самодельное пустое состояние мимо `EmptyState` |
| `ui.no-internal-jargon` | 06.09.2026 ✓ поймана (прогон №6): код требования `У-112` в разметке |
| `worker.processor-coverage` | 06.09.2026 ✓ поймана (прогон №6): процессор воркера без единого теста |
| `auth.teamMode-required` | 06.09.2026 ✓ поймана (прогон №7, круг 2): у `teamMode` появилось значение по умолчанию |
| `components.no-db-import` | 06.09.2026 ✓ поймана (прогон №7, круг 2): компонент импортирует prisma |
| `components.upload-size-hint` | 06.09.2026 ✓ поймана (прогон №7, круг 2): захардкоженное «200 МБ» в разметке |
| `config.alerts` | 06.09.2026 ✓ поймана (прогон №7, круг 2): порог читается мимо настроек |
| `config.env-example` | 06.09.2026 ✓ поймана (прогон №7, круг 2): переменная из кода не задокументирована |
| `config.env-registry` | 06.09.2026 ✓ поймана (прогон №7, круг 2): `process.env` вне реестра `У-122` |
| `config.login-sla` | 06.09.2026 ✓ поймана (прогон №7, круг 2): пропало запасное чтение `INVITE_TOKEN_TTL_DAYS` |
| `config.onec-params` | 06.09.2026 ✓ поймана (прогон №7, круг 2): границы объявлены, но не проверяются (первая проба — в чужом файле, вне инварианта) |
| `config.settings-from-ui` | 06.09.2026 ✓ поймана (прогон №7, круг 2): роут 2FA не праймит снапшот флагов |
| `config.telephony-webhooks` | 06.09.2026 ✓ поймана (прогон №7, круг 2): со страницы звонков снят гард флага (первая проба снимала гард роли — вне инварианта, см. журнал) |
| `config.upload-formats` | 06.09.2026 ✓ поймана (прогон №7, круг 2): роут завёл свою копию списка MIME |
| `deps.overrides` | 07.09.2026 ✓ поймана (прогон №8, круг 2): из `package.json` убран override `postcss` |
| `docs.commands-exist` | 07.09.2026 ✓ поймана (прогон №8, круг 2): README зовёт несуществующую `npm run` |
| `documents.document-template` | 07.09.2026 ✓ поймана (прогон №8, круг 2): текст договора просочился в файл печати КП |
| `documents.generate-panel-mirror` | 07.09.2026 ✓ поймана (прогон №8, круг 2): со страницы заказа руководителя убрана панель выпуска (проба «панель собрана в мёртвую переменную» — вне инварианта, её ловит `lint`) |
| `documents.send-button-mirror` | 07.09.2026 ✓ поймана (прогон №8, круг 2): у администратора на карточке документа пропал проп `canSend` |
| `documents.superseded-versions` | 07.09.2026 ✓ поймана (прогон №9, круг 2): из канальной политики убран фильтр действующей версии |
| `e4.no-network` | 07.09.2026 ✓ поймана (прогон №9, круг 2): тест зовёт `fetch` без подмены |
| `email.templates` | 07.09.2026 ✓ поймана (прогон №9, круг 2): своё письмо шлётся мимо общей вёрстки |
| `errors.codes-covered` | 07.09.2026 ✓ поймана (прогон №9, круг 2): у кода сервиса не стало русского текста |
| `errors.no-silent-catch` | 07.09.2026 ✓ поймана (прогон №9, круг 2): тихий `.catch(() => {})` в сервисе |
| `featureFlags.route-gated` | 07.09.2026 ✓ поймана (прогон №10, круг 2): middleware завёл свою копию списка префиксов |
| `featureFlags.third-gate` | 07.09.2026 ✓ поймана (прогон №10, круг 2): из layout кабинета убрана проверка своего флага (проба через `notFound` — вне инварианта: слово остаётся в импорте) |
| `help.glossary` | 07.09.2026 ✓ поймана (прогон №10, круг 2): термин назван, но не объяснён |
| `import.no-second-writer` | 07.09.2026 ✓ поймана (прогон №10, круг 2): прямая запись `prisma.order.update` в слое импорта |
| `navigation.org-settings-sections` | 07.09.2026 ✓ поймана (прогон №13, круг 2): у секции настроек пустое пояснение (проба «разошлось название» — вне инварианта: реестр общий на все кабинеты) |
| `navigation.page-title-matches-menu` | 07.09.2026 ✓ поймана (прогон №13, круг 2): подпись раздела «Заказы» разошлась с заголовком экрана |
| `navigation.org-card-registry-usage` | 07.09.2026 ✓ поймана (прогон №12, круг 2): страница карточки перестала звать `orgCardTabsFor` |
| `navigation.org-card-tabs` | 07.09.2026 ✓ поймана (прогон №12, круг 2): из реестра вкладок убрана вкладка «Документы» |
| `navigation.org-card-tiles` | 07.09.2026 ✓ поймана (прогон №12, круг 2): подпись плитки «Заказы» переименована в «Заявки» |
| `import.org-name-key` | 07.09.2026 ✓ поймана (прогон №11, круг 2): писатель организаций потерял `nameKey` |
| `navigation.icons` | 07.09.2026 ✓ поймана (прогон №11, круг 2): у раздела «Заказы» подменён значок — разошлась пара «подпись ↔ значок» |
| `navigation.menu-groups` | 07.09.2026 ✓ пойманы 2 (прогон №11, круг 2): чужая группа `«Сбыт»` в реестре меню |
| `navigation.mirror` | 07.09.2026 ✓ поймана (прогон №11, круг 2): у руководителя убран пункт «Документы», который есть у менеджера |
| `navigation.partner-org-card` | 08.09.2026 ✓ поймана (прогон №14, круг 2): страница портфеля партнёра перестала звать реестр вкладок |
| `navigation.partner-orders-rename` | 08.09.2026 ✓ поймана (прогон №14, круг 2): пункт меню партнёра назван «Заявки на обучение» вместо «Заказы» |
| `navigation.quick-tasks` | 08.09.2026 ✓ пойманы 2 (прогон №16, круг 2): плитка быстрой задачи ведёт на несуществующий экран |
| `navigation.same-section-same-name` | 08.09.2026 ✓ поймана (прогон №16, круг 2): «Обращения» переименованы в «Запросы» — один объект стал называться по-разному |
| `navigation.personal-settings` | 08.09.2026 ✓ поймана (прогон №15, круг 2): в реестре личных настроек переименован ключ вкладки |
| `services.stable-pagination-order.guardrail` (хотфикс №25) | 09.09.2026 ✓ пойманы 5: у удостоверений снят хвост `id`; у портфеля хвост подменён неуникальным `createdAt`; у курсорного списка заявок снят хвост; новый постраничный список вообще без сортировки; починенный файл забыли убрать из очереди |
| `services.stable-pagination-order.guardrail` (хотфикс №28) | 09.09.2026 ✓ пойманы 3: у заявок партнёра снят хвост `id`; у отчётов хвост подменён неуникальным `periodTo`; починенные документы партнёра вернули в очередь |
| `services.stable-pagination-order.guardrail` (хотфикс №29) | 09.09.2026 ✓ пойманы 3: у обращений снят хвост `id`; у курсорного списка лидов хвост подменён неуникальным `updatedAt`; починенные входящие вернули в очередь |
| `services.stable-pagination-order.guardrail` (хотфикс №30) | 09.09.2026 ✓ пойманы 3: у списка организаций снят хвост `id`; у партнёров хвост подменён неуникальным `inn`; починенный список людей вернули в очередь |
| `services.stable-pagination-order.guardrail` (хотфикс №31) | 09.09.2026 ✓ пойманы 3: у сотрудников снят хвост `id`; у звонков хвост подменён неуникальным `startedAt`; починенные отчёты вернули в очередь |
| `services.stable-pagination-order.guardrail` (хотфикс №32) | 09.09.2026 ✓ пойманы 3: у очереди 1С снят хвост `id`; хвост подменён неуникальным `paidAt`; в опустевшую очередь вписано новое исключение |
| `services.stable-pagination-order.guardrail` (хотфикс №26) | 09.09.2026 ✓ пойманы 3: у заказов заказчика снят хвост `id`; у документов хвост подменён неуникальным `signedAt`; починенный список слушателей вернули в очередь |
| `email.templates.guardrail` · `email.templateRegistry` (хотфикс №22) | 08.09.2026 ✓ пойманы 4: проверка длины убрана; проверка переехала ПОСЛЕ записи в базу; предел переписан числом на экране; отказ показан не по-русски |
| `dates.moscow-day-boundary.guardrail` (хотфикс №21) | 08.09.2026 ✓ пойманы 2: «дела на сегодня» вернулись к полуночи процесса; список PENDING объявлен непустым, хотя все места починены |
| `dates.moscow-day-boundary.guardrail` · `lib.dates.calendar` (хотфикс №20) | 08.09.2026 ✓ пойманы 4: удостоверения вернулись к полуночи процесса; новое место с `setHours(0,0,0,0)`; место из очереди объявлено, но уже починено; хелпер считает границу в зоне процесса |
| `services.write-atomicity.guardrail` (хотфикс №19) | 07.09.2026 ✓ пойманы 4: статус меняется вне транзакции; история пишется вне транзакции; снятие участника снова без гашения сессий внутри; рассылка заехала внутрь транзакции |
| `api.documents-list-scope.guardrail` · `api.documents.list` · `components.documents-panel` (хотфикс №18) | 07.09.2026 ✓ пойманы 5: у выборки убран `take`; счётчик считает по другому условию; роут перестал читать `orderId`; панель вернулась к отбору в браузере (упал и страж, и тест панели); пропала подпись усечения |
| `services.no-inmemory-distinct.guardrail` · `services.admin.auditLog` · `services.admin.piiAccess` · `pii.access-journal.integration` (хотфикс №17) | 07.09.2026 ✓ пойманы 6: фильтры аудита вернулись к `findMany({ distinct })`; `distinct` в новом сервисе; из исключения убран `distinct`, а запись осталась; причина исключения стёрта; фильтр ПДн вернулся к `distinct + take` (список неполный — упал integration); журнал аудита снова читается построчно |
| `security.api-route-guard.guardrail` (хотфикс №16) | 07.09.2026 ✓ пойманы 6: новый роут без гарда; у роута сняли `requireManager`; вебхук перестал сверять секрет; в список публичных вписан роут с гардом; причина исключения стёрта; `matcher` middleware начал смотреть на `/api` |
| `server-actions.session-guard.guardrail` (хотфикс №15) | 07.09.2026 ✓ пойманы 4: новое действие без гарда; у действия сняли `requireSession`; обёртка перестала делегировать в действие с гардом; файл гардов из правила исчез |
| `docs.live-links.guardrail` (хотфикс №14) | 06.09.2026 ✓ пойманы 4: ссылка на `C:/Users/…` вернулась; относительная ссылка в никуда; ссылка на `/home/…`; каталог-снимок в списке исключений перестал существовать |
| `docs.feature-flags-matrix` (хотфикс №1) | 05.09.2026 ✓ пойманы 5: строка флага убрана; opt-out в таблице opt-in; счётчик шапки; призрак `partner_leads`; чужой класс в runbook |
| `services.documents.generalList` · `services.commission.corrections.unit` · `services.admin.pendingRecords.unit` · `components.ui-list-cap-notice` · `components.admin-pending-records-section` · страницы documents/corrections/1c-auto (хотфикс №4) | 05.09.2026 ✓ пойманы 8: `count` не зовётся (×2), `count` по другому `where`, условие `<` вместо `<=`, подпись не рисуется на странице, секция подставляет `records.length`, страница 1С теряет `total`, очереди корректировок передают 0 |
| `services.partner.finance` · `pages.partner-finance` (хотфикс №5) | 05.09.2026 ✓ пойманы 5: `count` не зовётся (total = 0), `skip` из адреса не уходит в сервис, `MAX_TAKE` не режет, `Paginator` убран со страницы, счётчик без `status`/`from`/`to` |
| `docs.commands-exist.guardrail` (хотфикс №6) | 05.09.2026 ✓ пойманы 3: в README появилась `npm run` несуществующего скрипта, в runbook — ссылка на несуществующий `scripts/*.ts`, из `package.json` пропал `worker` |
| `errors.no-silent-catch.guardrail` · `logging.bestEffort` (решение `Р-25`, `В-1`) | 05.09.2026 ✓ пойманы 6: тихий `.catch(() => {})` вернулся в `sessions.ts`; пустой `catch {}` без комментария в `admin/dashboard.ts`; исключение убрано из allow-list (стало «лишнее вхождение»); allow-list просит 3 вместо 2 в `worker/index.ts`; `bestEffort` перестал писать `warn` (упали 6 тестов — хелпер и четыре расширенных); `.catch(() => undefined)` вернулся в `attachments.ts` |
| `services.organizationCard.tab-caps.guardrail` · `services.organizationCard.integration` · `components.org-card-tabs` (хотфикс №10) | 06.09.2026 ✓ пойманы 7: голое `take: 20` у лидов; `count` лидов по `where` сделок; `tabTotals.deals` = 0 (страж + integration); задолженность снова `reduce` по показанным; подпись не рисуется; подпись берёт `shown` соседней вкладки; `count` комментариев не зовётся (страж + integration) |
| `services.chat.threads.unit` · `components.order-thread-inbox` · `pages.partner-messages` · `pages.admin-messages` (хотфикс №11) | 06.09.2026 ✓ пойманы 6: `count` не зовётся; `count` по `where: {}`; голое `take: 50` при `THREADS_CAP` = 40; подпись не рисуется; страница партнёра не передаёт `total`; админ передаёт 0 |
| `navigation.page-title-matches-menu` (хотфикс №9) | 06.09.2026 ✓ пойманы 3: «Заказы» → «Заявки» в `manager/orders/page.tsx`; `sectionLabel('documents')` → `sectionLabel('orders')` в общем компоненте `staff-documents` (упали оба кабинета); страж перестал читать `PageHeader` — счётчик «прочитано > 50» падает |
| `services.no-test-only-modules.guardrail` · `security.idor-comments.integration` (хотфикс №12) | 06.09.2026 ✓ пойманы 7: модуль без импортёров; модуль, который импортирует только тест; у `chat/migrate.ts` отнят единственный импортёр в `scripts/`; у `contacts/backfill.ts` отнят импорт из `prisma/seed.ts`; комментарии карточки без скоупа организации; `count` по другому условию; скоуп по продавцу (`companyId`) вместо организации |
| `e2e organization-team-modal-focus-trap` (хотфикс №13) | 06.09.2026 ✓ пойманы 3: после `el.close()` фокус уведён в фон — возврат на кнопку сломан (2 теста); начальный фокус в модалку не ставится (5 тестов); `show()` вместо `showModal()` — фон не inert (3 теста) |
| `deps.overrides.guardrail` (хотфикс №7, решение `Р-26`, `В-2`) | 05.09.2026 ✓ пойманы 3: в lock вернулась вложенная `next/node_modules/postcss@8.4.31`; из `package.json` убран override `uuid`; корневой `deepmerge-ts` откатился на 7.1.5 |
| `prisma.enum-terminal-last.guardrail` · `boards.status-order.integration` · `boards.cap-and-order.unit` · `services.deals.board` · `components.task-dialog` · страницы deals/funnel/tasks (решение `Р-27`, `В-3`) | 05.09.2026 ✓ пойманы 9: в `deals/board.ts` вернулся `orderBy createdAt`; из `tasks/board.ts` убран `count`; в `funnel/board.ts` `take` без `BOARD_CAP`; в `schema.prisma` `lost` переставлен перед `open`; в диалоге убран `withCurrent` (привязка терялась); `capHint` всегда `null`; `ListCapNotice` снят со страницы сделок руководителя и задач менеджера; организации формы снова по `name` |
| `services.deals.board` + `knip` (хотфикс №8, `С-1`) | 05.09.2026 ✓ пойманы 3: в сервисе `take: 501` вместо константы (тест + страж enum); `BOARD_CAP = 400`; из теста убран импорт константы — `knip` снова показывает 6 экспортов |
| `security.order-deal-visibility` | 10.09.2026 ✓ пойманы 2 (прогон №22, круг 2): панель «Сделка» смонтирована в карточке заказа кабинета заказчика |
| `security.role-access-matrix` | 10.09.2026 ✓ поймана (прогон №22, круг 2): в `managerPolicy` добавлен экспортируемый предикат, не описанный в матрице |
| `security.role-model-inventory` | 10.09.2026 ✓ поймана (прогон №22, круг 2): в `ROLE_VALUES` добавлена седьмая роль |
| `security.settings-matrix` | 10.09.2026 ✓ пойманы 2 (прогон №22, круг 2): админ-только раздел настроек открыт руководителю |
| `server-actions.async-exports` | 10.09.2026 ✓ поймана (прогон №22, круг 2): из файла с `use server` экспортирована синхронная функция |
| `server-actions.session-guard` | 10.09.2026 ✓ поймана (прогон №22, круг 2): добавлено серверное действие без гарда — страж назвал его и объяснил, чем это опасно |
| `services.graceful-degrade` | 11.09.2026 — проба вне объёма (прогон №23, круг 2): снятый `log.warn` внутри `catch` не ловится, страж сторожит наличие `try`/`catch`; записано в журнал как `ℹ` |
| `services.no-inmemory-distinct` | 11.09.2026 ✓ поймана (прогон №23, круг 2): `groupBy` по действиям аудита заменён на `findMany({ distinct })` |
| `services.no-test-only-modules` | 11.09.2026 ✓ поймана (прогон №23, круг 2): заведён модуль сервиса, который зовёт только тест; мёртвый **экспорт** внутри живого модуля — вне объёма, `ℹ` |
| `services.organizationCard.tab-caps` | 11.09.2026 ✓ поймана (прогон №23, круг 2): предел вкладки записан числом вместо общей константы |
| `services.requisites.partner-org` | 11.09.2026 ✓ поймана (прогон №23, круг 2): право правки реквизитов выдано любому партнёрскому пользователю |
| `services.stable-pagination-order` | 11.09.2026 ✓ пойманы 2 (прогон №23, круг 2): у списка партнёров убран хвост `id` в сортировке |
| `services.students` | 11.09.2026 ✓ поймана (прогон №23, круг 2): рядовому участнику организации выдано право заводить сотрудников |
| `services.write-atomicity` | 11.09.2026 ✓ поймана (прогон №23, круг 2): запись истории статуса вынесена из транзакции |
| `shell.command-palette` | 11.09.2026 ✓ пойманы 2 (прогон №24, круг 2): палитра убрана из каркаса руководителя целиком (три красных: сама палитра, разделы, `searchEnabled`); список разделов вписан руками вместо реестра меню. Переименование тега (`<CommandPaletteX`) страж не видит — но это не регресс, а несобирающийся код |
| `ui.empty-states` | 11.09.2026 ✓ поймана (прогон №24, круг 2): самодельное пустое состояние в файле без `EmptyState`; та же вставка в файл с импортом `EmptyState` — вне объёма, `ℹ` |
| `ui.no-internal-jargon` | 11.09.2026 ✓ пойманы 3 (прогон №24, круг 2): «(этап 8)» в тексте, код `У-12` в тексте, «(этап 9)» в `placeholder`; тексты `src/lib` вне объёма — `ℹ` |
| `worker.processor-coverage` | 11.09.2026 ✓ поймана (прогон №24, круг 2): новый процессор `zz-probe.ts` без единого теста; закомментированный импорт считается покрытием — `ℹ` |
| `api.documents-list-scope` | 11.09.2026 ✓ пойманы 3 (прогон №24, круг 3): панель перестала передавать `orderId` в запрос; подпись усечения `ListCapNotice` убрана; роут перестал отдавать `total` |
| `auth.teamMode-required` | 11.09.2026 ✓ пойманы 2 (прогон №24, круг 3): у `teamMode` вернулось значение по умолчанию `= false`; `teamMode` сделан необязательным через `?` |
| `components.no-db-import` | 11.09.2026 ✓ пойманы 2 (прогон №24, круг 3): компонент импортирует prisma по алиасу `@/lib/db/prisma` и по относительному пути `../../lib/db/prisma` |
| `components.upload-size-hint` | 11.09.2026 ✓ пойманы 2 (прогон №24, круг 3): «Максимум 200 МБ.» вписано руками в разметку; форма `order-less-upload-form` перестала брать `DEFAULT_MAX_FILE_SIZE_MB` |
| `config.upload-formats` | 13.09.2026 ✓ поймана (прогон №27, круг 3, долг №26): в роуте загрузки завёлся свой список расширений `['.docx', '.xlsx']` |
| `docs.commands-exist` | 13.09.2026 ✓ поймана (прогон №27, круг 3): `npm run zz-nonexistent-probe` в `docs/ARCHITECTURE.md`; проба в `docs/tz/` не ловится намеренно (реестры пропускаются, см. шапку стража) |
| `docs.live-links` | 13.09.2026 ✓ поймана (прогон №27, круг 3): относительная ссылка на несуществующий файл в действующем документе |
| `docs.onec-contract-fields` | 13.09.2026 ✓ поймана (прогон №27, круг 3): из образца раздела 1 контракта 1С удалено поле `externalId` |
| `documents.document-template` | 13.09.2026 ✓ поймана (прогон №27, круг 3): вёрстка договора перестала печатать номер пункта (`${c.clause}. ${c.text}` → `${c.text}`) |
| `documents.generate-panel-mirror` | 13.09.2026 ✓ поймана (прогон №27, круг 3): у руководителя `<GenerateDocumentsPanel>` заменён на другой компонент |
| `documents.requisites-request-reach` | 13.09.2026 ✓ пойманы 4 (прогон №27, круг 3): кнопка «Запросить у клиента» переименована |
| `documents.send-button-mirror` | 13.09.2026 ✓ поймана (прогон №27, круг 3): у администратора пропал `canSend` в карточке документа |
| `documents.superseded-versions` | 13.09.2026 ✓ пойманы 2 (прогон №27, круг 3): строитель канала в `documentChannelPolicy.ts` без `...ACTIVE_VERSION_WHERE` |
| `e4.no-network` | 13.09.2026 ✓ поймана (прогон №27, круг 3): тест зовёт `fetch` без заглушки `global.fetch` |
| `components.role-editor` — подпись охвата заказов (хотфикс №48) | 13.09.2026 ✓ пойманы 3: подпись «Заказы» возвращена в «Заявки» |
| `oneCSync.reconcile`, `services.commission.corrections.unit`, `jobs.background-scan-batching` — порционность (хотфикс №49) | 13.09.2026 ✓ пойманы 5: без `take` у лидов; порядок `asc`; без второго ключа сортировки возвратов; без `take` возвратов; запрос к истории на каждый лид |
| `prisma.contact-lookup-indexes` (хотфикс №50) | 13.09.2026 ✓ пойманы 2: убран `@@index([primaryContactId])` у `Order`; убран `@@index([contactId])` у `Deal` |

## Журнал прогонов

| Дата | Коммит | Проверка | Итог | PR |
|---|---|---|---|---|
| 13.09.2026 | `ac9fdc12` → `55d6ce95` | С-1…С-10 (прогон №27 — закрытие этапа 1, §9 индекса) | ❌ 2 · ⚠ 0 · ℹ 3; **лимит §9.4 исчерпан с учётом хотфикса №48** (`В-1-7` спеки этапа 1 → `У-96`, [#592](https://github.com/aiprocadm/lk_otsfera/pull/592)). Очередь прогона №25 закрыта хотфиксом №49 [#593](https://github.com/aiprocadm/lk_otsfera/pull/593) (перенос готового коммита `62b4aa70` из закрытого дублем #577: обе ночные задачи берут пачку с пределом, возвраты — от закрытых периодов, индекс `Lead(pushedToOneCAt)`; пять мутаций пойманы); `С-8` нашла колонки этапа 1 без индексов — хотфикс №50 [#594](https://github.com/aiprocadm/lk_otsfera/pull/594). `С-5` — круг 3 продвинулся на 10 стражей (все пойманы), следующий `email.templates`. `С-10` — 19 эталонов устарели на пункты меню «Мессенджеры»/«Контакты», вкладки карточки организации и текст Mango; пересняты, две сверки подряд зелёные, процедура дополнена флагами. Полные прогоны локально: unit 1183 / 12 728, integration 191 / 1423, build. | [#595](https://github.com/aiprocadm/lk_otsfera/pull/595) (прогон) · хотфиксы №48 [#592](https://github.com/aiprocadm/lk_otsfera/pull/592) · №49 [#593](https://github.com/aiprocadm/lk_otsfera/pull/593) · №50 [#594](https://github.com/aiprocadm/lk_otsfera/pull/594) |
| 12.09.2026 | `56638d63` → `b8acaa97` | С-1…С-10 (прогон №26) | ❌ 2 · ⚠ 0 · ℹ 3. Прогон начат с очереди журнала (`С-8`) и закрыл её хотфиксом №45; `С-5` дала второй дефект (хотфикс №46). Хвост находки №8 (`take`) остаётся в очереди с готовым и проверенным CI решением. `С-10` — 274 сверки, одно известное падение по окружению стенда. **Прогон вёлся двумя сессиями параллельно:** вторая взяла отдельный дефект `С-5` (стражи считают закомментированный код живым) — хотфикс №47 | [#575](https://github.com/aiprocadm/lk_otsfera/pull/575), [#576](https://github.com/aiprocadm/lk_otsfera/pull/576), [#578](https://github.com/aiprocadm/lk_otsfera/pull/578) |
| 11.09.2026 | `c88bcb70` → `88b3e7a9` | С-1…С-10 (прогон №25) | ❌ 3 · ⚠ 0 · ℹ 4. Два дефекта закрыты хотфиксами (лимит §9.4 — 2 из 3), третий (`С-8`, фоновые задачи без ограничения) взят в очередь. Круг 3 мутаций продвинулся на 5 стражей + 1 сверх круга. `С-10` — 267 сверок, 5 расхождений оказались окружением стенда (флаг телефонии), после пересборки все зелёные | [#572](https://github.com/aiprocadm/lk_otsfera/pull/572), [#573](https://github.com/aiprocadm/lk_otsfera/pull/573), [#574](https://github.com/aiprocadm/lk_otsfera/pull/574) |
| 05.09.2026 | `594cf347` | С-1…С-10 (первый полный прогон, этап 9 PR-3) | ❌ 5 · ⚠ 1 · ℹ 5; три хотфикса в очереди (№1 матрица флагов, №2 страж подсказки 200 МБ, №3 `npm audit fix`), два вопроса заказчику | #497 (прогон) · хотфиксы №1 #498 · №2 #499 · №3 #500 — все три влиты |
| 05.09.2026 | `f567f19f` → `c9e25e31` | прогон №2: очередь журнала (усечение списков, `С-6`), затем `С-1`…`С-10` | ❌ 2 · ⚠ 1 · ℹ 2 (плюс очередь); три хотфикса влиты — №4 подписи под усечёнными списками, №5 пагинация `/partner/finance`, №6 команды в чеклистах; новый вопрос заказчику `В-3`; unit 1093 / 11 849 · integration 181 / 1378 · Playwright 480 (275 выполнены, 0 падений) | [#501](https://github.com/aiprocadm/lk_otsfera/pull/501) · [#502](https://github.com/aiprocadm/lk_otsfera/pull/502) · [#503](https://github.com/aiprocadm/lk_otsfera/pull/503) · итог [#504](https://github.com/aiprocadm/lk_otsfera/pull/504) |
| 05.09.2026 | `12c3078a` → `b9d94408` | прогон №3: `С-1`…`С-10` после решений `Р-25`…`Р-27` | ❌ 1 · ⚠ 0 · ℹ 4; один хотфикс влит — №8 `BOARD_CAP` в тесте доски сделок (knip); unit 1099 / 11 892 · integration 182 / 1382 · Playwright 480 (275 выполнены, 0 падений, 30 мин); очередь журнала пуста | [#508](https://github.com/aiprocadm/lk_otsfera/pull/508) · итог [#509](https://github.com/aiprocadm/lk_otsfera/pull/509) |
| 06.09.2026 | `e284ca7a` → `42b16151` | прогон №4: `С-1`…`С-10` | ❌ 4 · ⚠ 0 · ℹ 4; три хотфикса влиты — №9 страж заголовков читает `PageHeader`, №10 карточка организации (счётчики вкладок, подпись усечения, задолженность по всем заказам), №11 инбокс «Сообщения» (счётчик и подпись); четвёртый дефект (`partner/orgComments.ts` мёртв) — в очереди на прогон №5; unit 1100 / 11 905 · integration 182 / 1385 · Playwright 480 (274 выполнены, 1 флейк перегнан, 34 мин) | [#510](https://github.com/aiprocadm/lk_otsfera/pull/510) · [#511](https://github.com/aiprocadm/lk_otsfera/pull/511) · [#512](https://github.com/aiprocadm/lk_otsfera/pull/512) · итог [#513](https://github.com/aiprocadm/lk_otsfera/pull/513) |
| 06.09.2026 | `42b16151` → `73fab7ab` | прогон №5: очередь журнала, затем `С-1`…`С-10` | ❌ 2 · ⚠ 0 · ℹ 2; два хотфикса влиты — №12 удалены мёртвые сервисы партнёрской карточки (`orgComments`, `orgHistory`), IDOR-тест переведён на живой путь, новый страж «сервис живёт только тестом»; №13 e2e-страж фокуса модалки перестал падать на гонке гидратации (ронял прогон второй раз подряд); knip: мёртвых файлов 0 (был 1); unit 1101 / 11 906 · integration 182 / 1386 · Playwright 480 (274 выполнены, 41 мин, диффов 0); очередь журнала пуста | [#514](https://github.com/aiprocadm/lk_otsfera/pull/514) · [#515](https://github.com/aiprocadm/lk_otsfera/pull/515) · итог [#516](https://github.com/aiprocadm/lk_otsfera/pull/516) |
| 06.09.2026 | `4b2b98ff` | прогон №6: `С-1`…`С-10` | ✅ **дефектов нет** — ❌ 0 · ⚠ 0 · ℹ 3; хотфиксов не потребовалось. `С-5` замкнула круг: все 66 стражей проверены мутацией (13 в этом прогоне, каждая поймана); `С-2` — вместо сверки по diff (кода не менялось) вживую проверены цепочки `У-145`, `У-164`, `У-165`; `С-10` — впервые прогон без единого падения. unit 1101 / 11 906 · integration 182 / 1386 · `next build` ✅ · Playwright 480 (275 выполнены, 47 мин, диффов 0) | итог [#517](https://github.com/aiprocadm/lk_otsfera/pull/517) |
| 06.09.2026 | `4b2b98ff` → `dc482afb` | прогон №7: `С-1`…`С-10` | ❌ 1 · ⚠ 0 · ℹ 4; один хотфикс влит — №14 (рабочие чеклисты вели по пути с машины автора; новый страж на ссылки всех действующих `docs/**`). `С-5` — второй круг, 11 стражей от начала алфавита, все поймали; `С-2` — вживую закрыты оставшиеся цепочки блока КП (`У-161`…`У-166`); `С-10` — падений 0 второй прогон подряд. unit 1101 / 11 906 · integration 182 / 1386 · `next build` ✅ · Playwright 480 (275 выполнены, 55 мин, диффов 0) | [#518](https://github.com/aiprocadm/lk_otsfera/pull/518) · итог [#519](https://github.com/aiprocadm/lk_otsfera/pull/519) |
| 07.09.2026 | `dc482afb` → `1aa3ef47` | прогон №8: `С-1`…`С-10` | ❌ 1 · ⚠ 0 · ℹ 1; один хотфикс влит — №15 (страж «серверное действие само спрашивает права»: каждое действие — открытый HTTP-эндпоинт, а проверял это только человек; все 189 разобраны, дыр нет). `С-5` — круг 2 продолжен, 9 стражей; `С-2` — вживую проверены `У-171` и `У-172` (обмен с 1С); `С-10` — падений 0 третий прогон подряд. unit 1102 / 11 910 · integration 182 / 1386 · `next build` ✅ · Playwright 480 (275 выполнены, 51 мин, диффов 0) | [#520](https://github.com/aiprocadm/lk_otsfera/pull/520) · итог [#521](https://github.com/aiprocadm/lk_otsfera/pull/521) |
| 07.09.2026 | `1aa3ef47` → `29f4839b` | прогон №9: `С-1`…`С-10` | ❌ 1 · ⚠ 0 · ℹ 0; один хотфикс влит — №16 (страж «API-роут сам спрашивает права»: `/api/**` не закрыт middleware вовсе; все 83 обработчика разобраны, дыр нет). `С-5` — круг 2 продолжен, 11 стражей; `С-2` — вживую закрыты `У-173` и `У-174`; `С-10` — падений 0 четвёртый прогон подряд. unit 1103 / 11 914 · integration 182 / 1386 · `next build` ✅ · Playwright 480 (275 выполнены, 53 мин, диффов 0) | [#522](https://github.com/aiprocadm/lk_otsfera/pull/522) · итог [#523](https://github.com/aiprocadm/lk_otsfera/pull/523) |
| 07.09.2026 | `29f4839b` → `5df8246e` | прогон №10: `С-1`…`С-10` | ❌ 1 · ⚠ 0 · ℹ 0; один хотфикс влит — №17 (страницы «Аудит» и ПДн читали журналы целиком: Prisma считает `distinct` в памяти, `take` в SQL не попадает; оба места на `groupBy`, плюс страж и integration-проверка полноты фильтра). `С-8` — системная сверка 174 выборок с индексами; `С-5` — круг 2 продолжен, 10 стражей; `С-10` — падений 0 пятый прогон подряд. unit 1105 / 11 924 · integration 182 / 1387 · `next build` ✅ · Playwright 480 (275 выполнены, 56 мин, диффов 0) | [#524](https://github.com/aiprocadm/lk_otsfera/pull/524) · итог [#525](https://github.com/aiprocadm/lk_otsfera/pull/525) |
| 07.09.2026 | `5df8246e` → `ac3ad548` | прогон №11: `С-1`…`С-10` | ❌ 2 · ⚠ 0 · ℹ 2; хотфикс №18 (карточка заказа тянула все документы платформы и отбирала их в браузере; фильтр ушёл на сервер, добавлены предел, счётчик и подпись) плюс доводка [#527](https://github.com/aiprocadm/lk_otsfera/pull/527) — мой недосмотр с моком, `main` был красным ~15 минут. `С-8` — разобрана 91 выборка без `take`; `С-5` — круг 2 продолжен, 7 стражей; `С-10` — падений 0 шестой прогон подряд. unit 1106 / 11 933 · integration 182 / 1387 · `next build` ✅ · Playwright 480 (275 выполнены, 53 мин, диффов 0) | [#526](https://github.com/aiprocadm/lk_otsfera/pull/526) · [#527](https://github.com/aiprocadm/lk_otsfera/pull/527) · итог [#528](https://github.com/aiprocadm/lk_otsfera/pull/528) |
| 07.09.2026 | `8fabd7d4` | прогон №12: `С-1`…`С-10` | ✅ **дефектов нет** — ❌ 0 · ⚠ 0 · ℹ 0; хотфиксов не потребовалось. `С-8` прошла три новых направления (N+1 — 63 места, избыточная выборка — 57 `include`, денежная арифметика) и не нашла дефектов; `С-2` — реестр `У-1`…`У-176` пройден вживую полностью; `С-5` — круг 2 продолжен; `С-10` — падений 0 седьмой прогон подряд. unit 1106 / 11 933 · integration 182 / 1387 · `next build` ✅ · Playwright 480 (275 выполнены, диффов 0) | итог [#529](https://github.com/aiprocadm/lk_otsfera/pull/529) |
| 07.09.2026 | `8fabd7d4` → `c5280645` | прогон №13: `С-1`…`С-10` | ❌ 1 · ⚠ 0 · ℹ 0; один хотфикс влит — №19 (смена статуса заказа и снятие участника партнёра стали атомарными: раньше сбой между двумя записями оставлял заказ без строки в истории, а снятого участника — работающим по старому токену). `С-8` — новое направление «целостность записи», 12 мест разобрано; `С-5` — круг 2 продолжен; `С-10` — падений 0 восьмой прогон подряд. unit 1107 / 11 936 · integration 182 / 1387 · `next build` ✅ · Playwright 480 (275 выполнены, 54 мин, диффов 0) | [#530](https://github.com/aiprocadm/lk_otsfera/pull/530) · итог [#531](https://github.com/aiprocadm/lk_otsfera/pull/531) |
| 08.09.2026 | `c5280645` → `896ca2a3` | прогон №14: `С-1`…`С-10` | ❌ 2 · ⚠ 0 · ℹ 0; два хотфикса влиты — №20 и №21 (сутки считаются по Москве, а не по зоне процесса: ночью удостоверение, истёкшее вчера, считалось действующим, а «дела на сегодня» показывали вчерашний день). Очередь закрыта в том же прогоне. `С-8` — три новых направления (даты, дубли при повторе, утечка деталей ошибок), дефект нашёлся в первом; `С-5` — круг 2 продолжен; `С-10` — падений 0 девятый прогон подряд. unit 1108 / 11 945 · integration 182 / 1387 · `next build` ✅ · Playwright 480 (275 выполнены, 56 мин, диффов 0) | [#532](https://github.com/aiprocadm/lk_otsfera/pull/532) · [#533](https://github.com/aiprocadm/lk_otsfera/pull/533) · итог [#534](https://github.com/aiprocadm/lk_otsfera/pull/534) |
| 08.09.2026 | `896ca2a3` → `791b7691` | прогон №15: `С-1`…`С-10` | ❌ 1 · ⚠ 0 · ℹ 0; один хотфикс влит — №22 (свой текст письма ограничен по длине: раньше принималась строка любого размера, и письмо потом отвергал почтовый шлюз). `С-6` — проверены пишущие пути без валидации: три подозрения разобраны, дефект один; `С-4` — отдельно проверены подписанные ссылки, мост слушателя и правила уведомлений (чисто); `С-10` — падений 0 десятый прогон подряд. unit 1108 / 11 952 · integration 182 / 1387 · `next build` ✅ · Playwright 480 (275 выполнены, 60 мин, диффов 0) | [#535](https://github.com/aiprocadm/lk_otsfera/pull/535) · итог [#536](https://github.com/aiprocadm/lk_otsfera/pull/536) |
| 08.09.2026 | `791b7691` → `12654333` | прогон №16: `С-1`…`С-10` + два поручения заказчика | ❌ 2 · ⚠ 0 · ℹ 1; оба поручения выполнены одним PR [#537](https://github.com/aiprocadm/lk_otsfera/pull/537): нарезан релиз **0.11.0** (271 запись, 757 коммитов) и живая приёмка §7 п.3 заменена воспроизводимым сценарным тестом на живой базе. `С-9` — dev-хвост перепроверен поимённо, безопасного фикса нет; `С-5` — круг 2 продолжен; `С-10` — 2 таймаута первого захода, перегон зелёный. unit 1108 / 11 952 · integration 183 / 1388 · `next build` ✅ · Playwright 480 (273 выполнены, диффов 0) | [#537](https://github.com/aiprocadm/lk_otsfera/pull/537) · итог [#538](https://github.com/aiprocadm/lk_otsfera/pull/538) |
| 08.09.2026 | `12654333` → `57690368` | прогон №17: `С-1`…`С-10` + возвращение серверных проверок | ❌ 5 · ⚠ 1 · ℹ 3; **главная находка — CI был выключен в настройках репозитория с 13.08.2026**, месяц PR-ов уезжал в `main` без серверной проверки [#540](https://github.com/aiprocadm/lk_otsfera/pull/540). Вместе с ним закрыты накопленные: дрейф схемы [#540](https://github.com/aiprocadm/lk_otsfera/pull/540), красный `verify` [#541](https://github.com/aiprocadm/lk_otsfera/pull/541), браузерные проверки не гонялись автоматически [#539](https://github.com/aiprocadm/lk_otsfera/pull/539), старый адрес «Команда» не доводил до приглашения участников — хотфикс №23 [#544](https://github.com/aiprocadm/lk_otsfera/pull/544). `С-9`: мажор `vitest` проверен делом и откачен [#543](https://github.com/aiprocadm/lk_otsfera/pull/543). unit 1108 / 11 954 · integration 183 / 1388 · CI зелёный на шести job впервые с 13.08.2026 | [#539](https://github.com/aiprocadm/lk_otsfera/pull/539) · [#540](https://github.com/aiprocadm/lk_otsfera/pull/540) · [#541](https://github.com/aiprocadm/lk_otsfera/pull/541) · [#543](https://github.com/aiprocadm/lk_otsfera/pull/543) · [#544](https://github.com/aiprocadm/lk_otsfera/pull/544) · итог [#545](https://github.com/aiprocadm/lk_otsfera/pull/545) |
| 09.09.2026 | `57690368` → `6bd454a2` | прогон №18: `С-1`…`С-10` | ❌ 2 · ⚠ 0 · ℹ 3; **лимит §9.4 исчерпан — три хотфикса влиты**. `С-9`: аудит рабочих зависимостей впервые с 05.09.2026 стал не нулевым — дыра в разборе адресов входящих писем, хотфикс №24 [#546](https://github.com/aiprocadm/lk_otsfera/pull/546). `С-8`: постраничные списки листаются неустойчиво (проверено на живой базе: из 60 показанных строк уникальных 58) — хотфиксы №25 [#547](https://github.com/aiprocadm/lk_otsfera/pull/547) и №26 [#548](https://github.com/aiprocadm/lk_otsfera/pull/548) закрыли шесть мест из шестнадцати, остальные десять в очереди стража. unit 1108 / 11 954 · integration 183 / 1388 · обход экранов 147 из 147 · CI зелёный | [#546](https://github.com/aiprocadm/lk_otsfera/pull/546) · [#547](https://github.com/aiprocadm/lk_otsfera/pull/547) · [#548](https://github.com/aiprocadm/lk_otsfera/pull/548) · итог [#549](https://github.com/aiprocadm/lk_otsfera/pull/549) |
| 09.09.2026 | `6bd454a2` → `d8fd8f2a` | прогон №19: `С-1`…`С-10` | ❌ 3 · ⚠ 0 · ℹ 2; **лимит §9.4 исчерпан**. Прогон целиком отдан очереди журнала: хотфикс №27 [#550](https://github.com/aiprocadm/lk_otsfera/pull/550) убрал плавающий тест (причина найдена и воспроизведена — 1 падение на 12 прогонов), хотфиксы №28 [#551](https://github.com/aiprocadm/lk_otsfera/pull/551) и №29 [#552](https://github.com/aiprocadm/lk_otsfera/pull/552) закрыли шесть списков из очереди устойчивого листания (осталось 7). Заодно уточнён масштаб находки `С-8`: выборок 33, неустойчивых было 19, а не 16. unit 1109 / 11 957 · integration 183 / 1388 · обход экранов 147 из 147 · CI зелёный | [#550](https://github.com/aiprocadm/lk_otsfera/pull/550) · [#551](https://github.com/aiprocadm/lk_otsfera/pull/551) · [#552](https://github.com/aiprocadm/lk_otsfera/pull/552) · итог [#553](https://github.com/aiprocadm/lk_otsfera/pull/553) |
| 09.09.2026 | `d8fd8f2a` → `76093bdf` | прогон №20: `С-1`…`С-10` | ❌ 4 · ⚠ 0 · ℹ 0; **очередь журнала закрыта полностью**. Хотфиксы №30 [#554](https://github.com/aiprocadm/lk_otsfera/pull/554), №31 [#555](https://github.com/aiprocadm/lk_otsfera/pull/555) и №32 [#556](https://github.com/aiprocadm/lk_otsfera/pull/556) исправили семь последних неустойчивых списков — направление `С-8` закрыто целиком (19 выборок из 33). Четвёртым, сверх лимита §9.4, вышел хотфикс №33 [#557](https://github.com/aiprocadm/lk_otsfera/pull/557): из-за протухшего по календарю теста `main` стал красным, а `❌` чинится сразу. unit 1109 / 11 957 · integration 183 / 1388 · обход экранов 147 из 147 · CI зелёный | [#554](https://github.com/aiprocadm/lk_otsfera/pull/554) · [#555](https://github.com/aiprocadm/lk_otsfera/pull/555) · [#556](https://github.com/aiprocadm/lk_otsfera/pull/556) · [#557](https://github.com/aiprocadm/lk_otsfera/pull/557) · итог [#558](https://github.com/aiprocadm/lk_otsfera/pull/558) |
| 09.09.2026 | `76093bdf` → `2d8d02a7` | прогон №21: `С-1`…`С-10` | ❌ 1 · ⚠ 0 · ℹ 2; очередь была пуста, прогон целиком ушёл на поиск нового. Хотфикс №34 [#559](https://github.com/aiprocadm/lk_otsfera/pull/559): НДС при разборе выписки считался в рублях и терял копейку у каждого 35-го платежа (перебор 200 000 сумм) — переведён в целые копейки. Проверены и признаны чистыми пять направлений: план запросов после хвостов `id`, согласованность счётчиков, идемпотентность вебхуков, проверка загружаемых файлов по сигнатуре, фильтр активности у получателей писем. unit 1109 / 11 957 · integration 183 / 1388 · обход экранов 147 из 147 · CI зелёный | [#559](https://github.com/aiprocadm/lk_otsfera/pull/559) · итог [#560](https://github.com/aiprocadm/lk_otsfera/pull/560) |
| 10.09.2026 | `2d8d02a7` → `037cf277` | прогон №22: очередь журнала, затем `С-1`…`С-10` | ❌ 3 · ⚠ 0 · ℹ 3; **лимит §9.4 исчерпан**. Одна дыра в трёх местах: запись упирается в уникальное поле базы, а человек видит падение вместо текста. №35 [#561](https://github.com/aiprocadm/lk_otsfera/pull/561) — гонка при создании партнёра (находка из очереди прогона №21, там её воспроизвести не удалось); №36 [#562](https://github.com/aiprocadm/lk_otsfera/pull/562) — занятая почта сотрудника, воспроизведена на живой базе, гонка не нужна; №37 [#563](https://github.com/aiprocadm/lk_otsfera/pull/563) — импорт сотрудников падал целиком вместо построчной ошибки. В очередь журнала: та же гонка ещё в четырёх точках приглашения людей. `С-5` — круг 2 продолжен, 6 стражей. `С-10` — впервые прогнан на прод-стенде: 6,7 мин вместо часа. unit 1109 / 11 967 · integration 183 / 1388 · `next build` ✅ · CI зелёный трижды | [#561](https://github.com/aiprocadm/lk_otsfera/pull/561) · [#562](https://github.com/aiprocadm/lk_otsfera/pull/562) · [#563](https://github.com/aiprocadm/lk_otsfera/pull/563) · итог [#564](https://github.com/aiprocadm/lk_otsfera/pull/564) |
| 11.09.2026 | `5da88311` → `3e4b3556` | прогон №23: очередь журнала, затем `С-1`…`С-10` | ❌ 3 · ⚠ 0 · ℹ 3; **лимит §9.4 исчерпан**. Класс «уникальное поле в базе против отсутствия текста на экране» закрыт целиком: хотфикс №38 [#565](https://github.com/aiprocadm/lk_otsfera/pull/565) (админ заводит пользователя, приглашение в команду партнёра), №39 [#566](https://github.com/aiprocadm/lk_otsfera/pull/566) (приглашение в организацию и назначение менеджера — там «найти или завести», поэтому чинится повтором транзакции, а не текстом), №40 [#567](https://github.com/aiprocadm/lk_otsfera/pull/567) (повторная выдача удостоверения: проверки не было вовсе, роут отвечал 500 вместо 409). Разбор `P2002` вынесен в общую `lib/db/uniqueViolation.ts`. `С-5` — круг 2 продолжен, 8 стражей, две пробы вне их объёма записаны как `ℹ`. Очередь журнала пуста. unit 1109 / 11 988 · integration 183 / 1388 · `next build` ✅ · CI зелёный трижды | [#565](https://github.com/aiprocadm/lk_otsfera/pull/565) · [#566](https://github.com/aiprocadm/lk_otsfera/pull/566) · [#567](https://github.com/aiprocadm/lk_otsfera/pull/567) · итог [#568](https://github.com/aiprocadm/lk_otsfera/pull/568) |
| 11.09.2026 | `3e4b3556` → `bba08529` | прогон №24: `С-1`…`С-10` | ❌ 2 · ⚠ 0 · ℹ 3; очередь была пуста, прогон целиком ушёл на поиск нового. Хотфикс №41 [#569](https://github.com/aiprocadm/lk_otsfera/pull/569): мусор в запросе к API (`?skip=abc`, `?from=вчера`, дата удостоверения строкой) ронял два роута в 500 вместо 400 — разбор входа переведён на общий `parseQuery` и помощник `dateString`. Хотфикс №42 [#570](https://github.com/aiprocadm/lk_otsfera/pull/570): контракт 1С три месяца не описывал шесть полей, которые код уже принимает (ключ организации по ИНН, реквизиты платежа) — документ дополнен, новый страж сверяет поля контракта со схемами. `С-5` — **круг 2 замкнут и начат круг 3**: 8 стражей, 19 проб, три слепые зоны записаны как `ℹ`. `С-10` на прод-стенде: 274 сверки, то же одно объяснимое падение. unit 1109 / 11 988 · integration 183 / 1388 · `next build` ✅ · CI зелёный на обоих PR | [#569](https://github.com/aiprocadm/lk_otsfera/pull/569) · [#570](https://github.com/aiprocadm/lk_otsfera/pull/570) · итог [#571](https://github.com/aiprocadm/lk_otsfera/pull/571) |
