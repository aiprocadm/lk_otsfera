# Этап 12 ТЗ — Модуль 5: чек-лист готовности заказа и передача результата

Дата: 2026-07-27 · Статус: **✅ подтверждена заказчиком 27.07.2026** ·
Решения: (1) нажимать «Передать результат» может **любой менеджер в скоупе
заказа**; (2) для `document_development` готовность требует **явной отметки
согласования менеджером** (не выводится автоматически из загруженных файлов);
(3) дата передачи ставится **один раз**, кнопки «передать повторно» нет
(в диалоге выбора кликнулось «повторно», но текстом заказчик указал «один раз» —
взят текстовый ответ как окончательный); (4) разбивка на 2 PR · ТЗ:
[docs/tz/2026-07-23-tz-lk-otsfera-v1.md](../../tz/2026-07-23-tz-lk-otsfera-v1.md)
§Модуль 5 (ФТ-5.1–5.4), §4 (сквозной поток).

## 1. Почему появился этап вне §9 ТЗ

Модуль 5 **выпал из разбивки**: в таблице этапов §9 перечислены модули 14, 2, 6,
10, 1, 4, 8+7+3, 9, 11, зачистка, 15 — пятого нет ни в одном этапе. При этом §4
ТЗ описывает сквозной поток как «… → ЗАКАЗ → **чек-лист готовности** →
**"Передано"**: удостоверения, документы, уведомление».

Обнаружено при реализации ФТ-15.3 («Мой день»): блок «Готово к передаче»
определён как «чек-лист закрыт, **передача не нажата**» — а нажимать нечего.
Так же зависит ФТ-15.7 (тип `order_result_delivered`).

**Решение заказчика 27.07.2026: делать Модуль 5 сейчас.** Этап 11
приостановлен после PR-1 (влит, #248) и продолжится после этого этапа — тогда
«Мой день» сразу получит блок «Готово к передаче» и переделывать не придётся.

## 2. Что уже есть (разведка)

| Кусок | Статус |
|---|---|
| `evaluateOrderCompletion` (`lib/orders/completion.ts`) | ✅ чистая функция: `documents_uploaded`, `accounting_signed`, `certificates_issued` (для обучения). ТЗ сам называет её основой, которую Модуль 5 расширяет |
| `manager/orderLifecycle.ts` | ✅ единственный потребитель проверки; переходы статуса живут там |
| `humanStage` (клиентский степпер) | ✅ есть, последняя точка — «Завершён» |
| `notifyOrgUsers` / `notifyPartnerUsers` | ✅ мультиканальная доставка готова |
| `OrderItem.trainingStatus`, `Certificate.documentId` | ✅ данные для попозиционной готовности есть |
| `Order.resultDeliveredAt` | ❌ нет |
| Кнопка «Передать результат», тип `order_result_delivered` | ❌ нет |
| Массовая загрузка сканов удостоверений | ❌ нет |

## 3. Дизайн

### ФТ-5.1 — блок «Готовность к передаче» на деталке заказа (staff)

Расширяем `evaluateOrderCompletion` до **попозиционной** картины:

- `serviceType='training'` — по каждой `OrderItem`: обучение завершено ·
  удостоверение создано · скан удостоверения загружен (`Certificate.documentId`).
- `serviceType='document_development'` — загружены итоговые документы
  (`Document.direction='outgoing'`, `scanStatus='clean'`).
- Существующие условия (`accounting_signed`, `documents_uploaded`) остаются:
  блок **расширяет**, а не заменяет «список несоблюдённых условий завершения».

Функция остаётся чистой и юнит-тестируемой (без БД) — как сейчас.

### ФТ-5.2 — кнопка «Передать результат клиенту»

- Активна **только** при полной готовности (иначе — disabled + список
  недостающего, как у генерации документов этапа 8).
- Ставит `Order.resultDeliveredAt` (аддитивная миграция, nullable).
- Идемпотентность: повторное нажатие не перезаписывает дату и не шлёт второе
  уведомление (проверка `resultDeliveredAt === null` внутри транзакции).
- Уведомление `order_result_delivered` организации и — если заказ партнёрский —
  партнёру; ссылка на реестр удостоверений (обучение) или документы.
- Аудит: `order_result_delivered`, entity `order`.

### ФТ-5.3 — массовая загрузка сканов удостоверений

- На заказе: мультизагрузка файлов с сопоставлением «файл → позиция»
  (выбор слушателя вручную).
- Автоподстановка по совпадению ФИО в имени файла — **подсказка**, требующая
  ручного подтверждения (ТЗ: «с обязательным ручным подтверждением»).
- Каждый файл — обычный `Document` со сканом ClamAV и записью в аудит, как при
  одиночной загрузке (переиспользуем `persistUploadedDocument`).

### ФТ-5.4 — клиентский степпер

`humanStage` получает финальную точку **«Результат передан»** с датой
`resultDeliveredAt`. Для клиента это ответ на вопрос «моё готово?» без звонка
менеджеру.

## 4. Разбивка на PR

- **PR-1 «Готовность и передача»** (ФТ-5.1, 5.2, 5.4): миграция
  `resultDeliveredAt`, расширенный чек-лист, кнопка + уведомление + аудит,
  точка степпера у клиента.
- **PR-2 «Массовая загрузка сканов»** (ФТ-5.3).

После этапа — возврат к этапу 11: PR-2 «Мой день» (уже с блоком «Готово к
передаче») + хлебные крошки, затем PR-3.

## 5. Тестовая стратегия

- `evaluateOrderCompletion`: попозиционные ветки, оба типа услуги, пустые позиции.
- Передача: недоступна при неполной готовности; идемпотентность (второе нажатие
  — no-op); уведомление уходит организации и партнёру; аудит записан.
- Клиентский степпер: точка появляется только после передачи.
- Массовая загрузка: сопоставление, автоподсказка требует подтверждения,
  заражённый файл не привязывается.
- Integration на живом Postgres: полный путь «все условия выполнены → передача →
  клиент видит "Результат передан"».
- Порог покрытия 100% на изменённые слои.

## 6. Решения заказчика (получены до кода)

1. **Кто нажимает «Передать результат»** — ✅ любой менеджер в скоупе заказа.
   Отдельного гейта на руководителя нет; скоуп — обычный `canSeeOrder` (C8).
2. **Заказы `document_development`** — ✅ нужна **явная отметка согласования**
   менеджером. Заводим `Order.deliverablesApprovedAt` (+ кто поставил): «работа
   согласована» ставится руками, автоматический вывод из загруженных файлов
   недостаточен. В чек-листе это отдельное условие `deliverables_approved`.
3. **Повторная передача** — ✅ дата ставится **один раз**. Повторное нажатие —
   no-op (второго уведомления нет). Довоз результата обсуждается с клиентом
   комментариями к заказу.
4. **Разбивка на 2 PR** — ✅ подтверждена.
