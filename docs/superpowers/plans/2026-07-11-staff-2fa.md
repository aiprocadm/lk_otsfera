# Staff 2FA (email-код) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Обязательный второй фактор (6-значный email-код) при логине staff-ролей (admin/manager/leader) за opt-in флагом `FEATURE_STAFF_2FA`, с backup-кодами восстановления.

**Architecture:** Login-роут при включённом флаге не выдаёт сессию staff-пользователю, а создаёт одноразовую challenge (sha256-хеш кода, TTL 10 мин, ≤5 попыток), шлёт код письмом и ставит короткий pre-auth JWT (`purpose:'2fa'`, без `role` — существующая zod-схема сессии его отвергает автоматически). `/api/auth/2fa/verify` сверяет код (или backup-код) и выдаёт полноценный session-JWT через общий хелпер сборки клеймов, извлечённый из login-роута. Спека: [2026-07-11-staff-2fa-design.md](../specs/2026-07-11-staff-2fa-design.md).

**Tech Stack:** Next.js 15 route handlers, Prisma (2 новые модели), jose (уже в проекте), Resend email, Redis-лимитер `@/lib/rateLimit`, vitest.

**Ветка:** `claude/staff-2fa` (stacked поверх `claude/release-hardening-r0`). Каждая задача — отдельный коммит; `--no-verify` допустим только с ручным прогоном затронутых тестов (см. §6 CLAUDE.md про таймауты test:changed).

---

## File Structure

| Файл | Ответственность |
|---|---|
| `prisma/schema.prisma` (modify) | +`TwoFactorChallenge`, +`TwoFactorBackupCode`, +2 relation-поля в `User` |
| `src/lib/featureFlags.ts` (modify) | +`staff_2fa` (opt-in) |
| `src/lib/errors/messages.ts` (modify) | +5 RU-кодов |
| `src/lib/auth/jwt.ts` (modify) | +`signTwoFactorPendingToken` / `verifyTwoFactorPendingToken` |
| `src/lib/auth/buildSessionClaims.ts` (create) | сборка SessionPayload из user-строки (вынос из login) |
| `src/lib/services/auth/twoFactor.ts` (create) | challenge create/verify + backup-коды (чистый сервис §3) |
| `src/lib/email/templates/two-factor-code.tsx` (create) | письмо с кодом |
| `src/app/api/auth/login/route.ts` (modify) | 2FA-ветка для staff |
| `src/app/api/auth/2fa/verify/route.ts` (create) | обмен кода на сессию |
| `src/app/api/auth/2fa/resend/route.ts` (create) | переотправка кода |
| `src/components/auth/login-form.tsx` (modify) | шаг 2 — ввод кода |
| `src/server-actions/staff/backupCodes.ts` (create) | regenerate-action (self-service) |
| `src/components/settings/staff-backup-codes-section.tsx` (create) | секция «Коды восстановления» |
| `src/app/{admin,manager,leader}/settings/page.tsx` (modify) | подключение секции |
| `src/lib/services/admin/users/mutations.ts` (modify) | перевыпуск кодов админом |
| `docs/feature-flags-matrix.md`, `.env.example`, `.env.production.example` (modify) | документация флага |

Тесты: `src/__tests__/auth.twoFactor.unit.test.ts`, `services.auth.twoFactor.integration.test.ts`, `auth.jwt.2fa-pending.test.ts`, `api.auth.login.2fa.test.ts`, `api.auth.2fa.verify.test.ts`, `api.auth.2fa.resend.test.ts`, `components.login-form.2fa.test.tsx`, `server-actions.staff.backupCodes.test.ts`, `components.staff-backup-codes-section.test.tsx` + правки существующих (login, settings-страницы, featureFlags).

---

### Task 1: Prisma-модели

**Files:** Modify: `prisma/schema.prisma`

- [ ] **Step 1: Добавить модели** (после `model PasswordResetToken`, ~строка 916):

```prisma
model TwoFactorChallenge {
  id        String   @id @default(cuid())
  createdAt DateTime @default(now())
  userId    String   @unique // одна активная challenge на пользователя (upsert)
  user      User     @relation(fields: [userId], references: [id])
  codeHash  String // sha256(code) — плейнтекст не хранится (§12)
  expiresAt DateTime
  attempts  Int      @default(0)

  @@index([expiresAt])
}

model TwoFactorBackupCode {
  id        String    @id @default(cuid())
  createdAt DateTime  @default(now())
  userId    String
  user      User      @relation(fields: [userId], references: [id])
  codeHash  String    @unique
  usedAt    DateTime?

  @@index([userId])
}
```

В `model User` — два relation-поля рядом с `passwordResetTokens`:

```prisma
  twoFactorChallenge  TwoFactorChallenge?
  twoFactorBackupCodes TwoFactorBackupCode[]
```

- [ ] **Step 2: Миграция + генерация**

Run: `npx prisma migrate dev --name staff_2fa && npm run prisma:generate`
Expected: миграция создана, клиент сгенерирован.

- [ ] **Step 3: Commit** — `git add prisma && git commit -m "feat(2fa): TwoFactorChallenge + TwoFactorBackupCode models"`

---

### Task 2: Флаг `staff_2fa`

**Files:** Modify: `src/lib/featureFlags.ts`, `docs/feature-flags-matrix.md`; Test: существующий `src/__tests__/featureFlags.test.ts` (найти точное имя `ls src/__tests__ | grep -i flag`).

- [ ] **Step 1: Failing test** — в тест флагов добавить:

```ts
it('staff_2fa is opt-in: disabled by default, enabled by FEATURE_STAFF_2FA=1', () => {
  delete process.env.FEATURE_STAFF_2FA;
  expect(isFeatureEnabled('staff_2fa')).toBe(false);
  process.env.FEATURE_STAFF_2FA = '1';
  expect(isFeatureEnabled('staff_2fa')).toBe(true);
  delete process.env.FEATURE_STAFF_2FA;
});
```

- [ ] **Step 2: Убедиться, что падает** (тип не знает флага) — `npx vitest run --mode=unit src/__tests__/<файл-флагов>`
- [ ] **Step 3: Реализация** — в `FEATURE_FLAGS` перед закрывающей скобкой:

```ts
  // 2FA сотрудников (email-код). Поведенческий флаг (не route): точки чтения —
  // login/verify/resend-роуты + секция настроек. Спека 2026-07-11-staff-2fa.
  'staff_2fa',
```

и в `OPT_IN_FLAGS` добавить `'staff_2fa'`. В `docs/feature-flags-matrix.md` — строка по образцу соседей (opt-in, поведенческий, точки чтения).

- [ ] **Step 4: Тест зелёный** → **Step 5: Commit** `feat(2fa): staff_2fa opt-in feature flag`

---

### Task 3: RU-коды ошибок

**Files:** Modify: `src/lib/errors/messages.ts`; Test: `src/__tests__/lib.errorMessages.test.ts`

- [ ] **Step 1: Failing test** — добавить в существующий файл:

```ts
it('maps the staff-2FA codes to Russian', () => {
  for (const code of ['email_send_failed', 'code_expired', 'invalid_code', 'too_many_attempts', 'session_expired']) {
    expect(errorMessageRu(code)).not.toBe('Произошла ошибка.');
  }
});
```

- [ ] **Step 2: Реализация** — в конец `RU` перед `};`:

```ts
  // Staff 2FA (спека 2026-07-11)
  email_send_failed: 'Не удалось отправить код на почту. Попробуйте ещё раз.',
  code_expired: 'Код истёк. Войдите заново.',
  invalid_code: 'Неверный код.',
  too_many_attempts: 'Слишком много попыток. Войдите заново.',
  session_expired: 'Сессия входа истекла. Войдите заново.',
```

- [ ] **Step 3: Тест зелёный** → **Step 4: Commit** `feat(2fa): RU strings for the 2FA error codes`

---

### Task 4: Pre-auth JWT (`purpose:'2fa'`)

**Files:** Modify: `src/lib/auth/jwt.ts`; Test: Create `src/__tests__/auth.jwt.2fa-pending.test.ts`

- [ ] **Step 1: Failing test**

```ts
import { describe, it, expect, beforeAll } from 'vitest';
import {
  signTwoFactorPendingToken,
  verifyTwoFactorPendingToken,
  verifyToken
} from '@/lib/auth/jwt';

beforeAll(() => {
  process.env.JWT_SECRET = 'unit-test-secret-that-is-32-chars!!';
});

describe('two-factor pending token', () => {
  it('round-trips sub through sign/verify', async () => {
    const token = await signTwoFactorPendingToken('user-1');
    const claims = await verifyTwoFactorPendingToken(token);
    expect(claims.sub).toBe('user-1');
  });

  it('GUARD: pre-auth token is NOT a valid session token', async () => {
    // Подложенный в cookie `session` pre-auth токен обязан отвергаться:
    // в нём нет role, а sessionPayloadSchema требует role.
    const token = await signTwoFactorPendingToken('user-1');
    await expect(verifyToken(token)).rejects.toThrow();
  });

  it('rejects a session token passed as a pending token (purpose mismatch)', async () => {
    const { signToken } = await import('@/lib/auth/jwt');
    const session = await signToken({ sub: 'user-1', role: 'manager' });
    await expect(verifyTwoFactorPendingToken(session)).rejects.toThrow();
  });
});
```

- [ ] **Step 2: Run — FAIL** (функций нет).
- [ ] **Step 3: Реализация** — в `jwt.ts` после `verifyToken`:

```ts
const TWO_FACTOR_PENDING_TTL = '10m';

const twoFactorPendingSchema = z.object({
  sub: z.string().min(1),
  purpose: z.literal('2fa')
});

/**
 * Pre-auth токен шага 2FA: выдан после верного пароля, до ввода кода.
 * Намеренно НЕ содержит role — sessionPayloadSchema (verifyToken) такой
 * payload отвергает, поэтому pre-auth токен не открывает ни один маршрут,
 * даже если подложить его в cookie `session`.
 */
export async function signTwoFactorPendingToken(userId: string) {
  return new SignJWT({ sub: userId, purpose: '2fa' })
    .setProtectedHeader({ alg: 'HS256' })
    .setExpirationTime(TWO_FACTOR_PENDING_TTL)
    .sign(getJwtSecret());
}

export async function verifyTwoFactorPendingToken(token: string): Promise<{ sub: string }> {
  const { payload } = await jwtVerify(token, getJwtSecret());
  return twoFactorPendingSchema.parse(payload);
}
```

- [ ] **Step 4: Тест зелёный** → **Step 5: Commit** `feat(2fa): pre-auth pending JWT with purpose claim + session-guard test`

---

### Task 5: Сервис twoFactor (unit, TDD)

**Files:** Create: `src/lib/services/auth/twoFactor.ts`; Test: Create `src/__tests__/auth.twoFactor.unit.test.ts` (mock-prisma, эталон — `auth.passwordReset.test.ts`)

- [ ] **Step 1: Failing tests** (полный файл):

```ts
import { beforeEach, describe, expect, it, vi } from 'vitest';
import { createHash } from 'crypto';

const sha256 = (v: string) => createHash('sha256').update(v).digest('hex');

const { chUpsert, chFindUnique, chUpdate, chDelete, bcFindFirst, bcUpdate, bcDeleteMany, bcCreateMany, txFn } =
  vi.hoisted(() => {
    const m = {
      chUpsert: vi.fn(), chFindUnique: vi.fn(), chUpdate: vi.fn(), chDelete: vi.fn(),
      bcFindFirst: vi.fn(), bcUpdate: vi.fn(), bcDeleteMany: vi.fn(), bcCreateMany: vi.fn(),
      txFn: vi.fn()
    };
    m.txFn.mockImplementation((cb: (tx: unknown) => unknown) =>
      cb({
        twoFactorChallenge: { upsert: m.chUpsert, findUnique: m.chFindUnique, update: m.chUpdate, delete: m.chDelete },
        twoFactorBackupCode: { findFirst: m.bcFindFirst, update: m.bcUpdate, deleteMany: m.bcDeleteMany, createMany: m.bcCreateMany }
      })
    );
    return m;
  });

const mockPrisma = {
  twoFactorChallenge: { upsert: chUpsert, findUnique: chFindUnique, update: chUpdate, delete: chDelete },
  twoFactorBackupCode: { findFirst: bcFindFirst, update: bcUpdate, deleteMany: bcDeleteMany, createMany: bcCreateMany },
  $transaction: txFn
} as unknown as import('@prisma/client').PrismaClient;

import { createTwoFactorChallenge, verifyTwoFactorCode, generateBackupCodes } from '@/lib/services/auth/twoFactor';

beforeEach(() => { vi.clearAllMocks(); });

describe('createTwoFactorChallenge', () => {
  it('upserts a sha256-hashed 6-digit code with a ~10min expiry, attempts reset', async () => {
    chUpsert.mockResolvedValue(undefined);
    const before = Date.now();
    const { code } = await createTwoFactorChallenge(mockPrisma, 'u1');
    expect(code).toMatch(/^\d{6}$/);
    const arg = chUpsert.mock.calls[0][0];
    expect(arg.where).toEqual({ userId: 'u1' });
    expect(arg.create.codeHash).toBe(sha256(code));
    expect(arg.update.codeHash).toBe(sha256(code));
    expect(arg.update.attempts).toBe(0);
    const ttl = arg.create.expiresAt.getTime() - before;
    expect(ttl).toBeGreaterThanOrEqual(10 * 60_000 - 100);
    expect(ttl).toBeLessThanOrEqual(10 * 60_000 + 1000);
  });
});

describe('verifyTwoFactorCode', () => {
  const FUTURE = new Date(Date.now() + 60_000);
  it('ok via challenge code; challenge deleted', async () => {
    chFindUnique.mockResolvedValue({ userId: 'u1', codeHash: sha256('123456'), expiresAt: FUTURE, attempts: 0 });
    const r = await verifyTwoFactorCode(mockPrisma, 'u1', '123456');
    expect(r).toEqual({ ok: true, method: 'challenge' });
    expect(chDelete).toHaveBeenCalledWith({ where: { userId: 'u1' } });
  });
  it('code_expired when no challenge', async () => {
    chFindUnique.mockResolvedValue(null);
    expect(await verifyTwoFactorCode(mockPrisma, 'u1', '123456')).toEqual({ ok: false, error: 'code_expired' });
  });
  it('code_expired when expiresAt in the past (challenge deleted)', async () => {
    chFindUnique.mockResolvedValue({ userId: 'u1', codeHash: sha256('123456'), expiresAt: new Date(Date.now() - 1000), attempts: 0 });
    expect(await verifyTwoFactorCode(mockPrisma, 'u1', '123456')).toEqual({ ok: false, error: 'code_expired' });
    expect(chDelete).toHaveBeenCalled();
  });
  it('too_many_attempts at 5 (challenge deleted)', async () => {
    chFindUnique.mockResolvedValue({ userId: 'u1', codeHash: sha256('123456'), expiresAt: FUTURE, attempts: 5 });
    expect(await verifyTwoFactorCode(mockPrisma, 'u1', '999999')).toEqual({ ok: false, error: 'too_many_attempts' });
    expect(chDelete).toHaveBeenCalled();
  });
  it('wrong code falls back to backup codes; hit consumes it', async () => {
    chFindUnique.mockResolvedValue({ userId: 'u1', codeHash: sha256('123456'), expiresAt: FUTURE, attempts: 0 });
    bcFindFirst.mockResolvedValue({ id: 'bc1' });
    const r = await verifyTwoFactorCode(mockPrisma, 'u1', 'BACKUP-CODE');
    expect(r).toEqual({ ok: true, method: 'backup' });
    expect(bcFindFirst).toHaveBeenCalledWith({
      where: { userId: 'u1', codeHash: sha256('BACKUP-CODE'), usedAt: null },
      select: { id: true }
    });
    expect(bcUpdate).toHaveBeenCalledWith({ where: { id: 'bc1' }, data: { usedAt: expect.any(Date) } });
    expect(chDelete).toHaveBeenCalledWith({ where: { userId: 'u1' } });
  });
  it('both miss → invalid_code, attempts incremented', async () => {
    chFindUnique.mockResolvedValue({ userId: 'u1', codeHash: sha256('123456'), expiresAt: FUTURE, attempts: 1 });
    bcFindFirst.mockResolvedValue(null);
    expect(await verifyTwoFactorCode(mockPrisma, 'u1', '000000')).toEqual({ ok: false, error: 'invalid_code' });
    expect(chUpdate).toHaveBeenCalledWith({ where: { userId: 'u1' }, data: { attempts: { increment: 1 } } });
  });
});

describe('generateBackupCodes', () => {
  it('transactionally replaces old codes with 10 hashed new ones', async () => {
    const { codes } = await generateBackupCodes(mockPrisma, 'u1');
    expect(codes).toHaveLength(10);
    for (const c of codes) expect(c).toMatch(/^[A-Z2-7]{10}$/);
    expect(new Set(codes).size).toBe(10);
    expect(bcDeleteMany).toHaveBeenCalledWith({ where: { userId: 'u1' } });
    const rows = bcCreateMany.mock.calls[0][0].data;
    expect(rows.map((r: { codeHash: string }) => r.codeHash)).toEqual(codes.map(sha256));
  });
});
```

- [ ] **Step 2: Run — FAIL.**
- [ ] **Step 3: Реализация** `src/lib/services/auth/twoFactor.ts`:

```ts
import { createHash, randomBytes, randomInt } from 'crypto';
import type { PrismaClient } from '@prisma/client';

const CODE_TTL_MS = 10 * 60 * 1000;
const MAX_ATTEMPTS = 5;
const BACKUP_COUNT = 10;
const BACKUP_LEN = 10;
// base32 без похожих глифов (0/O, 1/I/L) — коды диктуются по телефону
const BACKUP_ALPHABET = 'ABCDEFGHJKMNPQRSTUVWXYZ23456789'.replace(/[IL01O]/g, '');

// В БД — только sha256 (тот же принцип, что reset-токены): дамп не даёт кодов.
function hashCode(code: string): string {
  return createHash('sha256').update(code).digest('hex');
}

export type VerifyTwoFactorResult =
  | { ok: true; method: 'challenge' | 'backup' }
  | { ok: false; error: 'code_expired' | 'too_many_attempts' | 'invalid_code' };

export async function createTwoFactorChallenge(
  prisma: PrismaClient,
  userId: string
): Promise<{ code: string }> {
  const code = String(randomInt(0, 1_000_000)).padStart(6, '0');
  const expiresAt = new Date(Date.now() + CODE_TTL_MS);
  await prisma.twoFactorChallenge.upsert({
    where: { userId },
    create: { userId, codeHash: hashCode(code), expiresAt },
    update: { codeHash: hashCode(code), expiresAt, attempts: 0 }
  });
  return { code };
}

export async function verifyTwoFactorCode(
  prisma: PrismaClient,
  userId: string,
  code: string
): Promise<VerifyTwoFactorResult> {
  const challenge = await prisma.twoFactorChallenge.findUnique({ where: { userId } });
  if (!challenge) return { ok: false, error: 'code_expired' };
  if (challenge.expiresAt <= new Date()) {
    await prisma.twoFactorChallenge.delete({ where: { userId } });
    return { ok: false, error: 'code_expired' };
  }
  if (challenge.attempts >= MAX_ATTEMPTS) {
    await prisma.twoFactorChallenge.delete({ where: { userId } });
    return { ok: false, error: 'too_many_attempts' };
  }
  if (hashCode(code) === challenge.codeHash) {
    await prisma.twoFactorChallenge.delete({ where: { userId } });
    return { ok: true, method: 'challenge' };
  }
  // Fallback: backup-код (одноразовый)
  const backup = await prisma.twoFactorBackupCode.findFirst({
    where: { userId, codeHash: hashCode(code), usedAt: null },
    select: { id: true }
  });
  if (backup) {
    await prisma.twoFactorBackupCode.update({ where: { id: backup.id }, data: { usedAt: new Date() } });
    await prisma.twoFactorChallenge.delete({ where: { userId } });
    return { ok: true, method: 'backup' };
  }
  await prisma.twoFactorChallenge.update({
    where: { userId },
    data: { attempts: { increment: 1 } }
  });
  return { ok: false, error: 'invalid_code' };
}

function randomBackupCode(): string {
  const bytes = randomBytes(BACKUP_LEN);
  let out = '';
  for (let i = 0; i < BACKUP_LEN; i++) out += BACKUP_ALPHABET[bytes[i]! % BACKUP_ALPHABET.length];
  return out;
}

export async function generateBackupCodes(
  prisma: PrismaClient,
  userId: string
): Promise<{ codes: string[] }> {
  const codes = Array.from({ length: BACKUP_COUNT }, () => randomBackupCode());
  await prisma.$transaction(async (tx) => {
    await tx.twoFactorBackupCode.deleteMany({ where: { userId } });
    await tx.twoFactorBackupCode.createMany({
      data: codes.map((c) => ({ userId, codeHash: hashCode(c) }))
    });
  });
  return { codes };
}
```

Примечание: сравнение хешей здесь `===`, а не `secretEquals` — сравниваются
два sha256-хеша (не сам секрет), тайминг не раскрывает префикс кода. Если
ревью потребует единообразия — заменить на `secretEquals(hashCode(code), challenge.codeHash)`.

- [ ] **Step 4: Тесты зелёные** → **Step 5: Commit** `feat(2fa): twoFactor service — challenge lifecycle + backup codes`

---

### Task 6: Integration-тест сервиса (живой PG)

**Files:** Create: `src/__tests__/services.auth.twoFactor.integration.test.ts`

- [ ] **Step 1: Написать тест** (снимает то, что unit-моки не видят: `@unique`-гонки, транзакцию, реальный upsert):

```ts
import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import { PrismaClient } from '@prisma/client';
import { createTwoFactorChallenge, verifyTwoFactorCode, generateBackupCodes } from '@/lib/services/auth/twoFactor';

let prisma: PrismaClient;
let userId: string;

beforeAll(async () => {
  prisma = new PrismaClient();
  const u = await prisma.user.create({
    data: { email: `2fa-${Date.now()}@t.local`, passwordHash: 'x', name: '2FA T', role: 'manager' }
  });
  userId = u.id;
});

afterAll(async () => {
  await prisma.twoFactorChallenge.deleteMany({ where: { userId } });
  await prisma.twoFactorBackupCode.deleteMany({ where: { userId } });
  await prisma.user.delete({ where: { id: userId } });
  await prisma.$disconnect();
});

describe('twoFactor service (integration)', () => {
  it('create → verify happy path; challenge row is consumed', async () => {
    const { code } = await createTwoFactorChallenge(prisma, userId);
    expect(await verifyTwoFactorCode(prisma, userId, code)).toEqual({ ok: true, method: 'challenge' });
    expect(await prisma.twoFactorChallenge.findUnique({ where: { userId } })).toBeNull();
  });

  it('re-login overwrites the previous challenge (single row per user)', async () => {
    const first = await createTwoFactorChallenge(prisma, userId);
    await createTwoFactorChallenge(prisma, userId);
    expect(await verifyTwoFactorCode(prisma, userId, first.code)).toEqual({ ok: false, error: 'invalid_code' });
    const rows = await prisma.twoFactorChallenge.findMany({ where: { userId } });
    expect(rows).toHaveLength(1);
    await prisma.twoFactorChallenge.deleteMany({ where: { userId } });
  });

  it('5 wrong attempts kill the challenge', async () => {
    await createTwoFactorChallenge(prisma, userId);
    for (let i = 0; i < 5; i++) {
      expect((await verifyTwoFactorCode(prisma, userId, '000000')).ok).toBe(false);
    }
    expect(await verifyTwoFactorCode(prisma, userId, '000000')).toEqual({ ok: false, error: 'too_many_attempts' });
    expect(await prisma.twoFactorChallenge.findUnique({ where: { userId } })).toBeNull();
  });

  it('backup code works once; regeneration invalidates old codes', async () => {
    const { codes } = await generateBackupCodes(prisma, userId);
    await createTwoFactorChallenge(prisma, userId);
    expect(await verifyTwoFactorCode(prisma, userId, codes[0]!)).toEqual({ ok: true, method: 'backup' });
    await createTwoFactorChallenge(prisma, userId);
    expect(await verifyTwoFactorCode(prisma, userId, codes[0]!)).toEqual({ ok: false, error: 'invalid_code' });
    const { codes: fresh } = await generateBackupCodes(prisma, userId);
    expect(await verifyTwoFactorCode(prisma, userId, codes[1]!)).toEqual({ ok: false, error: 'invalid_code' });
    expect(await verifyTwoFactorCode(prisma, userId, fresh[0]!)).toEqual({ ok: true, method: 'backup' });
  });
});
```

Внимание: между сценариями challenge может отсутствовать — `verifyTwoFactorCode` для backup-пути требует живую challenge (fallback идёт только при существующей). Тест выше создаёт challenge перед каждым backup-вводом.

- [ ] **Step 2: Прогнать** `npx vitest run --mode=integration src/__tests__/services.auth.twoFactor.integration.test.ts` (нужен Docker PG из gate).
- [ ] **Step 3: Commit** `test(2fa): integration coverage for the twoFactor service`

---

### Task 7: `buildSessionClaims` — вынос из login

**Files:** Create: `src/lib/auth/buildSessionClaims.ts`; Modify: `src/app/api/auth/login/route.ts` (строки 71-150)

- [ ] **Step 1: Создать хелпер** — перенести дословно блок login-роута (partnerRole / assignedOrgIds / organizationMemberships / managedOrgIds+C8 / managerRole / accessProfile):

```ts
import type { PrismaClient, User } from '@prisma/client';
import type { OrganizationMembership, SessionPayload } from '@/lib/auth/jwt';
import { toSessionAccessProfile, type SessionAccessProfile } from '@/lib/auth/accessProfile';

export type BuildClaimsResult =
  | { ok: true; claims: SessionPayload }
  | { ok: false; error: 'account_deactivated' };

/**
 * Сборка клеймов сессии из строки User — вынесена из login-роута, чтобы
 * 2FA-verify выдавал ТОТ ЖЕ токен, что и однофакторный логин. Любая новая
 * денормализация в токен добавляется здесь (одна точка).
 */
export async function buildSessionClaims(prisma: PrismaClient, user: User): Promise<BuildClaimsResult> {
  // <перенос существующего кода login-роута — без изменений логики;
  //  ветка `!membership.isActive` возвращает { ok: false, error: 'account_deactivated' }>
  // финал:
  // return { ok: true, claims: { sub: user.id, role: user.role, ... } };
}
```

(Полный код — дословный перенос строк 71-150 login-роута; единственное отличие — вместо `NextResponse` deactivated-ветка возвращает Result.)

- [ ] **Step 2: login-роут** — заменить блок на:

```ts
  const built = await buildSessionClaims(prisma, user);
  if (!built.ok) {
    return NextResponse.json({ code: 'ACCOUNT_DEACTIVATED', message: 'Account deactivated' }, { status: 403 });
  }
  const token = await signToken(built.claims);
```

- [ ] **Step 3: Существующие login-тесты зелёные** — `npx vitest run --mode=unit src/__tests__/api.auth.login.partner-enrichment.test.ts src/__tests__/api.auth.ratelimit.test.ts` (+ файл(ы) `grep -l "auth/login" src/__tests__`). Поведение не изменилось — правки тестов не требуются (кроме моков, если они мокали внутренности роута).
- [ ] **Step 4: Commit** `refactor(auth): extract buildSessionClaims from the login route`

---

### Task 8: Email-шаблон

**Files:** Create: `src/lib/email/templates/two-factor-code.tsx`; Test: дописать в `src/__tests__/cov.email-partner-shared.test.tsx`-стиле новый блок (или отдельный файл `components.email-two-factor.test.tsx`) — `renderToStaticMarkup` содержит код и НЕ содержит его в subject.

- [ ] **Step 1: Шаблон** (по образцу `password-reset.tsx` — посмотреть его перед написанием):

```tsx
import * as React from 'react';

export function twoFactorCodeSubject(): string {
  return 'Код подтверждения входа'; // код НЕ в теме — лок-скрин уведомления
}

export function twoFactorCodeText({ name, code }: { name: string; code: string }): string {
  return [
    `Здравствуйте, ${name}!`,
    '',
    `Ваш код подтверждения входа: ${code}`,
    'Код действует 10 минут. Если вы не входили в кабинет — смените пароль.'
  ].join('\n');
}

export function TwoFactorCodeTemplate({ name, code }: { name: string; code: string }) {
  return (
    <div style={{ fontFamily: 'Arial, sans-serif', maxWidth: 480 }}>
      <h2 style={{ color: '#111111' }}>Код подтверждения входа</h2>
      <p>Здравствуйте, {name}!</p>
      <p style={{ fontSize: 28, fontWeight: 700, letterSpacing: 6 }}>{code}</p>
      <p style={{ color: '#6B7280' }}>
        Код действует 10 минут. Если вы не входили в кабинет — смените пароль.
      </p>
    </div>
  );
}
```

(Сверить обвязку со стилем `password-reset.tsx` и повторить её.)

- [ ] **Step 2: Тест** (рендер содержит код; subject не содержит):

```tsx
it('renders the code in the body and never in the subject', () => {
  const html = renderToStaticMarkup(<TwoFactorCodeTemplate name='Иван' code='123456' />);
  expect(html).toContain('123456');
  expect(twoFactorCodeSubject()).not.toContain('123456');
  expect(twoFactorCodeText({ name: 'Иван', code: '123456' })).toContain('123456');
});
```

- [ ] **Step 3: Commit** `feat(2fa): two-factor code email template`

---

### Task 9: Login-роут — 2FA-ветка

**Files:** Modify: `src/app/api/auth/login/route.ts`; Test: Create `src/__tests__/api.auth.login.2fa.test.ts`

- [ ] **Step 1: Failing tests** (mock-паттерн `vi.hoisted` — эталон `api.manager.documents.upload.test.ts`; мокаются `@/lib/db/prisma`, `bcryptjs`, `@/lib/rateLimit`, `@/lib/featureFlags`, `@/lib/services/auth/twoFactor`, `@/lib/email/send`, `@/lib/auth/buildSessionClaims`, `@/lib/auth/jwt`):

Сценарии:
1. флаг ON + role=manager → 200 `{ ok: true, twoFactorRequired: true }`, cookie `2fa_pending` выставлена, `session` НЕ выставлена, `createTwoFactorChallenge` вызван, `send` вызван;
2. флаг ON + role=admin → то же;
3. флаг ON + role=partner → обычный логин (session-cookie, без challenge);
4. флаг OFF + role=manager → обычный логин;
5. флаг ON + сбой `send` (reject) → 502 `{ code: 'EMAIL_SEND_FAILED' }`, challenge удалена (`twoFactorChallenge.delete`), cookie нет;
6. audit `2fa_code_sent` записан (мок recordAudit).

```ts
// каркас
const res = await POST(jsonReq({ email: 'm@x.ru', password: 'pw' }));
expect(res.status).toBe(200);
expect(await res.json()).toEqual({ ok: true, twoFactorRequired: true });
const setCookie = res.headers.get('set-cookie') ?? '';
expect(setCookie).toContain('2fa_pending=');
expect(setCookie).not.toContain('session=');
```

- [ ] **Step 2: Run — FAIL.**
- [ ] **Step 3: Реализация** — в login-роуте после `buildSessionClaims` (успешный bcrypt, до выдачи токена):

```ts
import { isFeatureEnabled } from '@/lib/featureFlags';
import { createTwoFactorChallenge } from '@/lib/services/auth/twoFactor';
import { signTwoFactorPendingToken } from '@/lib/auth/jwt';
import { send } from '@/lib/email/send';
import { TwoFactorCodeTemplate, twoFactorCodeSubject, twoFactorCodeText } from '@/lib/email/templates/two-factor-code';
import { recordAudit } from '@/lib/auth/audit';

const isStaff = user.role === 'admin' || user.role === 'manager';
if (isStaff && isFeatureEnabled('staff_2fa')) {
  const { code } = await createTwoFactorChallenge(prisma, user.id);
  try {
    await send({
      to: user.email,
      subject: twoFactorCodeSubject(),
      html: await renderHtml(React.createElement(TwoFactorCodeTemplate, { name: user.name, code })),
      text: twoFactorCodeText({ name: user.name, code })
    });
  } catch (err) {
    // Без письма войти нельзя — честная ошибка вместо тихого проглота (§3-исключение осознанно)
    await prisma.twoFactorChallenge.delete({ where: { userId: user.id } }).catch(() => {});
    log.error('[auth/login] 2fa email send failed', {
      userId: user.id, error: err instanceof Error ? err.message : String(err)
    });
    return NextResponse.json({ code: 'EMAIL_SEND_FAILED', message: 'Failed to send the code' }, { status: 502 });
  }
  await recordAudit(prisma, {
    action: '2fa_code_sent', entity: 'auth_2fa', entityId: user.id, userId: user.id
  }).catch(() => {});
  const pending = await signTwoFactorPendingToken(user.id);
  const res = NextResponse.json({ ok: true, twoFactorRequired: true });
  res.cookies.set('2fa_pending', pending, {
    httpOnly: true, sameSite: 'lax',
    secure: process.env.NODE_ENV === 'production', path: '/', maxAge: 600
  });
  return res;
}
```

`renderHtml` — тот же dynamic-import хелпер, что в reset-password/request (скопировать 4 строки). `recordAudit` — сверить сигнатуру с `src/lib/auth/audit.ts` перед использованием.
ВАЖНО: ветка ставится ПОСЛЕ `buildSessionClaims` — так деактивированный партнёр не получает письмо (порядок проверок не меняется).

- [ ] **Step 4: Тесты зелёные (новые + все старые login-тесты)** → **Step 5: Commit** `feat(2fa): login issues an email challenge for staff behind staff_2fa`

---

### Task 10: `/api/auth/2fa/verify`

**Files:** Create: `src/app/api/auth/2fa/verify/route.ts`; Test: Create `src/__tests__/api.auth.2fa.verify.test.ts`

- [ ] **Step 1: Failing tests.** Сценарии: флаг OFF → 404; нет cookie → 401 SESSION_EXPIRED; битый pending-JWT → 401 SESSION_EXPIRED; rate-limit → 429; `verifyTwoFactorCode`=code_expired|too_many_attempts|invalid_code → 401 с соотв. UPPER-кодом; happy: session-cookie выставлен, `2fa_pending` погашен (maxAge=0), audit `2fa_verified`; backup-код: audit `2fa_backup_used`; user исчез из БД → 401 SESSION_EXPIRED.
- [ ] **Step 2: Run — FAIL.**
- [ ] **Step 3: Реализация**:

```ts
import { NextRequest, NextResponse } from 'next/server';
import { z } from 'zod';
import { prisma } from '@/lib/db/prisma';
import { notFoundIfDisabled } from '@/lib/featureFlags';
import { verifyTwoFactorPendingToken, signToken } from '@/lib/auth/jwt';
import { verifyTwoFactorCode } from '@/lib/services/auth/twoFactor';
import { buildSessionClaims } from '@/lib/auth/buildSessionClaims';
import { recordAudit } from '@/lib/auth/audit';
import { isRateLimited } from '@/lib/rateLimit';

const schema = z.object({ code: z.string().min(1).max(64) });

function clientIp(req: NextRequest): string {
  const fwd = req.headers.get('x-forwarded-for');
  if (fwd) return fwd.split(',')[0]!.trim();
  return req.headers.get('x-real-ip') ?? 'unknown';
}

export async function POST(req: NextRequest) {
  const disabled = notFoundIfDisabled('staff_2fa');
  if (disabled) return disabled;

  if (await isRateLimited(`2fa-verify:${clientIp(req)}`, { windowMs: 60_000, max: 10 })) {
    return NextResponse.json({ code: 'TOO_MANY_REQUESTS', message: 'Too many attempts' }, { status: 429 });
  }

  const pending = req.cookies.get('2fa_pending')?.value;
  if (!pending) return NextResponse.json({ code: 'SESSION_EXPIRED', message: 'Login again' }, { status: 401 });

  let sub: string;
  try {
    ({ sub } = await verifyTwoFactorPendingToken(pending));
  } catch {
    return NextResponse.json({ code: 'SESSION_EXPIRED', message: 'Login again' }, { status: 401 });
  }

  const parsed = schema.safeParse(await req.json().catch(() => null));
  if (!parsed.success) {
    return NextResponse.json({ code: 'INVALID_REQUEST', message: 'Invalid request' }, { status: 400 });
  }

  const result = await verifyTwoFactorCode(prisma, sub, parsed.data.code.trim());
  if (!result.ok) {
    await recordAudit(prisma, {
      action: '2fa_failed', entity: 'auth_2fa', entityId: sub, userId: sub, reason: result.error
    }).catch(() => {});
    return NextResponse.json({ code: result.error.toUpperCase(), message: 'Verification failed' }, { status: 401 });
  }

  const user = await prisma.user.findUnique({ where: { id: sub } });
  if (!user || !user.isActive) {
    return NextResponse.json({ code: 'SESSION_EXPIRED', message: 'Login again' }, { status: 401 });
  }
  const built = await buildSessionClaims(prisma, user);
  if (!built.ok) {
    return NextResponse.json({ code: 'ACCOUNT_DEACTIVATED', message: 'Account deactivated' }, { status: 403 });
  }

  await recordAudit(prisma, {
    action: result.method === 'backup' ? '2fa_backup_used' : '2fa_verified',
    entity: 'auth_2fa', entityId: sub, userId: sub
  }).catch(() => {});

  const token = await signToken(built.claims);
  const res = NextResponse.json({ ok: true });
  res.cookies.set('session', token, {
    httpOnly: true, sameSite: 'lax',
    secure: process.env.NODE_ENV === 'production', path: '/', maxAge: 60 * 60 * 24 * 7
  });
  res.cookies.set('2fa_pending', '', { httpOnly: true, path: '/', maxAge: 0 });
  return res;
}
```

- [ ] **Step 4: Тесты зелёные** → **Step 5: Commit** `feat(2fa): verify endpoint exchanges the code for a session`

---

### Task 11: `/api/auth/2fa/resend`

**Files:** Create: `src/app/api/auth/2fa/resend/route.ts`; Test: Create `src/__tests__/api.auth.2fa.resend.test.ts`

- [ ] **Step 1: Failing tests.** Сценарии: флаг OFF → 404; нет/битый cookie → 401; чаще 1/30с → 429; >3 переотправок → 429; happy: новый код (createTwoFactorChallenge вызван), письмо отправлено, 200; сбой send → 502 EMAIL_SEND_FAILED.
- [ ] **Step 2 — FAIL. Step 3: Реализация** (двухключевой лимитер по образцу reset-request: `2fa-resend-cool:<sub>` 1/30с и `2fa-resend-total:<sub>` 3/10мин; тело — как 2FA-ветка login: createTwoFactorChallenge + send + audit `2fa_code_sent`; user грузится из БД для email/name, отсутствует → 401 SESSION_EXPIRED).
- [ ] **Step 4: Тесты зелёные** → **Step 5: Commit** `feat(2fa): resend endpoint with cooldown limits`

---

### Task 12: LoginForm — шаг ввода кода

**Files:** Modify: `src/components/auth/login-form.tsx`; Test: Create `src/__tests__/components.login-form.2fa.test.tsx` (jsdom; существующий `components.login-form.test.tsx` НЕ трогать — прежний одношаговый путь остаётся)

- [ ] **Step 1: Failing tests.** Сценарии (fetch-мок): ответ login `{ twoFactorRequired: true }` → появляется поле «Код из письма», router.push НЕ вызван; сабмит кода → POST `/api/auth/2fa/verify` с `{ code }`, при ok → router.push('/dashboard'); ошибка INVALID_CODE → показан текст `errorMessageRu('invalid_code')`; кнопка «Отправить код ещё раз» дизейблится на 30с (vi.useFakeTimers) и дёргает `/api/auth/2fa/resend`; «Назад» возвращает шаг 1.
- [ ] **Step 2 — FAIL. Step 3: Реализация** — состояние `step: 'credentials' | 'code'`; в `onSubmit` после `res.ok`: `const data = await res.json(); if (data.twoFactorRequired) { setStep('code'); return; } router.push('/dashboard')`; второй сабмит-хендлер для кода; resend-кнопка с `useState(cooldown)` + `useEffect`-интервал; все строки — RU, ошибки через `errorMessageRu(code.toLowerCase())`.
- [ ] **Step 4: Тесты зелёные (оба файла формы)** → **Step 5: Commit** `feat(2fa): two-step login form`

---

### Task 13: Backup-коды self-service (action + секция + 3 страницы)

**Files:** Create: `src/server-actions/staff/backupCodes.ts`, `src/components/settings/staff-backup-codes-section.tsx`; Modify: `src/app/admin/settings/page.tsx`, `src/app/manager/settings/page.tsx`, `src/app/leader/settings/page.tsx`; Tests: Create `src/__tests__/server-actions.staff.backupCodes.test.ts`, `components.staff-backup-codes-section.test.tsx`; Modify: `pages.admin-settings.test.tsx`, `pages.manager-settings.test.tsx`, `pages.leader-settings.test.tsx`

- [ ] **Step 1: Action-тест** (staff ok / partner forbidden / флаг OFF → forbidden; audit `2fa_backup_regenerated`).
- [ ] **Step 2: Action:**

```ts
'use server';
import { prisma } from '@/lib/db/prisma';
import { getSession } from '@/lib/auth/session'; // сверить точный импорт guard'ов в соседних actions
import { isFeatureEnabled } from '@/lib/featureFlags';
import { generateBackupCodes } from '@/lib/services/auth/twoFactor';
import { recordAudit } from '@/lib/auth/audit';

export async function regenerateBackupCodesAction(): Promise<
  { ok: true; codes: string[] } | { ok: false; error: 'forbidden' }
> {
  const session = await getSession();
  const isStaff = session && (session.role === 'admin' || session.role === 'manager');
  if (!isStaff || !isFeatureEnabled('staff_2fa')) return { ok: false, error: 'forbidden' };
  const { codes } = await generateBackupCodes(prisma, session.sub);
  await recordAudit(prisma, {
    action: '2fa_backup_regenerated', entity: 'auth_2fa', entityId: session.sub, userId: session.sub
  }).catch(() => {});
  return { ok: true, codes };
}
```

(Перед написанием сверить паттерн соседнего server-action — гард, Result-форма.)
- [ ] **Step 3: Компонент** `StaffBackupCodesSection` ('use client'): кнопка «Сгенерировать коды восстановления» → action → список 10 кодов моноширинно + предупреждение «Сохраните: показываются один раз» + кнопка копирования (`useCopy`-паттерн соседей, если есть); ошибки — `errorMessageRu`. Тест: рендер, клик → показ кодов, forbidden → ошибка.
- [ ] **Step 4: Подключение**: в трёх `settings/page.tsx` — `{isFeatureEnabled('staff_2fa') ? <StaffBackupCodesSection /> : null}`; поправить их page-тесты (новый мок featureFlags при необходимости).
- [ ] **Step 5: Все тесты зелёные** → **Step 6: Commit** `feat(2fa): self-service backup codes in staff settings`

---

### Task 14: Перевыпуск кодов админом

**Files:** Modify: `src/lib/services/admin/users/mutations.ts` (+ соотв. server-action и карточка пользователя `src/app/admin/users/[id]/page.tsx` — точные файлы смотреть по месту); Tests: дописать в `services.admin.users.test.ts` + компонентный тест кнопки

- [ ] **Step 1: Сервис-тест**: `adminRegenerateBackupCodes(prisma, session, { userId })` — admin ok (codes.length=10, audit с entityId=target), не-admin forbidden, target не staff → `not_staff`.
- [ ] **Step 2: Сервис** (в mutations.ts, сигнатура §3):

```ts
export async function adminRegenerateBackupCodes(
  prisma: PrismaClient,
  session: SessionPayload,
  args: { userId: string }
): Promise<{ ok: true; codes: string[] } | { ok: false; error: 'forbidden' | 'not_found' | 'not_staff' }> {
  if (session.role !== 'admin') return { ok: false, error: 'forbidden' };
  const target = await prisma.user.findUnique({ where: { id: args.userId }, select: { id: true, role: true } });
  if (!target) return { ok: false, error: 'not_found' };
  if (target.role !== 'admin' && target.role !== 'manager') return { ok: false, error: 'not_staff' };
  const { codes } = await generateBackupCodes(prisma, target.id);
  await recordAudit(prisma, {
    action: '2fa_backup_regenerated', entity: 'auth_2fa', entityId: target.id, userId: session.sub
  }).catch(() => {});
  return { ok: true, codes };
}
```

- [ ] **Step 3: UI** — кнопка «Перевыпустить коды восстановления» в карточке `/admin/users/[id]` (видна для staff-target при флаге), диалог через общий `Dialog`-примитив (§9), показывает новые коды один раз. RU-код `not_staff: 'Пользователь не является сотрудником.'` в messages.ts (+тест).
- [ ] **Step 4: Тесты зелёные** → **Step 5: Commit** `feat(2fa): admin reissues a staff user's backup codes`

---

### Task 15: Документация + финальная верификация + PR

**Files:** Modify: `.env.example`, `.env.production.example`, `docs/feature-flags-matrix.md`, `CLAUDE.md` (§5 — упомянуть staff_2fa как поведенческий флаг, по образцу max_channel-комментария в featureFlags)

- [ ] **Step 1: env-примеры** — в блок opt-in флагов обоих файлов:

```
# FEATURE_STAFF_2FA=0            # 2FA сотрудников: email-код при логине admin/manager/leader (спека 2026-07-11)
```

- [ ] **Step 2: Полная верификация** (последовательно, НЕ параллельно — §6):
  - `npm run typecheck` → 0 ошибок
  - `npm run lint` → 0 warnings
  - `npm run test:unit` → зелёный
  - `npm run gate` → зелёный (миграция применится в Docker PG)
  - `npm run test:coverage` → 100% все метрики (новые файлы обязаны быть покрыты полностью)
- [ ] **Step 3: Push + PR** — `git push -u origin claude/staff-2fa`; PR на base `claude/release-hardening-r0` (stacked), тело: спека + чек-лист тестов + rollout-инструкция (включить FEATURE_STAFF_2FA=1 после мержа и прогрева).
- [ ] **Step 4: Close-out** — `docs/superpowers/plans/2026-07-11-staff-2fa-DONE.md` (что отгружено, отличия от плана).

---

## Self-Review (выполнен при написании)

- **Spec coverage:** флаг (Task 2), модель (1), login-ветка (9), verify (10), resend (11), pre-auth guard (4), хелпер клеймов (7), email (8), UI-форма (12), self-service коды (13), админ-перевыпуск (14), RU-коды (3), доки+верификация (15). Все разделы спеки покрыты.
- **Types:** `VerifyTwoFactorResult`, `BuildClaimsResult`, имена функций сверены между задачами (createTwoFactorChallenge / verifyTwoFactorCode / generateBackupCodes / signTwoFactorPendingToken / verifyTwoFactorPendingToken / buildSessionClaims / adminRegenerateBackupCodes).
- **Известные точки сверки по месту** (не placeholder'ы, а проверка чужих сигнатур): `recordAudit`, паттерн server-action guard, обвязка email-шаблона, точные имена login-тестов. Каждая помечена в своей задаче.
