# Plan: M5 — Календарь сотрудников

**Дата:** 2026-07-17
**Spec:** [2026-07-17-m5-calendar-design.md](../specs/2026-07-17-m5-calendar-design.md)
**Ветка:** `claude/m5-calendar` (от `main`@`774d490`)
**Паттерн:** subagent-driven (как M4) — сервисный слой в основной сессии, тесты — параллельными субагентами.

## Задачи

- [x] **Task 1 — Схема + миграция.** `CalendarEvent` + `CalendarEventAttendee` (спека §2), back-relations User/Company/Order/Organization, миграция `20260717124939_m5_calendar`, `prisma generate`.
- [x] **Task 2 — Сервисный слой.** `src/lib/services/calendar/`: `policy.ts` (`canSeeEvent` — C8-floor + уровень `tasks`), `events.ts` (CRUD, Result-контракт, zod, `validateRefs`, `remindAt`-пересчёт + rearm, audit `calendar_event_*`), `items.ts` (`listCalendarItems` — события ∪ задачи через `taskWhereForLevel`, `getEventFormOptions`). `AuditEntity` += `'calendar_event'`.
- [x] **Task 3 — Напоминания (worker).** Очередь `notifications.calendarReminder` (queues.ts), `CALENDAR_REMINDER_SCHEDULES` (`*/5 * * * *`) + `registerCalendarReminderSchedules` (scheduling.ts), процессор `calendar-reminder.ts` (атомарный claim `reminderSentAt`, протухшие >24ч, fan-out создатель∪участники, best-effort), регистрация в `worker/index.ts`.
- [x] **Task 4 — Флаг + server-actions.** `staff_calendar` (opt-in, 3 точки: nav / page-notFound / actions-гейт, middleware-точки нет — прецедент `internal_tasks`). `src/server-actions/calendar/index.ts`: create/update/delete + revalidate обеих страниц.
- [x] **Task 5 — UI.** `src/lib/calendar/month.ts` (чистая математика сетки — общая для выборки и рендера), `calendar-month-view.tsx` (сетка 6×7 Monday-first, чипы событий/задач, «Ближайшие», навигация `?m=YYYY-MM`), `event-dialog.tsx` (Dialog-примитив, идиома task-dialog). Страницы `/manager/calendar` + `/leader/calendar` (sibling, `force-dynamic`, флаг-гейт). Nav-пункты «Календарь» 📅 manager/leader.
- [x] **Task 6 — Тесты (3 субагента параллельно).**
  - A: обновление канон-тестов (nav leader/manager счётчики, featureFlags default-набор, сайдбары).
  - B: unit — policy / events / items / month / reminder / server-actions / scheduling.
  - C: page-тесты обеих страниц, компонент-тесты (event-dialog, month-view), integration-тест процессора (guardrail `worker.processor-coverage`).
- [x] **Task 7 — Гейты + close-out.** `typecheck` + `lint` + `test:unit` зелёные; коммиты серии; `-DONE.md`.

## Тестовая стратегия

См. спеку §7. Integration-слой ограничен процессором (guardrail-минимум); полный `test:coverage`/`build`/browser-smoke — на контроллере при финализации ветки (как M4).
