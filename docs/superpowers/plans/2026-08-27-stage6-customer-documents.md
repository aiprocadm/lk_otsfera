# Этап 6 «Документы для заказчика: счёт, акт, договор, ДС» — план

Спека — [2026-08-27-stage6-customer-documents-design.md](../specs/2026-08-27-stage6-customer-documents-design.md)
(предъявлена 27.08.2026, PR [#446](https://github.com/aiprocadm/lk_otsfera/pull/446);
умолчания §6 действуют).
Требования `У-141`…`У-160` действующего
[ТЗ кабинетов, документов и интеграций](../../tz/2026-08-21-tz-cabinets-documents-integrations.md).

REQUIRED SUB-SKILL: superpowers:subagent-driven-development

## Разбивка

| PR | Что | Требования | Статус |
|---|---|---|---|
| PR-1 «Фундамент» | `DocumentLine`, статусы и итоги на `Document`, матрица переходов, аудит-коды | `У-146`, `У-148` (модель) | ✅ |
| PR-2 «Печать» | Шесть колонок, сумма прописью, итоги и НДС, логотип/подпись/печать | `У-141`, `У-142`, `У-153` | ✅ |
| PR-3 «Форма выпуска» | Диалог с предпросмотром, сверка сумм, транзакция без рендера внутри, реквизиты по типу | `У-147`, `У-143`, `У-152`, `У-156` | ✅ |
| PR-4a «Имя файла и номера» | `Content-Disposition` во всех трёх роутах, номер и версия в списках | `У-154` | ✅ |
| PR-4b «Доступ» | Панель в трёх кабинетах, флаг opt-out, загрузка менеджером, документы партнёру | `У-144`, `У-155`, `У-158` | ✅ |
| PR-5a «Приёмка заказчиком» | Состояние и сумма в карточке, кнопка «Принять», уведомление менеджеру, запрос реквизитов раз в сутки | `У-150`, `У-157`, часть `У-159` | ✅ |
| PR-5b «Отправка письмом» | Отправка документа клиенту, признак оплаты счёта, остаток аудита | `У-149`, `У-148` (оплата), остаток `У-159` | ⏳ |
| PR-6 «Документ без заказа» | Выпуск из карточки организации и сделки | `У-145` | ⏳ |
| PR-7 «Шаблоны текстов» | Раздел хаба, версия шаблона, «вернуть стандартный» | `У-160` | ⏳ |
| PR-8 «Нумерация и версии» | Версии, уникальность, FK; **dry-run + миграция данных последней** | `У-151` | ⏳ |

**Порядок:** PR-1 первым — на нём стоят все остальные. PR-8 последним:
миграция данных идёт после того, как код перестанет зависеть от старой
схемы номеров (порядок, доказавший себя в этапе 5).

## PR-1 «Фундамент» — `У-146`, `У-148` (модель)

- [x] Модель `DocumentLine` — снимок строк документа на момент выпуска
      (`title`, `quantity`, `unit`, `unitPrice`, `discountPercent`,
      `vatRate`, `vatAmount`, `amount`, `sortOrder`), каскад по документу.
- [x] `Document`: `status DocumentStatus` (`draft` · `issued` · `sent` ·
      `accepted` · `rejected` · `expired` · `cancelled`), итоги
      `amountNet`/`amountVat`/`amountGross`, `currency`, `parentDocumentId`
      (акт → счёт, ДС → договор), `sentAt`/`sentById`,
      `acceptedAt`/`acceptedByUserId`, `cancelledAt`/`cancelReason`,
      `templateVersion`.
- [x] Миграция аддитивная; существующие документы получают `issued`, а при
      заполненном `signedAt` — `accepted`. **Строк и итогов старым
      документам не выдаём** — `У-146` прямо запрещает выдумывать суммы
      задним числом.
- [x] Матрица допустимых переходов — одним объектом в коде + сервис
      `setDocumentStatus` с кодом `invalid_transition`; страж на матрицу,
      проверенный мутацией.
- [x] Аудит-коды и русские названия для смены статуса.

### Как сделан PR-1

- Матрица — **один объект** `lib/documents/statusMatrix.ts`: тип документа →
  статус → куда можно. Разбросанные по сервисам `if`-ы разъезжаются, и
  документ получает состояние, которого по бумаге быть не может.
- `draft` / `rejected` / `expired` — состояния КП (этап 7). У счёта, акта,
  договора и ДС их нет, и это записано в матрице, а не оставлено на «никто
  так не сделает».
- `setDocumentStatus` — **единственная дверь** к полю `status`; страж
  `security.document-status.guardrail` ищет прямые `document.update` со
  `status` мимо неё (мутация «обход двери» ловится).
- Старым документам строки и итоги **не выдаются** — `У-146` прямо запрещает
  бэкфилл сумм задним числом; миграция лишь проставляет `accepted` тем, у
  кого заполнен `signedAt`.
- Мутации: «принятый документ снова отправляется», «аннулированный
  воскресает», «сервис перестал спрашивать матрицу», «запись мимо двери» —
  все ловятся.

## PR-2 «Печать» — ~У-141~, ~У-142~, ~У-153~

- [x] ~lib/format/amountInWords.ts~ — сумма прописью своей функцией: склонения
      (11–14 «многие», женский род тысяч), копейки цифрами, ноль, миллионы,
      «12.5» = пятьдесят копеек (дополняем справа, а не слева).
- [x] ~services/documents/printTable.ts~ — табличная часть: шесть колонок
      (№ · Наименование · Кол-во · Ед. · Цена · Сумма), «Итого», строка НДС,
      «Всего к оплате» (только когда налог сверху), «Всего наименований N»
      и сумма прописью. Арифметика — ~lineMath~ этапа 5.
- [x] ~fallbackPrintLine~ (~У-142~) — заказ без состава печатается одной
      строкой «Услуги по заказу №N» на сумму заказа, ставка НДС — своя у
      заказа, иначе умолчание компании.
- [x] ~services/documents/branding.ts~ (~У-153~) — логотип, подпись и печать
      из слотов ~У-138~ со статусом ~clean~; читаются ДО транзакции.
- [x] Оба шаблона (счёт/акт и договор/ДС) печатают одну и ту же таблицу и
      оформление.
- [x] Мутации: убрал две колонки · убрал логотип из разметки · погасил «Всего
      к оплате» · заглушка начисляет НДС сверху — все четыре ловятся.

### Как сделан PR-2

- **Одна арифметика на всё.** Печать не считает суммы заново, а зовёт
  ~computeLineTotals~ этапа 5. Считать «ещё раз, но здесь» — самый простой
  способ показать клиенту в счёте не ту цифру, что на карточке заказа.
- **Итог заглушки равен сумме заказа.** ~Order.totalAmount~ — это итог **с**
  НДС (его пишет ~syncOrderTotal~), поэтому у заказа без состава налог
  выделяется из суммы, а не добавляется сверху. Иначе счёт оказался бы
  больше, чем показывает карточка.
- **«Цены без НДС» ≠ «НДС не облагается».** Прежний код печатал вторую фразу
  при ~vatIncluded: false~ — это разные вещи: первое про способ расчёта,
  второе про освобождение от налога. Освобождение задаёт только пустая
  ставка; тест этого места переписан под требование.
- **Деньги форматируем сами.** ~toLocaleString('ru-RU')~ на сборке Node без
  полного ICU молча даёт «15,000.00» вместо «15 000,00» — в счёте это
  недопустимо.
- **Проверяем разметку, а не «%PDF-».** Рендер отдаёт сжатые байты, в которых
  текста не найти; поэтому компонент зовётся напрямую и обходится дерево —
  так видно, что шесть колонок и сумма прописью реально попали на страницу.
  Плюс один честный рендер с настоящим PNG: битые байты роняют рендер уже
  после того, как номер счёта израсходован.
- **Оформление не может сорвать выпуск.** Сбой хранилища, чужой формат,
  непроверенный антивирусом файл — слот просто пустеет (~У-153~: «без них —
  как сейчас»).

## PR-3 «Форма выпуска» — `У-147`, `У-143`, `У-152`, `У-156`

- [x] `issue-document-dialog.tsx` — одна форма на четыре типа: тип · контрагент ·
      правимый состав · дата · период акта и **выбор счёта-основания** · предмет,
      срок и порядок оплаты договора · текст изменения ДС.
- [x] Предпросмотр PDF до выпуска — роут `api/manager/documents/preview`:
      тот же шаблон и те же данные, **номер не резервируется**.
- [x] `У-143` — расхождение сумм отвечает кодом с ОБЕИМИ цифрами; форма
      показывает их и три ответа.
- [x] `У-152` — три коротких шага вместо одной транзакции; ключ файла с UUID;
      компенсирующее удаление объекта при сбое записи.
- [x] `У-156` — набор реквизитов по типу документа, контрольные суммы ИНН,
      ОГРН и расчётного счёта по БИК, снят `take: 50`.
- [x] Документ сохраняет свои строки-снимок и итоги (доводит `У-146` до дела).
- [x] Мутации (шесть): договор перестал требовать подписанта заказчика ·
      расхождение сумм замолчали · компенсирующее удаление сняли · ключ без
      UUID · корсчёт правилом расчётного · форма решает за человека — все ловятся.

### Как сделан PR-3

- **Предпросмотр не тратит номера.** Общая часть выпуска и предпросмотра —
  `loadIssueContext`; счётчик трогает только выпуск. Иначе каждое «посмотреть,
  как получится» пробивало бы дыру в нумерации.
- **Одна схема входа на оба пути** (`issueInput.ts`): разъедься они, человек
  видел бы один документ, а клиент получал бы другой.
- **Три шага вместо одной транзакции.** Раньше транзакция держала строку
  счётчика номеров всё время рендера PDF и загрузки в хранилище, а откат
  оставлял файл-сироту (`Д-1`, `Д-2`).
- **Сумма спрашивается, а не выбирается.** Сервис отдаёт обе цифры и ждёт
  ответа; форма показывает их рядом. Выбрать за человека — значит однажды
  выставить не ту сумму.
- **Реквизиты по типу.** Счёт платят по банковским реквизитам, договор
  подписывают люди — наборы разные (`Д-9`, `Д-10`). Заполненное, но неверное
  значение попадает в тот же список отдельной строкой: реквизит с опечаткой
  хуже отсутствующего, он выглядит готовым (`Д-11`).
- **КПП спрашиваем только у юрлиц:** у ИП (12 цифр ИНН) его не существует, и
  требовать его значило бы не дать выставить счёт предпринимателю.
- **Проверено на живой базе** (временная БД, общую не трогали): выпуск
  счёта → акт наследует номер → повтор даёт версию 2; у документа
  сохраняются свои строки, итоги, `currency` и ссылка на основание.

## PR-4a «Имя файла и номера» — `У-154`

- [x] `lib/documents/fileName.ts` — «Счёт С-2026-7 от 26.07.2026.pdf»; у
      документа без номера остаётся своё имя.
- [x] Три сервиса скачивания отдают это имя, три роута передают его в
      `createSignedUrl` и в ответ `fileName`; клиент сохраняет файл под ним.
- [x] Номер и версия — в общем списке документов (значит сразу у заказчика,
      партнёра и сотрудников); версия видна только у переизданного документа.
- [x] Страж `security.document-download-name` на все три роута, проверен
      мутацией «роут снова подписывает ссылку без имени».

### Как сделан PR-4a

- **Механизм был готов, не хватало аргумента.** `contentDisposition` в
  `s3.ts` существовал с этапа 8, но роуты звали `createSignedUrl` без третьего
  параметра — и файл сохранялся под ключом хранилища
  (`invoice-v1-<uuid>.pdf`). Поэтому страж следит именно за вызовом.
- **Имя не выдумывается.** У загруженного вручную документа номера нет —
  остаётся имя, под которым его загрузили. Подменять его «Документ … .pdf»
  значило бы потерять смысл файла.
- **Расширение берётся у файла, а не у типа документа.**
- **Номер и версия попали в ОДИН список**, который рисуют все кабинеты, —
  правило зеркала соблюдается само собой.

## PR-4b «Доступ» — `У-144`, `У-155`, `У-158`

- [x] Панель выпуска смонтирована в карточке заказа у менеджера, руководителя
      и админа — один и тот же компонент (`Д-13`).
- [x] Флаг `document_generation` переведён в opt-out, задокументирован в
      `.env.example` и в матрице флагов (`Д-20`).
- [x] Партнёр видит документы организаций своего портфеля (`У-155`, `Д-15`):
      выборка вкладки берёт оба канала, право даёт портфель
      (`canPartnerAccessOrg`), изоляция закреплена тестом.
- [x] Форма загрузки документа смонтирована в карточке заказа (`Д-14`) — сразу
      и у руководителя, потому что деталка общая.
- [x] Админская загрузка переведена на общий движок `upload-core` (`Д-18`):
      правильные тип, направление, размер и путь **по организации заказа**.
- [x] Мутации: панель не доходит до экрана у руководителя · портфель не
      проверяется · контрагент снова произвольный — все ловятся.

### Как сделан PR-4b

- **Страж зеркала пришлось усилить.** Первая версия проверяла наличие
  `<GenerateDocumentsPanel>` в файле — и промолчала на мутации «панель собрана,
  но не передана в разметку». Теперь проверяется и то, что она доходит до
  экрана.
- **Право партнёру даёт портфель, а не канал.** Изоляция при этом стала
  точнее: `canPartnerAccessOrg` сверяет и принадлежность портфелю, и
  назначенный сотруднику скоуп — партнёр не дотянется до соседней организации
  той же компании.
- **Админская загрузка клала документ в папку случайной организации** той же
  компании (`findFirst` без привязки к заказу) и не писала размер файла. Общий
  движок закрывает это целиком, а не заплаткой.

## PR-5a «Приёмка заказчиком» — `У-150`, `У-157`, часть `У-159`

- [x] Карточка документа показывает состояние, сумму, отметки «отправлен» и
      «принят» — во всех кабинетах одной и той же карточкой.
- [x] Заказчик принимает акт, договор и ДС кнопкой «Принять»/«Подписать»
      (`services/documents/accept.ts`); счёт вручную **не принимается** — его
      состояние определяют платежи.
- [x] Менеджер получает уведомление `document_accepted` (реестр + правила
      `У-127`); сбой доставки не отменяет приёмку.
- [x] `У-157`: запрос реквизитов — не чаще раза в сутки на организацию;
      кнопка сообщает, когда просили в прошлый раз (`Д-12`).
- [x] Мутации: счёт принимается вручную · ограничение раз в сутки снято —
      обе ловятся.

### Как сделан PR-5a

- **Счёт не принимают руками.** Кнопка «Оплачено» у клиента была бы способом
  объявить оплату, которой не было; состояние счёта считают платежи (`У-148`).
- **Статус меняется через ту же дверь** `setDocumentStatus`: приёмка не
  обходит матрицу переходов, поэтому аннулированный документ принять нельзя.
- **Ограничение «раз в сутки» считается по журналу.** Отдельное поле «когда
  просили» заводить не понадобилось — событие уже пишется. Запись идёт **до**
  отправки письма: иначе сбой доставки снимал бы ограничение, и клиент получил
  бы второй запрос сразу.
