# Этап 11 PR-1 — группировка меню сотрудников + счётчики (ФТ-15.1, 15.2)

Дата: 2026-07-27 · Спека: [2026-07-27-stage11-navigation-ux-design.md](../specs/2026-07-27-stage11-navigation-ux-design.md)
(✅ подтверждена 27.07.2026) · Ветка `claude/stage11-navigation` **от main** (§14).

## Задачи

### 1. Группировка меню менеджера и руководителя (ФТ-15.1)

- [x] Логика группировки вынесена из `admin-sidebar` в чистую
      `lib/navigation/groupItems.ts` (секции с одинаковым названием
      склеиваются; пункты без `group` — безымянная секция сверху).
- [x] `manager-sidebar` и `leader-sidebar` рендерят секции; `admin-sidebar`
      переведён на общий хелпер.
- [x] Проставлен `group` 36 пунктам: менеджер — Продажи · Работа · Клиенты ·
      Коммуникации · Финансы · Данные · Настройки (состав из ФТ-15.1);
      руководитель — те же смысловые секции + Аналитика.

### 2. Счётчики меню (ФТ-15.2)

- [x] `getStaffBadges` дополнен двумя счётчиками, названными ТЗ: новые
      обращения клиентов (`status='submitted'` поверх скоупа роли) и
      непрочитанная переписка (готовый `unreadCount` сервиса чата).
- [x] Ключи привязаны к пунктам «Обращения клиентов» (менеджер, руководитель)
      и «Сообщения» (менеджер).
- [x] Второго эндпоинта не появилось: тот же `/api/staff/badges`, тот же
      поллинг 30 с в `nav-badge` — как требует ТЗ.

### 3. Тесты

- [x] Сервис бейджей: все четыре счётчика; «новые обращения» фильтруются по
      `submitted`; непрочитанные вне скоупа → 0, а не падение.
- [x] Сайдбары и навигация — существующие наборы зелёные (48 тестов).

## Отклонение от спеки

Хлебные крошки (ФТ-15.6) перенесены в PR-2 к «Моему дню»: меню и счётчики —
законченная тема, а крошки требуют правок деталок цепочки заявка → лид →
сделка → заказ и логичнее едут вместе с дашбордом. Разбивка на 3 PR сохранена.

## Отгрузка

- [ ] typecheck/lint/unit зелёные; CHANGELOG, STATUS, PR с `base: main`.
