# Чат v1 (привязка к заказу) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Per-order chat that replaces order comments — up to two isolated threads per order (`org` / `partner`), команда видит всё, поллинг, вложения, непрочитанное, feature-flag `chat` с fallback на старый comment-UI.

**Architecture:** Новый домен `src/lib/services/chat/` с Result-контрактом; данные в `OrderThread`/`Message`/`ThreadReadState`; RBAC в 3 местах (middleware-флаг, route-`requireRole`+`requireFeature`, service-`canSeeThread`); тонкие роуты `src/app/api/messages/**`; sibling-UI по ролям; разовый backfill `Comment → Message`. Транспорт — клиентский поллинг.

**Tech Stack:** Next.js 15 (App Router) · Prisma 5 + PostgreSQL · Supabase Storage · Vitest. Спека: [chat-v1-design](../specs/2026-06-02-chat-v1-design.md).

---

## File Structure

**Create:**
- `src/lib/services/chat/policy.ts` — `deriveSide`, `canSeeThread`, scope-хелперы.
- `src/lib/services/chat/threads.ts` — `findOrCreateThread`, `listThreads`, `unreadCount`, `markRead`.
- `src/lib/services/chat/messages.ts` — `sendMessage`, `listMessages` (+ вложение).
- `src/lib/services/chat/migrate.ts` — `migrateCommentsToMessages` (backfill, идемпотентный).
- `src/app/api/messages/route.ts` — `GET` (список сообщений треда) + `POST` (отправка).
- `src/app/api/messages/threads/route.ts` — `GET` (inbox: список тредов роли).
- `src/app/api/messages/read/route.ts` — `POST` (отметить прочитанным).
- `src/components/chat/chat-thread-view.tsx` — презентационный (domain-agnostic) вид треда (разрешён §4).
- `src/components/chat/chat-composer.tsx` — inline-композер (текст + вложение).
- `src/components/{partner,organization,manager,admin}/*-messages-*.tsx` — sibling-обёртки по ролям.
- `src/app/partner/messages/page.tsx`, `src/app/organization/messages/page.tsx` — новые страницы.
- `src/hooks/useThreadPolling.ts` — клиентский поллинг.
- `scripts/migrate-comments-to-messages.ts` — обёртка-скрипт над `migrateCommentsToMessages`.
- Тесты — рядом в `src/__tests__/` (см. задачи).

**Modify:**
- `prisma/schema.prisma` — enum `ThreadSide` + модели + relations на `Order`/`User`.
- `src/lib/featureFlags.ts` — добавить `chat` в `FEATURE_FLAGS` + `OPT_IN_FLAGS`.
- `src/middleware.ts` — флаг-гейт префиксов `/<role>/messages`.
- `src/lib/navigation/cabinet.ts` — пункты меню «Сообщения»/«Чат» с `flag: 'chat'`.
- `src/app/manager/messages/page.tsx`, `src/app/admin/messages/page.tsx` — добавить вкладку «Чат».
- Order-detail comment-поверхности (см. задачу 13).

---

## Phase 0 — Схема и миграция БД

### Task 1: Prisma-модели чата

**Files:**
- Modify: `prisma/schema.prisma` (добавить enum + 3 модели; relations на `model Order` и `model User`)
- Test: `src/__tests__/schema.chat.test.ts`

- [ ] **Step 1: Failing test — модели существуют и связаны**

```ts
// src/__tests__/schema.chat.test.ts
import { describe, it, expect } from 'vitest';
import { Prisma } from '@prisma/client';

describe('chat schema', () => {
  it('exposes OrderThread / Message / ThreadReadState models', () => {
    const models = Prisma.dmmf.datamodel.models.map((m) => m.name);
    expect(models).toContain('OrderThread');
    expect(models).toContain('Message');
    expect(models).toContain('ThreadReadState');
  });

  it('OrderThread has a (orderId, side) unique', () => {
    const t = Prisma.dmmf.datamodel.models.find((m) => m.name === 'OrderThread')!;
    const uniq = t.uniqueIndexes.some(
      (u) => u.fields.length === 2 && u.fields.includes('orderId') && u.fields.includes('side')
    );
    expect(uniq).toBe(true);
  });

  it('ThreadSide enum has org and partner', () => {
    const e = Prisma.dmmf.datamodel.enums.find((x) => x.name === 'ThreadSide')!;
    expect(e.values.map((v) => v.name).sort()).toEqual(['org', 'partner']);
  });
});
```

- [ ] **Step 2: Run, verify FAIL**

Run: `npx vitest run src/__tests__/schema.chat.test.ts`
Expected: FAIL (`OrderThread` not in models).

- [ ] **Step 3: Add schema**

```prisma
enum ThreadSide {
  org
  partner
}

model OrderThread {
  id            String     @id @default(cuid())
  orderId       String
  order         Order      @relation(fields: [orderId], references: [id], onDelete: Cascade)
  side          ThreadSide
  createdAt     DateTime   @default(now())
  lastMessageAt DateTime   @default(now())
  messages      Message[]
  readStates    ThreadReadState[]

  @@unique([orderId, side])
  @@index([side, lastMessageAt])
}

model Message {
  id             String      @id @default(cuid())
  threadId       String
  thread         OrderThread @relation(fields: [threadId], references: [id], onDelete: Cascade)
  authorId       String
  author         User        @relation("MessageAuthor", fields: [authorId], references: [id])
  body           String
  attachmentPath String?
  createdAt      DateTime    @default(now())

  @@index([threadId, createdAt])
}

model ThreadReadState {
  id         String      @id @default(cuid())
  threadId   String
  thread     OrderThread @relation(fields: [threadId], references: [id], onDelete: Cascade)
  userId     String
  lastReadAt DateTime    @default(now())

  @@unique([threadId, userId])
}
```

Add to `model Order`: `threads OrderThread[]`.
Add to `model User`: `authoredMessages Message[] @relation("MessageAuthor")`.

- [ ] **Step 4: Migrate + generate**

Run: `npm run prisma:migrate -- --name chat_v1` then `npm run prisma:generate`
Expected: migration applied, client regenerated.

- [ ] **Step 5: Run test to verify PASS**

Run: `npx vitest run src/__tests__/schema.chat.test.ts`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add prisma/schema.prisma prisma/migrations src/__tests__/schema.chat.test.ts
git commit -m "feat(chat): OrderThread/Message/ThreadReadState models + migration"
```

---

## Phase 1 — Feature-flag

### Task 2: Флаг `chat` (opt-in)

**Files:**
- Modify: `src/lib/featureFlags.ts`
- Test: `src/__tests__/featureFlags.chat.test.ts`

- [ ] **Step 1: Failing test**

```ts
// src/__tests__/featureFlags.chat.test.ts
import { describe, it, expect, afterEach } from 'vitest';
import { isFeatureEnabled } from '@/lib/featureFlags';

describe('chat feature flag', () => {
  const KEY = 'FEATURE_CHAT';
  afterEach(() => { delete process.env[KEY]; });

  it('is OFF by default (opt-in)', () => {
    delete process.env[KEY];
    expect(isFeatureEnabled('chat')).toBe(false);
  });
  it('turns on with truthy env', () => {
    process.env[KEY] = '1';
    expect(isFeatureEnabled('chat')).toBe(true);
  });
});
```

- [ ] **Step 2: Run, verify FAIL** — `npx vitest run src/__tests__/featureFlags.chat.test.ts` → FAIL (type error: `'chat'` not assignable to `FeatureFlag`).

- [ ] **Step 3: Implement** — в `src/lib/featureFlags.ts` добавить `'chat'` в массив `FEATURE_FLAGS` и в `OPT_IN_FLAGS`:

```ts
export const FEATURE_FLAGS = [
  'partner_leads', 'commission_pdf', 'commission_xlsx', 'one_c_sync',
  'pwa_installer', 'document_scan', 'organization_cabinet', 'manager_cabinet',
  'chat',
] as const;

const OPT_IN_FLAGS = new Set<FeatureFlag>([
  'organization_cabinet', 'manager_cabinet', 'chat',
]);
```

- [ ] **Step 4: Run, verify PASS.**
- [ ] **Step 5: Commit** — `git commit -am "feat(chat): add opt-in 'chat' feature flag"`

---

## Phase 2 — Service + RBAC (ядро)

### Task 3: `chat/policy.ts` — деривация стороны + `canSeeThread`

**Files:**
- Create: `src/lib/services/chat/policy.ts`
- Test: `src/__tests__/services.chat.policy.test.ts`

- [ ] **Step 1: Failing test (роли × стороны)**

```ts
// src/__tests__/services.chat.policy.test.ts
import { describe, it, expect } from 'vitest';
import { canSeeThread, deriveSide } from '@/lib/services/chat/policy';
import type { SessionPayload } from '@/lib/auth/jwt';

const order = { id: 'o1', organizationId: 'org1', partnerId: 'p1' };
const s = (over: Partial<SessionPayload>): SessionPayload =>
  ({ sub: 'u', role: 'manager', ...over } as SessionPayload);

describe('deriveSide', () => {
  it('org user → org', () => expect(deriveSide(s({ role: 'organization' }))).toBe('org'));
  it('partner user → partner', () => expect(deriveSide(s({ role: 'partner' }))).toBe('partner'));
  it('manager → null (must pass side explicitly)', () => expect(deriveSide(s({ role: 'manager' }))).toBeNull());
});

describe('canSeeThread', () => {
  it('any manager sees both sides', () => {
    expect(canSeeThread(s({ role: 'manager' }), 'org', order)).toBe(true);
    expect(canSeeThread(s({ role: 'manager' }), 'partner', order)).toBe(true);
  });
  it('admin sees all', () => expect(canSeeThread(s({ role: 'admin' }), 'partner', order)).toBe(true));
  it('partner sees only partner side of own partner', () => {
    const sess = s({ role: 'partner', partnerId: 'p1' });
    expect(canSeeThread(sess, 'partner', order)).toBe(true);
    expect(canSeeThread(sess, 'org', order)).toBe(false);
  });
  it('partner cannot see another partner', () => {
    expect(canSeeThread(s({ role: 'partner', partnerId: 'pX' }), 'partner', order)).toBe(false);
  });
  it('org user sees only org side of own org', () => {
    const sess = s({ role: 'organization', organizationMemberships: ['org1'] } as Partial<SessionPayload>);
    expect(canSeeThread(sess, 'org', order)).toBe(true);
    expect(canSeeThread(sess, 'partner', order)).toBe(false);
  });
});
```

- [ ] **Step 2: Run, verify FAIL** — module not found.

- [ ] **Step 3: Implement**

```ts
// src/lib/services/chat/policy.ts
import type { ThreadSide } from '@prisma/client';
import type { SessionPayload } from '@/lib/auth/jwt';

export type ThreadOrder = {
  id: string;
  organizationId: string | null;
  partnerId: string | null;
};

/** Внешняя сторона выводится из роли; команда (manager/admin) выбирает сторону явно. */
export function deriveSide(session: SessionPayload): ThreadSide | null {
  if (session.role === 'organization') return 'org';
  if (session.role === 'partner') return 'partner';
  return null;
}

function orgIds(session: SessionPayload): string[] {
  // Phase 7 multi-org session; legacy singular kept as fallback.
  return (
    (session as { organizationMemberships?: string[] }).organizationMemberships ??
    (session.organizationId ? [session.organizationId] : [])
  );
}

export function canSeeThread(
  session: SessionPayload,
  side: ThreadSide,
  order: ThreadOrder
): boolean {
  // Командная видимость: любой менеджер/руководитель/админ видит обе стороны.
  if (session.role === 'manager' || session.role === 'admin') return true;
  if (session.role === 'organization') {
    return side === 'org' && !!order.organizationId && orgIds(session).includes(order.organizationId);
  }
  if (session.role === 'partner') {
    return side === 'partner' && !!order.partnerId && session.partnerId === order.partnerId;
  }
  return false;
}
```

- [ ] **Step 4: Run, verify PASS.**
- [ ] **Step 5: Commit** — `git add -A && git commit -m "feat(chat): policy — deriveSide + canSeeThread (team-wide visibility)"`

> **Note for executor:** сверь точное имя поля мульти-орг сессии (`organizationMemberships`) в `src/lib/auth/jwt.ts`/`organizationPolicy.ts`; если оно другое — поправь `orgIds()` и тест соответственно (это единственная неуверенная сигнатура).

### Task 4: `findOrCreateThread` (+ integration)

**Files:**
- Create: `src/lib/services/chat/threads.ts`
- Test (unit): `src/__tests__/services.chat.threads.test.ts`
- Test (integration): `src/__tests__/services.chat.threads.integration.test.ts`

- [ ] **Step 1: Failing integration test (идемпотентность)**

```ts
// src/__tests__/services.chat.threads.integration.test.ts
import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import { PrismaClient } from '@prisma/client';
import { findOrCreateThread } from '@/lib/services/chat/threads';
// helpers: предполагается фабрика сидов как в worker.oneCSync.upsert.test.ts

const prisma = new PrismaClient();
afterAll(async () => { await prisma.$disconnect(); });

describe('findOrCreateThread', () => {
  it('creates once per (orderId, side) and is idempotent', async () => {
    // ... сид: партнёр p1, организация org1, заказ o1 (organizationId=org1, partnerId=p1)
    const team = { sub: 'mgr1', role: 'manager' } as any;
    const r1 = await findOrCreateThread(prisma, team, { orderId: 'o1', side: 'org' });
    const r2 = await findOrCreateThread(prisma, team, { orderId: 'o1', side: 'org' });
    expect(r1.ok && r2.ok).toBe(true);
    if (r1.ok && r2.ok) expect(r1.thread.id).toBe(r2.thread.id);
    const count = await prisma.orderThread.count({ where: { orderId: 'o1', side: 'org' } });
    expect(count).toBe(1);
  });
});
```

- [ ] **Step 2: Run, verify FAIL** (нужен живой Postgres — `npm run test:integration` или gate). Expected FAIL: module/функция отсутствует.

- [ ] **Step 3: Implement `findOrCreateThread` + типы Result**

```ts
// src/lib/services/chat/threads.ts
import type { PrismaClient, ThreadSide, OrderThread } from '@prisma/client';
import type { SessionPayload } from '@/lib/auth/jwt';
import { canSeeThread } from './policy';

export type ChatError = 'forbidden' | 'order_not_found';

export type ThreadResult =
  | { ok: true; thread: OrderThread }
  | { ok: false; error: ChatError };

export async function findOrCreateThread(
  prisma: PrismaClient,
  session: SessionPayload,
  args: { orderId: string; side: ThreadSide }
): Promise<ThreadResult> {
  const order = await prisma.order.findUnique({
    where: { id: args.orderId },
    select: { id: true, organizationId: true, partnerId: true }
  });
  if (!order) return { ok: false, error: 'order_not_found' };
  if (!canSeeThread(session, args.side, order)) return { ok: false, error: 'forbidden' };

  const thread = await prisma.orderThread.upsert({
    where: { orderId_side: { orderId: args.orderId, side: args.side } },
    update: {},
    create: { orderId: args.orderId, side: args.side }
  });
  return { ok: true, thread };
}
```

- [ ] **Step 4: Run integration, verify PASS** — `npm run test:integration -- services.chat.threads.integration`
- [ ] **Step 5: Commit** — `git add -A && git commit -m "feat(chat): findOrCreateThread (idempotent per order+side)"`

### Task 5: `sendMessage` (+ уведомления, integration)

**Files:**
- Create: `src/lib/services/chat/messages.ts`
- Test (unit): `src/__tests__/services.chat.messages.test.ts` (мок prisma + notify via `vi.hoisted`)
- Test (integration): `src/__tests__/services.chat.messages.integration.test.ts`

- [ ] **Step 1: Failing unit test (Result-коды + fan-out)** — мок-паттерн как в `api.manager.documents.upload.test.ts`:

```ts
// src/__tests__/services.chat.messages.test.ts
import { describe, it, expect, vi, beforeEach } from 'vitest';
const { notifyManagers, notifyOrgUsers } = vi.hoisted(() => ({
  notifyManagers: vi.fn(), notifyOrgUsers: vi.fn()
}));
vi.mock('@/lib/notifications', () => ({ notifyManagers, notifyOrgUsers }));
vi.mock('@/lib/auth/audit', () => ({ recordAudit: vi.fn() }));

import { sendMessage } from '@/lib/services/chat/messages';

const order = { id: 'o1', organizationId: 'org1', partnerId: 'p1', orderNumber: 'N1', title: 'T' };

function prismaMock(over: any = {}) {
  return {
    order: { findUnique: vi.fn().mockResolvedValue(order) },
    orderThread: { upsert: vi.fn().mockResolvedValue({ id: 't1', side: 'org', orderId: 'o1' }), update: vi.fn().mockResolvedValue({}) },
    message: { create: vi.fn().mockResolvedValue({ id: 'm1' }) },
    ...over
  } as any;
}

beforeEach(() => { notifyManagers.mockReset(); notifyOrgUsers.mockReset(); });

describe('sendMessage', () => {
  it('rejects empty body', async () => {
    const r = await sendMessage(prismaMock(), { sub: 'u', role: 'organization', organizationMemberships: ['org1'] } as any, { orderId: 'o1', side: 'org', body: '   ' });
    expect(r).toEqual({ ok: false, error: 'empty_body' });
  });
  it('org author notifies managers', async () => {
    const r = await sendMessage(prismaMock(), { sub: 'u', role: 'organization', organizationMemberships: ['org1'] } as any, { orderId: 'o1', side: 'org', body: 'hi' });
    expect(r.ok).toBe(true);
    expect(notifyManagers).toHaveBeenCalled();
  });
  it('forbidden when partner writes org side', async () => {
    const r = await sendMessage(prismaMock(), { sub: 'u', role: 'partner', partnerId: 'p1' } as any, { orderId: 'o1', side: 'org', body: 'hi' });
    expect(r).toEqual({ ok: false, error: 'forbidden' });
  });
});
```

- [ ] **Step 2: Run, verify FAIL.**

- [ ] **Step 3: Implement `sendMessage`** (Result-контракт; fan-out best-effort, как в `manager/uploads.ts`):

```ts
// src/lib/services/chat/messages.ts
import type { PrismaClient, ThreadSide } from '@prisma/client';
import type { SessionPayload } from '@/lib/auth/jwt';
import { canSeeThread } from './policy';
import { recordAudit } from '@/lib/auth/audit';
import { notifyManagers, notifyOrgUsers } from '@/lib/notifications';

export type SendError = 'forbidden' | 'order_not_found' | 'empty_body' | 'too_large';
export type SendResult = { ok: true; messageId: string } | { ok: false; error: SendError };

const MAX_BODY = 5000;

export async function sendMessage(
  prisma: PrismaClient,
  session: SessionPayload,
  args: { orderId: string; side: ThreadSide; body: string; attachmentPath?: string }
): Promise<SendResult> {
  const body = args.body?.trim() ?? '';
  if (!body) return { ok: false, error: 'empty_body' };
  if (body.length > MAX_BODY) return { ok: false, error: 'too_large' };

  const order = await prisma.order.findUnique({
    where: { id: args.orderId },
    select: { id: true, organizationId: true, partnerId: true, orderNumber: true, title: true }
  });
  if (!order) return { ok: false, error: 'order_not_found' };
  if (!canSeeThread(session, args.side, order)) return { ok: false, error: 'forbidden' };

  const thread = await prisma.orderThread.upsert({
    where: { orderId_side: { orderId: args.orderId, side: args.side } },
    update: { lastMessageAt: new Date() },
    create: { orderId: args.orderId, side: args.side }
  });

  const message = await prisma.message.create({
    data: { threadId: thread.id, authorId: session.sub, body, attachmentPath: args.attachmentPath ?? null }
  });

  await recordAudit(prisma, {
    action: 'message_sent', entity: 'order_thread', entityId: thread.id,
    userId: session.sub, after: { messageId: message.id, side: args.side }
  });

  // Best-effort fan-out: внешний автор → команде; команда → внешней стороне.
  const isTeam = session.role === 'manager' || session.role === 'admin';
  try {
    if (isTeam) {
      if (args.side === 'org' && order.organizationId) {
        await notifyOrgUsers(prisma, {
          organizationId: order.organizationId,
          type: 'chat_message',
          payload: { orderId: order.id, orderNumber: order.orderNumber, orderTitle: order.title, excerpt: body.slice(0, 200) }
        });
      }
      // side==='partner': partner-нотификация (см. notifyMessageCreated / partner-канал)
    } else {
      await notifyManagers(prisma, {
        orderId: order.id, type: 'chat_message',
        payload: { excerpt: body.slice(0, 200), side: args.side }
      });
    }
  } catch (err) {
    console.warn('[chat/sendMessage] notify failed', { messageId: message.id, error: err instanceof Error ? err.message : String(err) });
  }

  return { ok: true, messageId: message.id };
}
```

- [ ] **Step 4: Run unit, verify PASS.**
- [ ] **Step 5: Integration test** — отправка + чтение из БД, scope-проверка (партнёр не пишет org). Запуск: `npm run test:integration -- services.chat.messages.integration`. Implement до зелёного.
- [ ] **Step 6: Commit** — `git commit -am "feat(chat): sendMessage with team/external notify fan-out"`

> **Note:** `'chat_message'` — новый notification type. Проверь, что `notifyOrgUsers`/`notifyManagers` принимают произвольный `type` (они принимают строку `type` + `payload`); если есть union-тип notification — расширь его в `src/lib/notifications.ts`/`@/lib/jobs/types`.

### Task 6: `listMessages` / `listThreads` / `markRead` / `unreadCount`

**Files:**
- Modify: `src/lib/services/chat/threads.ts` (+ `listThreads`, `markRead`, `unreadCount`), `src/lib/services/chat/messages.ts` (+ `listMessages`)
- Test: `src/__tests__/services.chat.inbox.test.ts` (unit) + `...inbox.integration.test.ts`

- [ ] **Step 1: Failing integration test** — кейсы:
  - `listThreads` менеджера возвращает треды обеих сторон; партнёра — только partner-стороны его заказов; орг — только org-стороны её заказов (cursor по `lastMessageAt`).
  - `listMessages(threadId, after?)` отдаёт сообщения по возрастанию `createdAt`, фильтр `after`.
  - `markRead` upsert'ит `ThreadReadState.lastReadAt`; `unreadCount` = число тредов с `lastMessageAt > lastReadAt` и наличием чужих сообщений.

- [ ] **Step 2: Run, verify FAIL.**

- [ ] **Step 3: Implement** (узкие селекты §13; scope-фильтры — те же примитивы, что в `canSeeThread`; для менеджера/админа — без фильтра по стороне). Пример `listMessages`:

```ts
// messages.ts (добавить)
export async function listMessages(
  prisma: PrismaClient, session: SessionPayload,
  args: { threadId: string; after?: string }
): Promise<{ ok: true; rows: Array<{ id: string; authorId: string; body: string; attachmentPath: string | null; createdAt: Date }> } | { ok: false; error: 'forbidden' | 'thread_not_found' }> {
  const thread = await prisma.orderThread.findUnique({
    where: { id: args.threadId },
    select: { id: true, side: true, order: { select: { id: true, organizationId: true, partnerId: true } } }
  });
  if (!thread) return { ok: false, error: 'thread_not_found' };
  if (!canSeeThread(session, thread.side, thread.order)) return { ok: false, error: 'forbidden' };
  const rows = await prisma.message.findMany({
    where: { threadId: thread.id, ...(args.after ? { createdAt: { gt: new Date(args.after) } } : {}) },
    select: { id: true, authorId: true, body: true, attachmentPath: true, createdAt: true },
    orderBy: { createdAt: 'asc' }, take: 200
  });
  return { ok: true, rows };
}
```

- [ ] **Step 4: Run, verify PASS.**
- [ ] **Step 5: Commit** — `git commit -am "feat(chat): inbox/read/unread + listMessages (scoped)"`

---

## Phase 3 — API-роуты (тонкие)

### Task 7: Роуты `/api/messages`

**Files:**
- Create: `src/app/api/messages/route.ts`, `src/app/api/messages/threads/route.ts`, `src/app/api/messages/read/route.ts`
- Test: `src/__tests__/api.messages.test.ts`

- [ ] **Step 1: Failing test** — `POST /api/messages` с org-сессией → 201; чужая сторона → 403; флаг off → 404; пустое тело → 400. (мок сервиса + сессии через `vi.hoisted`, как в `api.manager.documents.upload.test.ts`).

- [ ] **Step 2: Run, verify FAIL.**

- [ ] **Step 3: Implement тонкий роут** (флаг-гейт + мап Result→HTTP; для внешней роли `side = deriveSide`, для команды — из тела):

```ts
// src/app/api/messages/route.ts
import { requireSession, requireRole } from '@/lib/auth/guard';
import { notFoundIfDisabled } from '@/lib/featureFlags';
import { prisma } from '@/lib/db/prisma';
import { sendMessage } from '@/lib/services/chat/messages';
import { listMessages } from '@/lib/services/chat/messages';
import { deriveSide } from '@/lib/services/chat/policy';

export async function POST(req: Request) {
  const off = notFoundIfDisabled('chat'); if (off) return off;
  const sess = await requireSession(); if (!sess.ok) return sess.response;
  const s = sess.value;
  const body = await req.json().catch(() => null);
  if (!body?.orderId || typeof body.body !== 'string') {
    return Response.json({ ok: false, error: 'bad_request' }, { status: 400 });
  }
  const side = deriveSide(s) ?? body.side; // команда передаёт side явно
  const result = await sendMessage(prisma, s, { orderId: body.orderId, side, body: body.body, attachmentPath: body.attachmentPath });
  if (!result.ok) {
    const status = result.error === 'forbidden' ? 403 : result.error === 'order_not_found' ? 404 : result.error === 'too_large' ? 413 : 400;
    return Response.json({ ok: false, error: result.error }, { status });
  }
  return Response.json({ ok: true, messageId: result.messageId }, { status: 201 });
}

export async function GET(req: Request) {
  const off = notFoundIfDisabled('chat'); if (off) return off;
  const sess = await requireSession(); if (!sess.ok) return sess.response;
  const url = new URL(req.url);
  const threadId = url.searchParams.get('threadId') ?? '';
  const after = url.searchParams.get('after') ?? undefined;
  const result = await listMessages(prisma, sess.value, { threadId, after });
  if (!result.ok) return Response.json({ ok: false, error: result.error }, { status: result.error === 'forbidden' ? 403 : 404 });
  return Response.json({ ok: true, rows: result.rows });
}
```

(аналогично тонкие `threads/route.ts` → `listThreads`, `read/route.ts` → `markRead`.)

- [ ] **Step 4: Run, verify PASS.**
- [ ] **Step 5: Commit** — `git commit -am "feat(chat): thin /api/messages routes (flag-gated)"`

> **Вложение:** загрузка файла повторяет `createOrderDocument` (MIME allow-list + 20 МБ + Supabase upload + enqueue `docs.scanDocument`). В v1 — отдельный шаг: клиент грузит файл, получает `attachmentPath`, передаёт его в `POST /api/messages`. Реализуй как `POST /api/messages/attachment` либо переиспользуй существующий upload-путь; скачивание — через signed URL (§10).

---

## Phase 4 — Backfill комментариев

### Task 8: `migrateCommentsToMessages`

**Files:**
- Create: `src/lib/services/chat/migrate.ts`, `scripts/migrate-comments-to-messages.ts`
- Test (integration): `src/__tests__/services.chat.migrate.integration.test.ts`

- [ ] **Step 1: Failing integration test** — сид: заказ с комментариями org-автора, partner-автора, manager-автора; после миграции:
  - org-коммент → тред `(order, org)`; partner-коммент → `(order, partner)`;
  - manager-коммент → сторона ближайшего предыдущего внешнего комментария (fallback `org`);
  - `attachmentPath` перенесён; повторный запуск не дублирует (идемпотентность по карте `commentId`).

- [ ] **Step 2: Run, verify FAIL.**

- [ ] **Step 3: Implement** — детерминированный проход по `Comment` (orderBy createdAt asc), для каждого: вычислить сторону по роли автора (`organization`→org, `partner`→partner, иначе — сторона последнего внешнего на этом заказе, иначе `org`), `findOrCreate` тред, создать `Message` с `createdAt = comment.createdAt`, идемпотентность через таблицу-карту или маркер (например, не мигрировать, если уже есть `Message` с тем же `authorId+body+createdAt` в треде). Обнови `thread.lastMessageAt`.

- [ ] **Step 4: Run, verify PASS.**
- [ ] **Step 5: Commit** — `git commit -am "feat(chat): Comment→Message backfill (side heuristic, idempotent)"`

---

## Phase 5 — Флаг: middleware + навигация

### Task 9: Гейт префиксов + пункты меню

**Files:**
- Modify: `src/middleware.ts`, `src/lib/navigation/cabinet.ts`, `src/lib/auth/access.ts` (если префиксы там)
- Test: `src/__tests__/api.featureFlags.gating.test.ts` (расширить), `src/__tests__/navigation.cabinet.partner.test.ts`

- [ ] **Step 1: Failing test** — при `chat` off страница `/partner/messages` → 404 (middleware); пункт меню «Сообщения» отсутствует в `navItemsFor('partner')`.

- [ ] **Step 2: Run, verify FAIL.**

- [ ] **Step 3: Implement** — в `navByRole` добавить `{ href: '/partner/messages', label: 'Сообщения', flag: 'chat' }` и `{ href: '/organization/messages', label: 'Сообщения', flag: 'chat' }`; у менеджера/админа пункт `/…/messages` уже есть (вкладка «Чат» внутри — задача 13). В middleware — флаг-гейт новых префиксов (после auth, как `manager_cabinet`/`organization_cabinet`).

- [ ] **Step 4: Run, verify PASS.**
- [ ] **Step 5: Commit** — `git commit -am "feat(chat): middleware flag-gate + nav items"`

---

## Phase 6 — UI по ролям (sibling-паттерн §4)

> Все компоненты, тестируемые юнитом, **должны** `import React` (vitest без react-плагина — см. project-vitest-classic-jsx). Презентационный `chat-thread-view` принимает domain-agnostic тип → общий допустим.

### Task 10: Презентационные `chat-thread-view` + `chat-composer`

**Files:**
- Create: `src/components/chat/chat-thread-view.tsx`, `src/components/chat/chat-composer.tsx`
- Test: `src/__tests__/components.chat-thread-view.test.tsx`

- [ ] **Step 1: Failing test** — рендерит список сообщений (автор, тело, время), различает «моё/чужое» по `currentUserId`, показывает empty-state.
- [ ] **Step 2: FAIL.**
- [ ] **Step 3: Implement** `ChatThreadView({ messages, currentUserId })` + `ChatComposer({ onSend, disabled })` (inline textarea + кнопка + опц. вложение; палитра `#F97316`/`#EA580C`; русские строки). Domain-agnostic тип `ChatMessageVM = { id; authorId; authorName; body; attachmentPath?; createdAt }`.
- [ ] **Step 4: PASS.**
- [ ] **Step 5: Commit** — `git commit -am "feat(chat): presentational thread view + composer"`

### Task 11: Партнёрский кабинет — `/partner/messages`

**Files:**
- Create: `src/app/partner/messages/page.tsx`, `src/components/partner/partner-messages-inbox.tsx`
- Test: `src/__tests__/components.partner-messages-inbox.test.tsx`

- [ ] **Step 1: Failing test** — inbox рендерит список тредов (по заказам партнёра), пустое состояние, ссылку в тред.
- [ ] **Step 2: FAIL.**
- [ ] **Step 3: Implement** — страница: `requireRole`-эквивалент для партнёра + `notFoundIfDisabled('chat')` fallback на странице; грузит `listThreads`; рендерит `partner-messages-inbox` (обёртка вокруг `chat-thread-view`/`chat-composer`); поллинг через `useThreadPolling` (задача 14). Зеркаль `manager-messages-inbox.tsx`.
- [ ] **Step 4: PASS.**
- [ ] **Step 5: Commit** — `git commit -am "feat(chat): partner /messages inbox (covers C2)"`

### Task 12: Кабинет организации — `/organization/messages`

**Files:** Create: `src/app/organization/messages/page.tsx`, `src/components/organization/organization-messages-inbox.tsx`; Test: `src/__tests__/components.organization-messages-inbox.test.tsx`

- [ ] **Steps 1–5** — зеркало задачи 11 для роли `organization` (org-сторона своих заказов). Sibling, НЕ переиспользуем партнёрский компонент. Commit: `feat(chat): organization /messages inbox`.

### Task 13: Менеджер/админ — вкладка «Чат» + перенаведение order-detail

**Files:**
- Modify: `src/app/manager/messages/page.tsx`, `src/app/admin/messages/page.tsx` (+ вкладки), `src/components/manager/manager-order-timeline.tsx`, `src/components/organization/org-events-feed.tsx`, `src/components/partner/{deal-comments,org-comments-tab,add-comment-form}.tsx`
- Test: соответствующие `components.*` тесты

- [ ] **Step 1: Failing test** — у менеджера на странице сообщений есть вкладка «Чат», показывающая `listThreads` (обе стороны); order-detail рендерит чат-тред (с fallback на старый comment-UI при `chat` off).
- [ ] **Step 2: FAIL.**
- [ ] **Step 3: Implement** — добавить вкладку «Чат» рядом с инбоксом комментариев; на order-detail при `isFeatureEnabled('chat')` рендерить чат (сторона = по контексту: org-detail→org, partner-deal→partner; менеджер видит обе вкладками), иначе — текущий comment-компонент (fallback). Композер шлёт в `POST /api/messages`.
- [ ] **Step 4: PASS.**
- [ ] **Step 5: Commit** — `git commit -am "feat(chat): manager/admin chat tab + order-detail chat (comment fallback)"`

---

## Phase 7 — Поллинг, непрочитанное, финал

### Task 14: `useThreadPolling` + бейдж непрочитанного

**Files:** Create: `src/hooks/useThreadPolling.ts`; Modify: nav-shell (бейдж из `unreadCount`); Test: `src/__tests__/hooks.useThreadPolling.test.ts`

- [ ] **Step 1: Failing test** — хук опрашивает `GET /api/messages?threadId=&after=` на интервале и по `visibilitychange`, аппендит новые, не дублирует.
- [ ] **Step 2: FAIL.**
- [ ] **Step 3: Implement** — `'use client'` хук: `setInterval(~7000)` при `document.visibilityState==='visible'`, `after`-курсор = `createdAt` последнего; cleanup на unmount. Бейдж в навигации читает `unreadCount`.
- [ ] **Step 4: PASS.**
- [ ] **Step 5: Commit** — `git commit -am "feat(chat): client polling hook + unread badge"`

### Task 15: Финальная верификация

- [ ] `npm run typecheck` — чисто.
- [ ] `npm run lint` — чисто.
- [ ] `npm run test:unit` — зелёно.
- [ ] `npm run gate` (или `npm run test:integration` против живого Postgres) — зелёно (включая chat integration + migrate).
- [ ] `superpowers:requesting-code-review` по диффу ветки.
- [ ] `superpowers:finishing-a-development-branch` — PR/merge.

---

## Self-Review (выполнено автором плана)

**Spec coverage:** модель данных (T1), флаг с fallback (T2, T9, T13), `canSeeThread`/командная видимость (T3), findOrCreate/send/list/read/unread (T4–T6), тонкие роуты §3 (T7), вложения §10 (T7 note), миграция комментариев с эвристикой (T8), middleware+nav §5 (T9), UI по ролям sibling §4 (T10–T13), поллинг+непрочитанное (T14), верификация §6 (T15). C2 закрыт T11. ✅

**Placeholders:** код приведён для всего бэкенда; UI-задачи дают структуру + точные sibling-эталоны + тест-команды (паттерн «follow established patterns» для существующей кодовой базы), без «add error handling»-заглушек. Два явных `Note for executor` помечают единственные несверенные сигнатуры (`organizationMemberships`, union notification `type`).

**Type consistency:** `ThreadSide`, `canSeeThread(session, side, order)`, `findOrCreateThread(...,{orderId,side})`, `sendMessage(...,{orderId,side,body,attachmentPath?})`, Result-коды (`forbidden|order_not_found|empty_body|too_large|thread_not_found`) — согласованы между задачами.
