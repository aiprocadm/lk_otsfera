# Этап 3 — зеркальная логика кабинетов — план работ

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development или superpowers:executing-plans; шаги — чекбоксы `- [ ]`.

**Спека:** [2026-08-24-stage3-cabinet-mirror-design.md](../specs/2026-08-24-stage3-cabinet-mirror-design.md) ✅ подтверждена 24.08.2026 (мерж [#416](https://github.com/aiprocadm/lk_otsfera/pull/416)); заказчик ответил «да» по всем четырём вопросам: переключатель кабинетов в шапке, «Обмен с 1С» одним пунктом с вкладками, список исключений принят, порядок из шести PR принят.
**ТЗ:** [кабинеты, документы, интеграции](../../tz/2026-08-21-tz-cabinets-documents-integrations.md), `У-106`…`У-121`, решение `Р-21`.

| PR | Что | Требования | Статус |
|---|---|---|---|
| PR-1 «ключ раздела и словарь названий» | `sectionKey`, `sectionLabels.ts`, переименования, стражи названий и заголовков | `У-106`, `У-107`, `У-108` | ✅ [#417](https://github.com/aiprocadm/lk_otsfera/pull/417) |
| PR-2 «порядок групп и настройки внизу» | Единые группы меню сотрудников ЦО, `pinnedBottom` везде | `У-113` (часть), `У-114` (часть) | ✅ [#418](https://github.com/aiprocadm/lk_otsfera/pull/418) |
| PR-2b «Обмен с 1С одним пунктом» | Экран с вкладками у менеджера, старые адреса — шлюзы | `У-113` (остаток) | ← этот PR |
| PR-2c «личные настройки одним компонентом» | Профиль · Уведомления · Безопасность во всех пяти кабинетах | `У-114` (остаток) | ✅ |
| PR-3 «руководитель» | «Документы» и «Сообщения», переключатель кабинетов в шапке, вкладки «Обмен с 1С» | `У-110`, `У-111`, `У-118` | ✅ |
| PR-4 «администратор» | Список заказов, «Поиск», состав карточки заказа, `leader` в `/admin/users` | `У-112`, `У-119` | ✅ |
| PR-5a «PageHeader» | Компонент шаблона страницы, перевод всех экранов, страж | `У-120` | ✅ |
| PR-5b «деталки» | Деталки заявок и обращений у сотрудников ЦО | `У-116` | ✅ |
| PR-6a «мобильная панель» | Панель из реестра по ключам, запасные пункты | `У-117` | ✅ |
| PR-6b «переименование» | `/partner/deals` → `/partner/orders` (отдельным PR по §5 ТЗ) | `У-109` | ⏳ |
| PR-6c «зеркало» | Выравнивание кабинетов заказчика и партнёра, страж зеркальности | `У-115`, `У-121` | ⏳ |

Порядковые решения (из спеки §2.1): ключ раздела идёт **первым** — на нём
держатся и переключатель кабинетов (`У-111`), и мобильная панель (`У-117`);
страж зеркальности включается **последним**, иначе он красный всё время работы.
Ограничение §5 ТЗ: `У-117` строго перед `У-109`.

## PR-1 «ключ раздела и словарь названий» — `У-106`, `У-107`, `У-108`

- [x] Словарь [sectionLabels.ts](../../../src/lib/navigation/sectionLabels.ts):
      `SectionKey` → название и значок. Правило добавления записано в файле:
      «тот же раздел с другим названием» — это два разных раздела и два ключа
      (так живут пункты-мосты `leaderCabinet` и `myOrders`).
- [x] [cabinet.ts](../../../src/lib/navigation/cabinet.ts): пункт меню объявляет
      `sectionKey`; названия и значка в реестре **больше нет** — они выводятся
      функцией `withSection`. Экспорт `navByRole` сохранён под прежним именем:
      потребителей и тестов у него много, и менять их ради переименования не за что.
- [x] Переименования `У-107` — по одной строке словаря на раздел; глоссарий
      обновлён тем же PR.
- [x] Заголовки экранов приведены к пунктам меню: «Главная» у админа,
      руководителя и партнёра (было «Кабинет …» и «Сводка по команде»),
      «Выписка по счёту 51» (было «Импорт оплат»), «Справка» (было «Справка:
      словарь терминов» — пояснение переехало в подзаголовок).
- [x] Страж названий переписан с сегмента адреса на ключ раздела и расширен на
      вкладки карточки (`У-108`). **Проверен мутацией дважды**: первая версия
      проверки «пункт не носит своего названия» искала `label:` с начала строки
      и пропускала однострочный пункт — исправлено.
- [x] Новый страж [navigation.page-title-matches-menu.guardrail](../../../src/__tests__/navigation.page-title-matches-menu.guardrail.test.ts):
      заголовок экрана равен пункту меню. Сравнивает только буквальные
      заголовки — заголовок из данных (имя клиента) это карточка сущности, у
      неё своё правило.
- [x] Реестры тем же PR: `AUDIT.md`, `STATUS.md`, `CHANGELOG.md`, глоссарий.

## PR-2 «порядок групп и настройки внизу» — `У-113` (часть), `У-114` (часть)

- [x] Реестр порядка групп [menuGroups.ts](../../../src/lib/navigation/menuGroups.ts);
      сайдбар сортирует группы им, а не порядком пунктов в меню роли.
- [x] Пункты трёх кабинетов разложены по общим группам: Работа · Продажи ·
      Клиенты · Документы · Финансы · Коммуникации · Аналитика · Справочники
      (+ «Платформа» — пре-хабовые разделы администратора, исчезают вместе с
      флагом `settings_hub`).
- [x] Отдельной группы «Обмен с 1С» у администратора больше нет — её пункты в
      «Финансах», как у менеджера.
- [x] «Настройки» и «Справка» закреплены внизу во всех шести кабинетах; туда же
      уехали пункты-мосты в соседний кабинет.
- [x] Остаток переименований `У-107`: «Выписка (сч. 51)» → «Выписка по счёту 51»
      во вкладке обмена и в истории загрузок.
- [x] Страж [navigation.menu-groups.guardrail](../../../src/__tests__/navigation.menu-groups.guardrail.test.ts)
      — **проверен мутацией** (возврат своей группы у менеджера роняет тест).
- [x] Реестры тем же PR: `AUDIT.md`, `STATUS.md`, `CHANGELOG.md`.

**Почему PR-2 разделён.** В спеке он охватывал три разные работы: порядок групп
(правка реестра), объединение «Обмена с 1С» (новый экран с вкладками и историей
у менеджера) и личные настройки одним компонентом (переделка экранов настроек в
пяти кабинетах). Каждая проверяется по-своему, поэтому едут отдельно. Состав
требований не менялся.

## PR-2b «Обмен с 1С одним пунктом» — `У-113` (остаток)

- [x] Раздел [/manager/exchange](../../../src/app/manager/exchange/layout.tsx) с
      вкладками Загрузка Excel · Выписка по счёту 51 · История; компонент
      вкладок [one-c-tabs.tsx](../../../src/components/settings/one-c-tabs.tsx)
      обобщён основанием адреса и подмножеством вкладок — у менеджера нет
      «Автообмена» (расписаниями управляют админ и руководитель).
- [x] История загрузок переехала со вкладки загрузки на свою и стала общей для
      обоих каналов — двух историй на экране быть не должно.
- [x] Старые адреса `/manager/import` и `/manager/payments-import` — шлюзы на
      свои вкладки; гард роли на них остался.
- [x] Ключ раздела `exchange` в словаре со своим значком: `🔄` занят
      «Автообменом», а это другой раздел.
- [x] Реестры тем же PR: `AUDIT.md`, `STATUS.md`, `CHANGELOG.md`.

## PR-2c «личные настройки одним компонентом» — `У-114` (остаток)

- [x] Реестр вкладок [personalSettings.ts](../../../src/lib/navigation/personalSettings.ts)
      (Профиль · Команда · Уведомления · Безопасность) + разбор `?tab=`
      [personalSettingsTab.ts](../../../src/lib/navigation/personalSettingsTab.ts):
      неизвестная вкладка падает на «Профиль», а не на пустой экран.
- [x] Общий презентационный переключатель
      [personal-settings.tsx](../../../src/components/settings/personal-settings.tsx):
      названия, пояснения и порядок — из реестра, содержимое вкладки приходит
      готовым узлом от страницы своей роли (`Р-23`).
- [x] Менеджер, партнёр, заказчик переведены на него; свои вкладки партнёра
      (`partner/settings-tabs.tsx`) удалены. У заказчика реквизиты организации
      сняты с экрана — они в «Моей организации» (`У-100`).
- [x] Админ и руководитель: разделы хаба «Каналы уведомлений» и «Личная
      безопасность» слиты в один — «Личные настройки» (группа «Личное»,
      [staff-personal-settings.tsx](../../../src/components/settings/staff-personal-settings.tsx)).
      Старые адреса остались живыми и ведут на свою вкладку.
- [x] Право раздела — `'own'` («право быть собой»): разметка профиля доступа
      отнимает админские разделы, но не личные.
- [x] Данные грузятся только для открытой вкладки — открывая «Безопасность», в
      базу за телеграмом и командой не ходим.
- [x] Страж [navigation.personal-settings.guardrail](../../../src/__tests__/navigation.personal-settings.guardrail.test.ts),
      проверен четырьмя мутациями (своя навигация в кабинете, подпись вкладки
      руками, возврат прежнего раздела хаба, подмена права на админское).

## PR-3 «руководитель» — `У-110`, `У-111`, `У-118`

- [ ] «Документы» и «Сообщения» у руководителя тем же компонентом, что у
      менеджера, со скоупом компании (IDOR-проба на живом Postgres).
- [ ] Переключатель кабинетов в шапке; два пункта-моста снимаются.
- [ ] Вкладки «Обмен с 1С» у руководителя — без 404 (`Д-33`).

## PR-3 «руководитель» — `У-110`, `У-111`, `У-118`

- [x] «Документы» и «Сообщения» у руководителя — свои разделы, собранные ТЕМИ
      ЖЕ компонентами, что у менеджера
      ([staff-documents](../../../src/components/manager/staff-documents.tsx),
      [staff-document-detail](../../../src/components/documents/staff-document-detail.tsx),
      [staff-messages](../../../src/components/manager/staff-messages.tsx)).
      Раньше «Сообщения» были пунктом-мостом в чужой кабинет, а документов не
      было вовсе.
- [x] Охват — вся компания: `listDocuments`, `listManagerCounterparties` и
      `listIncomingComments` получили `teamModeOverride`, как давно был у
      `listOrders`. Личный кабинет менеджера у руководителя остаётся scoped
      («играющий тренер», §4).
- [x] Переключатель кабинетов в шапке
      ([cabinet-switcher](../../../src/components/shell/cabinet-switcher.tsx) +
      правило перехода [cabinetSwitch.ts](../../../src/lib/navigation/cabinetSwitch.ts));
      оба пункта-моста и их ключи разделов удалены.
- [x] `У-118`: корень «Обмена с 1С» у руководителя показывает навигатор задачи;
      вкладки «Автообмен» и «История» существуют (были 404 — дефект `Д-33`).
      Экран «Автообмена» общий с админом; пауза расписания, курсор и очередь
      разбора остаются админскими — они платформенные.
- [x] Стражи проверены мутацией: возврат пункта-моста, снятие `teamModeOverride`,
      выдача платформенного рычага руководителю — каждая роняет тест.

## PR-4 «администратор» — `У-112`, `У-119`

- [ ] Список заказов `/admin/orders` вместо редиректа на дашборд.
- [ ] Пункт «Поиск» админу; состав карточки заказа как у менеджера.
- [ ] `leader` в `/admin/users`: приглашение и фильтр; фильтр по компании (`Д-34`).

## PR-4 «администратор» — `У-112`, `У-119`

- [x] `/admin/orders` — список заказов всех компаний вместо редиректа на
      дашборд. Сервис [listOrdersForAdmin](../../../src/lib/services/admin/orders.ts)
      без скоупа компании (Model A), экран — на ТЕХ ЖЕ компонентах, что у
      менеджера; колонки «Компания»/«Долг» и фильтр по компании появляются
      только там, где для них есть данные.
- [x] Фильтра по организации у админа нет намеренно: организаций тысячи, список
      пришлось бы молча обрезать. Вместо него — поиск по названию и номеру.
- [x] Карточка заказа админа получила блок документов с загрузкой; номер заказа
      панель подставляет сама. Крошка ведёт в список, а не на «Главную».
- [x] «Поиск» в меню админа + страница `/admin/search`; страж палитры обновлён —
      расширение решения `У-75` записано, а не молча просочилось.
- [x] Шапка админа говорит «Кабинет администратора · e-mail» — был голый e-mail,
      единственный кабинет без ответа на вопрос «где я».
- [x] `У-119`: роль «Руководитель» в приглашении и в фильтре списка; `parseRole`
      знает `leader` (без этого фильтр молча превращался во «все роли»); фильтр
      по компании (`Д-34`).
- [x] **Найдено и закрыто по ходу:** приглашённый менеджер или руководитель
      заводился **без компании** — `session.companyId` пустой, скоупы deny-all,
      человек входил в кабинет, где ничего нет, и причину увидеть было негде.
      Теперь компания обязательна для сотрудников ЦО (`company_required`).
- [x] Стражи проверены мутациями: снятая проверка компании, пропавшая роль в
      фильтре, возврат редиректа вместо списка, `parseRole` без `leader`.

## PR-5 «PageHeader и деталки» — `У-120`, `У-116`

- [ ] Компонент `PageHeader` (заголовок из словаря + подзаголовок + главная
      кнопка + крошки); страж подзаголовков переводится на **каждый** H1.
- [ ] Деталки заявок на обучение и обращений у сотрудников ЦО.

## PR-5a «PageHeader» — `У-120`

- [x] Компонент [page-header.tsx](../../../src/components/ui/page-header.tsx):
      крошки → заголовок → подзаголовок → главная кнопка. `subtitle` —
      **обязательный** проп: экран без ответа «что здесь делают» это дефект
      приёмки (§15), а не вопрос вкуса.
- [x] Переведены **все 124 экрана** (101 страница + 23 компонента-экрана):
      своего `<h1>` в кабинетах не осталось. Заодно ушло расхождение
      начертаний (48 экранов `font-bold` против 41 `font-semibold`) и
      самодельные flex-обёртки «заголовок + кнопка».
- [x] Страж переписан с разметки на источник: любой `<h1>` мимо компонента и
      любой необъявленный `subtitle={null}` роняют тест. «Каждый заголовок с
      подзаголовком» держит теперь компилятор, а не текстовый поиск по первому
      вхождению.
- [x] Исключения — только `/(auth)/login` и `/forbidden`: экраны вне кабинетов
      со своим полноэкранным макетом; у каждого записана причина.
- [x] Стражи проверены тремя мутациями: свой `<h1>` на экране, молчаливый
      `subtitle={null}`, необязательный проп `subtitle`.

## PR-5b «деталки заявок и обращений» — `У-116`

- [x] Шесть страниц `/{admin,manager,leader}/{requests,enrollments}/[id]` на двух
      общих серверных компонентах; экран — ТОТ ЖЕ, что видит клиент.
- [x] Действия сотрудника — новый проп `actions` у обеих деталок; у клиента его
      нет, поэтому проп опциональный, а не «пустой блок у всех».
- [x] Очередь осталась списком, но название стало ссылкой в деталку
      (`cardHrefBase`); «Подробнее» по-прежнему разворачивает строку.
- [x] Скоуп режет сервис (чужая заявка → `not_found`), флаг раздела гейтит
      деталку **до** обращения к сервису — существование заявки не утекает.
- [x] Экран без доступных действий объясняет, почему их нет (§15), а не молчит.
- [x] Стражи проверены четырьмя мутациями: снятый скоуп, снятый флаг, пропавшие
      действия, строка очереди без ссылки. Четвёртая сначала НЕ ловилась —
      теста на ссылку не было, он дописан.

## PR-6a «мобильная панель» — `У-117`

- [x] `MOBILE_TABS` перешёл с адресов на `sectionKey` и стал **приоритетным**
      списком: шесть ключей вместо четырёх адресов.
- [x] Выключенный флагом пункт заменяется следующим по приоритету; если и
      запасных не хватило — панель добирается обычными пунктами меню.
      Закреплённые внизу («Настройки», «Справка») в добор не идут: их и так
      открывает вкладка «Ещё».
- [x] Закрыт `Д-40`: у менеджера «Входящие» и «Задачи» сидят под opt-in
      флагами, выключенными по умолчанию, и панель показывала **две** вкладки
      из четырёх. Есть тест на живом меню, а не только на выдуманном.
- [x] Переименование раздела панель больше не ломает — это и требуется перед
      `У-109` (порядок §5 ТЗ).
- [x] Стражи проверены четырьмя мутациями: убранный добор, список ровно из
      четырёх, поиск по адресу вместо ключа, закреплённые пункты в доборе.

## PR-6 «зеркало» — `У-109`, `У-115`, `У-117`, `У-121`

- [ ] Мобильная панель строится из реестра по ключам с запасными пунктами
      (`Д-40`) — **до** переименования партнёрского адреса.
- [ ] `/partner/deals` → `/partner/orders` отдельным PR с постоянным редиректом.
- [ ] Зеркальность заказчика и партнёра по `У-115`.
- [ ] Страж зеркальности + `mirrorExceptions.ts` со списком исключений.
