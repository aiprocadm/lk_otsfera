# UI-ремонт перед запуском — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Починить сломанный выход, привести все 5 кабинетов к единому канону навигации, словам, статусам и форматам по спеке [2026-06-11-ui-repair-design.md](../specs/2026-06-11-ui-repair-design.md).

**Architecture:** Канон меню (`cabinet.ts`) становится единственным источником для всех сайдбаров (items-prop с сервера, паттерн OrgSidebar). Новые чистые модули: `lib/format.ts` (деньги/даты), `lib/i18n/labels.ts` (enum→русский), `lib/i18n/auditActions.ts` (события админа), переписанный `lib/orders/humanStage.ts` → `orderStage()` (статус оплаты из чисел). UI-примитивы: `LogoutButton`, `BackLink`, `href` у `StatCard`.

**Tech Stack:** Next.js 15 App Router, React 19, TypeScript strict, Vitest (unit, `--mode=unit`). Прогон тестов: `npx vitest run --mode=unit <файл>`. Хук pre-commit сам гоняет lint+typecheck+test:changed.

**Ветка:** `claude/ui-repair` от свежего `main`.

**Решения пользователя (не пересматривать):** бренд «Промтехносфера» везде; «Заказы» везде (URL `/partner/deals` не трогаем); комиссию видят только партнёр/руководитель/админ — у организации блок удаляется; «Главная» вместо «Дашборд»/«Кабинет».

---

### Task 1: Форматтеры денег и дат

**Files:**
- Create: `src/lib/format.ts`
- Test: `src/__tests__/lib.format.test.ts`

- [ ] **Step 1: Написать падающий тест**

```ts
// src/__tests__/lib.format.test.ts
import { describe, it, expect } from 'vitest';
import { fmtMoney, fmtDate, fmtDateTime } from '@/lib/format';

describe('fmtMoney', () => {
  it('форматирует число с пробелами-разделителями и ₽', () => {
    expect(fmtMoney(250000)).toBe('250 000 ₽');
  });
  it('принимает строку (Decimal сериализуется строкой)', () => {
    expect(fmtMoney('100000.00')).toBe('100 000 ₽');
  });
  it('округляет копейки', () => {
    expect(fmtMoney('99.99')).toBe('100 ₽');
  });
  it('нечисло -> —', () => {
    expect(fmtMoney('abc')).toBe('—');
  });
});

describe('fmtDate', () => {
  it('dd.mm.yyyy', () => {
    expect(fmtDate(new Date('2026-04-20T17:00:00Z'))).toBe('20.04.2026');
  });
  it('невалидная дата -> —', () => {
    expect(fmtDate(new Date('invalid'))).toBe('—');
  });
});

describe('fmtDateTime', () => {
  it('dd.mm.yyyy, hh:mm', () => {
    expect(fmtDateTime(new Date(2026, 3, 20, 17, 5))).toBe('20.04.2026, 17:05');
  });
});
```

Внимание: `Intl.NumberFormat('ru-RU')` использует NBSP (` `) как разделитель — в тестах писать ` `, не пробел.

- [ ] **Step 2: Прогнать тест — убедиться, что падает**

Run: `npx vitest run --mode=unit src/__tests__/lib.format.test.ts`
Expected: FAIL — `Cannot find module '@/lib/format'`.

- [ ] **Step 3: Реализация**

```ts
// src/lib/format.ts
/**
 * Единые форматтеры пользовательских значений (₽, даты). Заменяют локальные
 * fmtMoney/таймстампы по компонентам — «250000 ₽» и смесь dd/mm/yy форматов.
 */
const MONEY = new Intl.NumberFormat('ru-RU', { maximumFractionDigits: 0 });

export function fmtMoney(value: number | string): string {
  const n = typeof value === 'number' ? value : Number(value);
  if (!Number.isFinite(n)) return '—';
  return `${MONEY.format(n)} ₽`;
}

export function fmtDate(value: Date | string): string {
  const d = typeof value === 'string' ? new Date(value) : value;
  if (Number.isNaN(d.getTime())) return '—';
  return d.toLocaleDateString('ru-RU', { day: '2-digit', month: '2-digit', year: 'numeric' });
}

export function fmtDateTime(value: Date | string): string {
  const d = typeof value === 'string' ? new Date(value) : value;
  if (Number.isNaN(d.getTime())) return '—';
  return d.toLocaleString('ru-RU', {
    day: '2-digit', month: '2-digit', year: 'numeric',
    hour: '2-digit', minute: '2-digit'
  });
}
```

Проверь ожидание теста `fmtMoney(250000)`: `MONEY.format` вернёт `250 000` с NBSP — итог `250 000 ₽`.

- [ ] **Step 4: Прогнать тест — зелёный**

Run: `npx vitest run --mode=unit src/__tests__/lib.format.test.ts`
Expected: PASS (7 тестов).

- [ ] **Step 5: Commit**

```bash
git add src/lib/format.ts src/__tests__/lib.format.test.ts
git commit -m "feat(ui): shared money/date formatters"
```

---

### Task 2: Словари enum→русский + русская ошибка входа

**Files:**
- Create: `src/lib/i18n/labels.ts`
- Modify: `src/lib/errors/messages.ts` (добавить `invalid_credentials`)
- Modify: `src/components/auth/login-form.tsx:29-31`
- Modify: `src/components/partner/deal-header.tsx:38-48` (productMix)
- Modify: `src/components/organization/org-finance-payments.tsx:44` (method)
- Modify: `src/components/manager/manager-finance-payments.tsx` (method — найти `p.method`)
- Test: `src/__tests__/lib.i18n.labels.test.ts`

Известные значения из 1С-фикстур: `productMix` ∈ {supply, service, training}, `Payment.method` ∈ {wire, card} (свободные строки — обязателен fallback «как есть»). Названия согласованы с формой заявки ([lead-create-form.tsx:9-10](../../src/components/partner/lead-create-form.tsx): service→«Услуги», supply→«Поставка»).

- [ ] **Step 1: Падающий тест**

```ts
// src/__tests__/lib.i18n.labels.test.ts
import { describe, it, expect } from 'vitest';
import { orderTypeRu, paymentMethodRu } from '@/lib/i18n/labels';

describe('orderTypeRu', () => {
  it('переводит известные коды', () => {
    expect(orderTypeRu('supply')).toBe('Поставка');
    expect(orderTypeRu('service')).toBe('Услуги');
    expect(orderTypeRu('training')).toBe('Обучение');
  });
  it('неизвестный код показывает как есть (не падает)', () => {
    expect(orderTypeRu('unknown_1c_code')).toBe('unknown_1c_code');
  });
});

describe('paymentMethodRu', () => {
  it('переводит известные коды', () => {
    expect(paymentMethodRu('wire')).toBe('Банковский перевод');
    expect(paymentMethodRu('card')).toBe('Карта');
  });
  it('null/undefined -> —', () => {
    expect(paymentMethodRu(null)).toBe('—');
    expect(paymentMethodRu(undefined)).toBe('—');
  });
  it('неизвестный код как есть', () => {
    expect(paymentMethodRu('crypto')).toBe('crypto');
  });
});
```

- [ ] **Step 2: Прогнать — FAIL** (`Cannot find module '@/lib/i18n/labels'`)

- [ ] **Step 3: Реализация**

```ts
// src/lib/i18n/labels.ts
/**
 * Словари «код 1С → русская подпись». Коды — свободные строки из 1С,
 * поэтому всегда fallback на исходный код (не падать на новом значении).
 */
const ORDER_TYPE_RU: Record<string, string> = {
  supply: 'Поставка',
  service: 'Услуги',
  training: 'Обучение'
};

export function orderTypeRu(code: string): string {
  return ORDER_TYPE_RU[code] ?? code;
}

const PAYMENT_METHOD_RU: Record<string, string> = {
  wire: 'Банковский перевод',
  card: 'Карта',
  cash: 'Наличные'
};

export function paymentMethodRu(code: string | null | undefined): string {
  if (!code) return '—';
  return PAYMENT_METHOD_RU[code] ?? code;
}
```

- [ ] **Step 4: Прогнать — PASS**

- [ ] **Step 5: Применить словари в трёх компонентах**

`src/components/partner/deal-header.tsx` — чип productMix (строка ~45): `{p}` → `{orderTypeRu(p)}`, импорт `import { orderTypeRu } from '@/lib/i18n/labels';`.

`src/components/organization/org-finance-payments.tsx:44`: `(p.method ?? '—')` → `paymentMethodRu(p.method)`, импорт `paymentMethodRu`.

`src/components/manager/manager-finance-payments.tsx`: найти `p.method` (`rg "p\.method" src/components/manager`) и заменить так же.

- [ ] **Step 6: Русская ошибка входа**

`src/lib/errors/messages.ts` — в map `RU` добавить строку:

```ts
  invalid_credentials: 'Неверный email или пароль.',
```

`src/components/auth/login-form.tsx:29-31` — заменить:

```ts
        const data = await res.json().catch(() => ({}));
        setError(data.message ?? 'Неверный email или пароль');
```

на:

```ts
        const data = await res.json().catch(() => ({} as { code?: string }));
        setError(errorMessageRu((data.code ?? '').toLowerCase(), 'Неверный email или пароль.'));
```

и импорт `import { errorMessageRu } from '@/lib/errors/messages';`. API ([login/route.ts:82](../../src/app/api/auth/login/route.ts)) возвращает `code: 'INVALID_CREDENTIALS'` — lowercase даёт ключ словаря. Сам route не трогаем (код — контракт).

- [ ] **Step 7: Прогнать смежные тесты и закоммитить**

Run: `npx vitest run --mode=unit src/__tests__/lib.errorMessages.test.ts src/__tests__/lib.i18n.labels.test.ts`
Expected: PASS.

```bash
git add src/lib/i18n/labels.ts src/lib/errors/messages.ts src/components/auth/login-form.tsx src/components/partner/deal-header.tsx src/components/organization/org-finance-payments.tsx src/components/manager/manager-finance-payments.tsx src/__tests__/lib.i18n.labels.test.ts
git commit -m "feat(ui): RU dictionaries for 1C enums + russian login error"
```

---

### Task 3: Статус заказа из чисел (`orderStage` вместо `humanStage`)

**Files:**
- Rewrite: `src/lib/orders/humanStage.ts`
- Rename/rewrite test: `src/__tests__/orders.humanStage.test.ts` → `src/__tests__/orders.orderStage.test.ts`
- Modify call sites (6): `src/lib/services/partner/deals.ts`, `src/lib/services/partner/dealDetail.ts`, `src/lib/services/organization/orders.ts`, `src/components/manager/manager-orders-table.tsx`, `src/components/manager/manager-order-header.tsx` (компонент `src/components/partner/deal-status-badge.tsx` не меняется — тип `Stage` сохраняем)

Проблема: бейдж «оплачена» берётся из 1С-статуса и противоречит цифрам («Оплачено 0 ₽ · Долг 100 000 ₽»), род женский («Завершена» — от «сделки»). Правило: **часть про оплату выводится из чисел**, исполнение — из 1С-статуса, род мужской («заказ»).

- [ ] **Step 1: Переписать тест целиком**

```bash
git mv src/__tests__/orders.humanStage.test.ts src/__tests__/orders.orderStage.test.ts
```

Новое содержимое `src/__tests__/orders.orderStage.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { orderStage } from '@/lib/orders/humanStage';

const base = { executionStatus: 'in_progress', financialStatus: 'billed', amount: '100000', paidTotal: '0' } as const;

describe('orderStage — исполнение', () => {
  it('cancelled -> Отменён (danger), оплата не показывается', () => {
    expect(orderStage({ ...base, executionStatus: 'cancelled' })).toEqual({ label: 'Отменён', tone: 'danger' });
  });
  it('on_hold -> На паузе (warning)', () => {
    expect(orderStage({ ...base, executionStatus: 'on_hold' })).toEqual({ label: 'На паузе', tone: 'warning' });
  });
});

describe('orderStage — оплата считается из чисел, не из 1С-статуса', () => {
  it('1С говорит paid, но оплачено 0 -> «не оплачен» (контр-кейс DEMO-COMM-001)', () => {
    const s = orderStage({ executionStatus: 'completed', financialStatus: 'paid', amount: '100000', paidTotal: '0' });
    expect(s.label).toBe('Завершён, счёт выставлен');
    expect(s.tone).toBe('warning');
  });
  it('полная оплата -> «оплачен», success', () => {
    const s = orderStage({ executionStatus: 'completed', financialStatus: 'paid', amount: '250000', paidTotal: '250000' });
    expect(s).toEqual({ label: 'Завершён, оплачен', tone: 'success' });
  });
  it('частичная оплата -> warning', () => {
    const s = orderStage({ executionStatus: 'in_progress', financialStatus: 'partially_paid', amount: '480000', paidTotal: '240000' });
    expect(s).toEqual({ label: 'В работе, частично оплачен', tone: 'warning' });
  });
  it('не выставлен счёт и 0 оплат -> «счёт не выставлен»', () => {
    const s = orderStage({ executionStatus: 'pending', financialStatus: 'not_billed', amount: '100', paidTotal: '0' });
    expect(s).toEqual({ label: 'Новый, счёт не выставлен', tone: 'neutral' });
  });
  it('refunded -> Возврат (danger)', () => {
    const s = orderStage({ executionStatus: 'in_progress', financialStatus: 'refunded', amount: '100', paidTotal: '100' });
    expect(s).toEqual({ label: 'Возврат', tone: 'danger' });
  });
  it('переплата (paid > amount) считается оплаченным', () => {
    const s = orderStage({ executionStatus: 'in_progress', financialStatus: 'paid', amount: '100', paidTotal: '150' });
    expect(s).toEqual({ label: 'В работе, оплачен', tone: 'success' });
  });
});
```

- [ ] **Step 2: Прогнать — FAIL** (`orderStage is not a function`)

- [ ] **Step 3: Переписать `src/lib/orders/humanStage.ts`**

```ts
import type { ExecutionStatus, FinancialStatus } from '@prisma/client';

export type StageTone = 'neutral' | 'success' | 'warning' | 'danger';

export type Stage = {
  label: string;
  tone: StageTone;
};

export type OrderStageInput = {
  executionStatus: ExecutionStatus;
  financialStatus: FinancialStatus;
  /** Decimal сериализуется строкой; принимаем и number. */
  amount: string | number;
  paidTotal: string | number;
};

const EXEC_RU: Record<string, string> = {
  pending: 'Новый',
  in_progress: 'В работе',
  completed: 'Завершён'
};

/**
 * Человеческий статус заказа. Исполнение — из 1С-статуса; оплата — ИЗ ЧИСЕЛ
 * (1С-статус оплаты бывает рассинхронизирован с платежами и подрывает доверие:
 * «оплачен» рядом с «Долг 100 000 ₽»). financialStatus используется только для
 * различения «счёт выставлен / не выставлен», когда оплат ещё нет.
 * Род мужской — «заказ» (канон терминологии: «Заказы» во всех кабинетах).
 */
export function orderStage(input: OrderStageInput): Stage {
  const { executionStatus, financialStatus } = input;

  if (executionStatus === 'cancelled') return { label: 'Отменён', tone: 'danger' };
  if (executionStatus === 'on_hold') return { label: 'На паузе', tone: 'warning' };
  if (financialStatus === 'refunded') return { label: 'Возврат', tone: 'danger' };

  const exec = EXEC_RU[executionStatus] ?? '—';
  const amount = Number(input.amount);
  const paid = Number(input.paidTotal);

  let pay: string;
  let tone: StageTone;
  if (Number.isFinite(amount) && Number.isFinite(paid) && amount > 0 && paid >= amount) {
    pay = 'оплачен';
    tone = 'success';
  } else if (Number.isFinite(paid) && paid > 0) {
    pay = 'частично оплачен';
    tone = 'warning';
  } else if (financialStatus === 'billed' || financialStatus === 'paid' || financialStatus === 'partially_paid') {
    // 1С считает, что счёт был выставлен (или даже оплачен) — но платежей нет.
    pay = 'счёт выставлен';
    tone = executionStatus === 'completed' ? 'warning' : 'neutral';
  } else {
    pay = 'счёт не выставлен';
    tone = executionStatus === 'completed' ? 'warning' : 'neutral';
  }

  return { label: `${exec}, ${pay}`, tone };
}
```

Старую функцию `humanStage` удалить. Тип `Stage` остаётся — его импортирует `deal-status-badge.tsx`.

- [ ] **Step 4: Прогнать — PASS**

Run: `npx vitest run --mode=unit src/__tests__/orders.orderStage.test.ts`

- [ ] **Step 5: Обновить вызывающие места**

Найти все: `rg "humanStage" src --files-with-matches`. В каждом сервисе заменить `humanStage({ executionStatus: ..., financialStatus: ... })` на `orderStage({ executionStatus: ..., financialStatus: ..., amount: <поле суммы>, paidTotal: <поле оплачено> })` — суммы уже выбираются рядом (эти страницы показывают «Сумма»/«Оплачено»/«Долг»; если select узкий — добавить поля `amount`, `paidTotal` в select). Компоненты `manager-orders-table.tsx` / `manager-order-header.tsx` получают строки из сервисов — у них заменить вызов и прокинуть оба числа.

- [ ] **Step 6: typecheck + затронутые тесты**

Run: `npm run typecheck && npx vitest run --mode=unit src/__tests__/orders.orderStage.test.ts src/__tests__/services.partner.portfolio.test.ts`
Expected: PASS. Сломавшиеся ожидания старых женских строк («Завершена, оплачена») в любых тестах поправить на новые мужские.

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "feat(orders): payment part of order stage derived from numbers, masculine RU labels"
```

---

### Task 4: LogoutButton — выход без JSON-тупика

**Files:**
- Create: `src/components/ui/logout-button.tsx`
- Modify: `src/components/ui/index.ts` (barrel)
- Modify: `src/components/dashboard/app-shell.tsx:32-39`, `src/components/admin/admin-app-shell.tsx:14-21`, `src/components/manager/manager-app-shell.tsx:21-28`, `src/components/organization/org-app-shell.tsx:31-38`
- Test: `src/__tests__/components.ui-logout-button.test.tsx`

Сейчас все 4 шелла рендерят `<form action='/api/auth/logout' method='post'>` — браузер уходит на страницу `{"ok":true}`.

- [ ] **Step 1: Падающий тест**

```tsx
// src/__tests__/components.ui-logout-button.test.tsx
import { describe, it, expect, vi, beforeEach } from 'vitest';
import React from 'react';
import { renderToString } from 'react-dom/server';

const { push, refresh } = vi.hoisted(() => ({ push: vi.fn(), refresh: vi.fn() }));
vi.mock('next/navigation', () => ({ useRouter: () => ({ push, refresh }) }));

import { LogoutButton } from '@/components/ui/logout-button';

describe('LogoutButton', () => {
  beforeEach(() => {
    push.mockClear();
    refresh.mockClear();
  });

  it('рендерит кнопку с единым текстом «Выйти» (не «Выход»)', () => {
    const html = renderToString(React.createElement(LogoutButton));
    expect(html).toContain('Выйти');
    expect(html).not.toContain('Выход<');
    expect(html).toContain('type="button"');
  });

  it('принимает className для тёмной шапки партнёра', () => {
    const html = renderToString(React.createElement(LogoutButton, { className: 'text-gray-400' }));
    expect(html).toContain('text-gray-400');
  });
});
```

(Поведение click с fetch в unit-слое без DOM не проверяем — renderToString; редирект ловится ручным smoke в Task 15.)

- [ ] **Step 2: Прогнать — FAIL** (`Cannot find module '@/components/ui/logout-button'`)

- [ ] **Step 3: Реализация**

```tsx
// src/components/ui/logout-button.tsx
'use client';

import React, { useState } from 'react';
import { useRouter } from 'next/navigation';

/**
 * Единая кнопка выхода. Раньше шеллы отправляли <form> прямо на
 * /api/auth/logout — браузер приземлялся на голый JSON {"ok":true}.
 * Здесь: POST по fetch -> явный уход на /login.
 */
export function LogoutButton({ className }: { className?: string }) {
  const router = useRouter();
  const [busy, setBusy] = useState(false);

  async function onClick() {
    setBusy(true);
    try {
      await fetch('/api/auth/logout', { method: 'POST' });
    } catch {
      // даже при сетевой ошибке уходим на /login — middleware разберётся
    }
    router.push('/login');
    router.refresh();
  }

  return (
    <button
      type='button'
      onClick={onClick}
      disabled={busy}
      className={className ?? 'text-sm text-gray-600 hover:text-[#F97316] transition-colors disabled:opacity-60'}
    >
      {busy ? 'Выходим…' : 'Выйти'}
    </button>
  );
}
```

В `src/components/ui/index.ts` добавить: `export { LogoutButton } from './logout-button';`

- [ ] **Step 4: Прогнать — PASS**

- [ ] **Step 5: Заменить формы в 4 шеллах**

В каждом шелле удалить блок `<form action='/api/auth/logout' ...>...</form>` и поставить:

`admin-app-shell.tsx`, `manager-app-shell.tsx`, `org-app-shell.tsx` (светлая шапка):
```tsx
<LogoutButton />
```

`dashboard/app-shell.tsx` (тёмная шапка партнёра, сохранить стиль рамки):
```tsx
<LogoutButton className='text-xs text-gray-400 hover:text-[#F97316] transition-colors px-2 py-1 border border-gray-700 rounded hover:border-[#F97316] disabled:opacity-60' />
```

Импорт во всех: `import { LogoutButton } from '@/components/ui';`

- [ ] **Step 6: Проверить, что старых форм не осталось**

Run: `rg "api/auth/logout" src/components`
Expected: только `logout-button.tsx`.

Run: `npm run typecheck`
Expected: clean.

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "fix(ui): logout via shared LogoutButton -> /login instead of raw JSON page"
```

---

### Task 5: Канон навигации для всех кабинетов

**Files:**
- Modify: `src/lib/navigation/cabinet.ts`
- Modify: `src/components/admin/admin-sidebar.tsx` (items-prop, удалить GROUPS)
- Modify: `src/components/admin/admin-app-shell.tsx` (передать items)
- Modify: `src/components/manager/manager-sidebar.tsx` (items-prop, удалить ITEMS)
- Modify: `src/components/manager/manager-app-shell.tsx` (передать items)
- Modify: `src/components/partner/bottom-tab-bar.tsx` (метки)
- Tests: `src/__tests__/navigation.cabinet.partner.test.ts`, `src/__tests__/components.admin-sidebar.test.tsx`, `src/__tests__/components.manager-sidebar.test.tsx`

Сейчас: канон используют только partner и organization; `AdminSidebar`/`ManagerSidebar` хардкодят свои списки, из-за чего у админа недостижимы 4 страницы (`/admin/orders|documents|messages|finance`), а `navByRole.admin` — мёртвые английские данные. У менеджера «Финансы» есть в каноне, но нет в сайдбаре; «Команда» руководителя не видна вообще.

- [ ] **Step 1: Обновить канон `cabinet.ts`**

В тип `NavItem` добавить поле:

```ts
  /** Секция админского сайдбара («Платформа» / «Операции» / «Справочники»). Прочие шеллы игнорируют. */
  group?: string;
```

`navByRole.admin` заменить целиком:

```ts
  admin: [
    { href: '/admin/dashboard', label: 'Главная', icon: '⌂', group: 'Платформа' },
    { href: '/admin/health', label: 'Здоровье', icon: '💚', group: 'Платформа' },
    { href: '/admin/sync', label: 'Синхронизация', icon: '🔄', group: 'Платформа' },
    { href: '/admin/orders', label: 'Заказы', icon: '📋', group: 'Операции' },
    { href: '/admin/documents', label: 'Документы', icon: '📄', group: 'Операции' },
    { href: '/admin/messages', label: 'Сообщения', icon: '💬', group: 'Операции' },
    { href: '/admin/commission-statements', label: 'Комиссии', icon: '💰', group: 'Операции' },
    { href: '/admin/finance', label: 'Финансы', icon: '₽', group: 'Операции' },
    { href: '/admin/import', label: 'Загрузка из 1С', icon: '📥', group: 'Операции' },
    { href: '/admin/audit', label: 'Аудит', icon: '🧾', group: 'Операции' },
    { href: '/admin/users', label: 'Пользователи', icon: '👤', group: 'Справочники' },
    { href: '/admin/partners', label: 'Партнёры', icon: '🏢', group: 'Справочники' },
    { href: '/admin/organizations', label: 'Организации', icon: '🏛', group: 'Справочники' }
  ],
```

`navByRole.manager` — добавить иконки (берём из текущего manager-sidebar) и оставить состав как есть:

```ts
  manager: [
    { href: '/manager/dashboard', label: 'Главная', icon: '⌂', flag: 'manager_cabinet' },
    { href: '/manager/orders', label: 'Заказы', icon: '📋', flag: 'manager_cabinet' },
    { href: '/manager/organizations', label: 'Организации', icon: '🏢', flag: 'manager_cabinet' },
    { href: '/manager/finance', label: 'Финансы', icon: '₽', flag: 'manager_cabinet' },
    { href: '/manager/import', label: 'Загрузка из 1С', icon: '📥', flag: 'manager_cabinet' },
    { href: '/manager/documents', label: 'Документы', icon: '📄', flag: 'manager_cabinet' },
    { href: '/manager/students', label: 'Сотрудники', icon: '👥', flag: 'manager_cabinet' },
    { href: '/manager/messages', label: 'Сообщения', icon: '💬', flag: 'manager_cabinet' },
    { href: '/manager/team', label: 'Команда', icon: '⚙', flag: 'manager_cabinet', leaderOnly: true }
  ],
```

`navByRole.partner` — переименовать два пункта (только label): `'Дашборд'` → `'Главная'`, `'Сделки'` → `'Заказы'`. `navByRole.organization` — `'Главная'` уже верно, не трогаем.

- [ ] **Step 2: Обновить тесты канона (упадут — это ожидаемо)**

`src/__tests__/navigation.cabinet.partner.test.ts`: во всех `arrayContaining` заменить `'Дашборд'`→`'Главная'`, `'Сделки'`→`'Заказы'`. Добавить новый describe в конец файла:

```ts
describe('navByRole.admin — русский канон с группами (все 13 страниц)', () => {
  it('содержит все админские страницы, включая ранее потерянные orders/documents/messages/finance', () => {
    const hrefs = navByRole.admin.map((i) => i.href);
    for (const lost of ['/admin/orders', '/admin/documents', '/admin/messages', '/admin/finance']) {
      expect(hrefs).toContain(lost);
    }
    expect(navByRole.admin).toHaveLength(13);
  });
  it('каждый пункт по-русски, с иконкой и группой', () => {
    for (const item of navByRole.admin) {
      expect(item.label).toMatch(/[А-Яа-яЁё]/);
      expect(item.icon).toBeTruthy();
      expect(['Платформа', 'Операции', 'Справочники']).toContain(item.group);
    }
  });
});
```

Run: `npx vitest run --mode=unit src/__tests__/navigation.cabinet.partner.test.ts`
Expected: PASS после правок Step 1.

- [ ] **Step 3: AdminSidebar на items-prop**

`src/components/admin/admin-sidebar.tsx` заменить целиком:

```tsx
'use client';

import React from 'react';
import Link from 'next/link';
import { usePathname } from 'next/navigation';
import type { NavItem } from '@/lib/navigation/cabinet';

/**
 * Чисто презентационный сайдбар: пункты приходят с сервера (AdminAppShell ->
 * navItemsFor('admin')). Группировка — по item.group в порядке появления.
 * Единый источник меню — src/lib/navigation/cabinet.ts (не хардкодить списки).
 */
export function AdminSidebar({ items }: { items: NavItem[] }) {
  const pathname = usePathname();

  const groups: Array<{ title: string; items: NavItem[] }> = [];
  for (const item of items) {
    const title = item.group ?? '';
    const last = groups[groups.length - 1];
    if (last && last.title === title) last.items.push(item);
    else groups.push({ title, items: [item] });
  }

  return (
    <nav className="w-60 min-h-screen bg-white border-r border-gray-200 p-4 flex-shrink-0">
      <div className="text-lg font-bold text-[#111111] mb-6 px-2">Админ</div>
      {groups.map((group) => (
        <div key={group.title} className="mb-6">
          {group.title && (
            <div className="text-xs font-medium uppercase tracking-wider text-gray-500 px-2 mb-2">
              {group.title}
            </div>
          )}
          <ul className="space-y-0.5">
            {group.items.map((item) => {
              const isActive = pathname === item.href || pathname?.startsWith(item.href + '/');
              return (
                <li key={item.href}>
                  <Link
                    href={item.href}
                    className={`flex items-center gap-2 px-2 py-1.5 rounded text-sm transition-colors ${
                      isActive
                        ? 'bg-[#F97316] text-white font-medium'
                        : 'text-gray-700 hover:bg-gray-100'
                    }`}
                    data-active={isActive ? 'true' : 'false'}
                  >
                    <span className="text-base">{item.icon}</span>
                    <span>{item.label}</span>
                  </Link>
                </li>
              );
            })}
          </ul>
        </div>
      ))}
    </nav>
  );
}
```

Примечание: сохранить текущую разметку рендера групп из старого файла, если она богаче (важно поведение: группы из канона, не из локального GROUPS).

`src/components/admin/admin-app-shell.tsx`: добавить импорт `import { navItemsFor } from '@/lib/navigation/cabinet';` и заменить `<AdminSidebar />` на `<AdminSidebar items={navItemsFor('admin')} />`.

- [ ] **Step 4: ManagerSidebar на items-prop (+ видимая «Команда» у руководителя)**

`src/components/manager/manager-sidebar.tsx`: удалить локальный `const ITEMS` и тип `NavItem`; принять props:

```tsx
import type { NavItem } from '@/lib/navigation/cabinet';

export function ManagerSidebar({ items }: { items: NavItem[] }) {
```

В JSX заменить `ITEMS.map` на `items.map`.

`src/components/manager/manager-app-shell.tsx`:

```tsx
import { navItemsFor } from '@/lib/navigation/cabinet';
import { isManagerLeader } from '@/lib/auth/managerPolicy';
```

и заменить `<ManagerSidebar />` на:

```tsx
<ManagerSidebar
  items={navItemsFor('manager', { isManagerLeader: isManagerLeader(props.session) })}
/>
```

Это автоматически добавляет менеджеру «Финансы», а руководителю — «Команда» (исправление «невидимой роли»).

- [ ] **Step 5: BottomTabBar — метки мобильного меню**

`src/components/partner/bottom-tab-bar.tsx:8-9`: `label: 'Кабинет'` → `label: 'Главная'`; `label: 'Сделки'` → `label: 'Заказы'`.

- [ ] **Step 6: Обновить тесты сайдбаров**

`src/__tests__/components.admin-sidebar.test.tsx`: рендерить с items из канона —

```tsx
import { navByRole } from '@/lib/navigation/cabinet';
// ...
const html = renderToString(React.createElement(AdminSidebar, { items: navByRole.admin }));
```

`EXPECTED_HREFS` дополнить `'/admin/orders', '/admin/documents', '/admin/messages', '/admin/finance'`; «exactly 9 nav links» → 13; тест про группы оставить (заголовки те же).

`src/__tests__/components.manager-sidebar.test.tsx`: аналогично — `items: navByRole.manager` (без leaderOnly-фильтра в raw-каноне «Команда» присутствует; для проверки фильтра использовать `navItemsFor` с `FEATURE_MANAGER_CABINET='1'` в env теста). Добавить кейс:

```tsx
it('у рядового менеджера нет «Команда», у руководителя — есть', () => {
  process.env.FEATURE_MANAGER_CABINET = '1';
  const plain = navItemsFor('manager').map((i) => i.label);
  const leader = navItemsFor('manager', { isManagerLeader: true }).map((i) => i.label);
  expect(plain).not.toContain('Команда');
  expect(leader).toContain('Команда');
  expect(plain).toContain('Финансы');
});
```

- [ ] **Step 7: Прогон + commit**

Run: `npm run typecheck && npx vitest run --mode=unit src/__tests__/navigation.cabinet.partner.test.ts src/__tests__/components.admin-sidebar.test.tsx src/__tests__/components.manager-sidebar.test.tsx`
Expected: PASS.

```bash
git add -A
git commit -m "feat(nav): single nav canon for all cabinets; restore lost admin/manager menu items"
```

---

### Task 6: Убрать комиссию из кабинета организации

**Files:**
- Modify: `src/app/organization/finance/page.tsx`
- Modify: `src/components/organization/org-finance-commission.tsx` (только комментарий)
- Tests: найти затронутые `rg "Комиссия посредника|getOrgIntermediaryCommission" src/__tests__ --files-with-matches`

Правило пользователя: комиссию видят только партнёр (свою), руководитель менеджеров и админ. Организация (включая org-admin и org-leader) — больше нет.

- [ ] **Step 1: Упростить страницу**

`src/app/organization/finance/page.tsx`: удалить импорты `OrgFinanceCommission` и `getOrgIntermediaryCommission`; удалить `canSeeCommission` и третий элемент `Promise.all`; удалить `{commission && <OrgFinanceCommission data={commission} />}`. Итоговое тело:

```tsx
  const sp = await searchParams;
  const ctx = await getOrgPageContext(sp);

  const [kpis, payments] = await Promise.all([
    getOrgFinanceKpis(prisma, ctx.activeOrgId),
    listOrgPayments(prisma, { organizationId: ctx.activeOrgId })
  ]);
```

(JSX: остаются `OrgFinanceKpisGrid` и `OrgFinancePayments`.)

- [ ] **Step 2: Комментарий-предохранитель в компоненте**

В начало `src/components/organization/org-finance-commission.tsx` добавить doc-комментарий:

```tsx
/**
 * Блок «Комиссия посредника». С 2026-06 НЕ показывается в кабинете организации
 * (правило видимости: комиссию видят только партнёр/руководитель/админ — см.
 * spec 2026-06-11-ui-repair §4). Компонент жив, потому что его рендерит
 * менеджерско-админская витрина (manager-finance-view).
 */
```

- [ ] **Step 3: Обновить тесты**

`rg "getOrgIntermediaryCommission|Комиссия посредника" src/__tests__ --files-with-matches` — в тестах страницы org-finance убрать ожидания блока комиссии для admin/leader (сервисные тесты `getOrgIntermediaryCommission` НЕ удалять — функция используется manager/admin-витриной). Если есть тест «leader видит комиссию» на org-странице — инвертировать в «никто из org-ролей не видит комиссию».

Закрепить вторую половину правила (§4 спеки): в `src/__tests__/components.manager-finance.test.tsx` убедиться/добавить кейс — при `commission: null` в данных секции рядового менеджера рендер `ManagerFinanceView` **не содержит** строки «Комиссия» (блок появляется только когда сервис отдал commission, т.е. для руководителя/админа):

```tsx
it('рядовой менеджер (commission=null) не видит блок комиссии', () => {
  const html = renderToString(
    React.createElement(ManagerFinanceView, {
      data: {
        summary: { billed: '100', paid: '0', outstanding: '100' },
        sections: [{ orgId: 'o1', orgName: 'Орг', kpis: { billed: '100', paid: '0', outstanding: '100' }, payments: [], commission: null }],
        canSeeCommission: false
      }
    })
  );
  expect(html).not.toContain('Комиссия');
});
```

- [ ] **Step 4: Прогон + commit**

Run: `npm run typecheck && npx vitest run --mode=unit src/__tests__/components.manager-finance.test.tsx`
Expected: PASS (менеджерская витрина не задета).

```bash
git add -A
git commit -m "feat(finance): remove intermediary commission block from organization cabinet"
```

---

### Task 7: «Сделки» → «Заказы» во всех надписях партнёра

**Files:**
- Modify: всё из `rg "[Сс]делк" src --files-with-matches -g '!__tests__'` (на момент написания: `components/partner/{deals-table,portfolio-card-list,kpi-grid,attention-list,events-feed,documents-list}.tsx`, `app/partner/deals/page.tsx`, `app/partner/deals/[id]/page.tsx`, `app/partner/leads/[id]/page.tsx`, `app/manager/documents/page.tsx`, `app/admin/documents/page.tsx`, `lib/email/templates/{lead-promoted,document-uploaded}.tsx`; cabinet.ts и bottom-tab-bar уже сделаны в Task 5)
- Tests: обновить ожидания в `rg "[Сс]делк" src/__tests__ --files-with-matches`

- [ ] **Step 1: Замена по морфологической таблице**

Только русские надписи; **код не трогаем** (идентификаторы `deals`, `dealDetail`, маршруты `/partner/deals`, типы `DealDetail` остаются английскими):

| Было | Стало |
|---|---|
| Сделка / сделка | Заказ / заказ |
| Сделки / сделки | Заказы / заказы |
| сделок | заказов |
| сделку | заказ |
| сделке | заказу/заказе (по падежу: «по сделке»→«по заказу», «в сделке»→«в заказе») |
| сделкой | заказом |
| Открытые сделки | Открытые заказы |
| ← Все сделки | ← Все заказы |
| стала сделкой / стал(а) заказом | стал заказом |

Род прилагательных согласовать: «новая сделка» → «новый заказ», «эта сделка» → «этот заказ».

- [ ] **Step 2: Счётчики-плюрализации**

В `app/partner/deals/page.tsx` подзаголовок-счётчик («3 сделки») перевести на формы заказа: 1 заказ / 2-4 заказа / 5+ заказов. Если там используется функция плюрализации — поправить словоформы в ней.

- [ ] **Step 3: Проверка полноты**

Run: `rg "[Сс]делк" src -g '!__tests__'`
Expected: 0 совпадений (если что-то осталось — это пропуск, доправить).

- [ ] **Step 4: Обновить тесты и прогнать**

В тестах (`navigation.cabinet.partner.test.ts` уже сделан; `services.partner.dashboard.events.test.ts`, `email.send.test.ts` и др.) заменить ожидаемые строки по той же таблице.

Run: `npm run typecheck && npm run test:unit`
Expected: PASS (полный unit-слой, потому что правка размазана).

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "feat(terms): partner UI says «Заказы» everywhere (canon term, labels only)"
```

---

### Task 8: Бренд «Промтехносфера» везде

**Files:**
- Modify: `src/components/dashboard/app-shell.tsx:24-25,71`, `src/app/(auth)/login/page.tsx:29-30,39,47-48`, `src/app/layout.tsx:20`, `src/lib/services/commission/xlsx.ts:34`, `src/lib/services/commission/pdf.ts:136`
- Tests: `rg "ОТСФЕРА" src/__tests__ --files-with-matches`

- [ ] **Step 1: Замены**

Везде: логотип-буква `О` → `П`; текст `ОТСФЕРА` → `Промтехносфера` (класс `uppercase` у логотипа убрать, чтобы не получить «ПРОМТЕХНОСФЕРА» там, где задумано смешанное начертание — заголовок шапки оставить как `font-semibold tracking-wide text-sm`).

- `app-shell.tsx:24`: `>О<` → `>П<`; `:25`: `ОТСФЕРА` → `Промтехносфера`, удалить `uppercase` из className.
- `app-shell.tsx:71`: `ОТСФЕРА &copy; 2024` → `Промтехносфера &copy; 2026`.
- `login/page.tsx:29-30` и `:47-48`: буква и название аналогично; `:39`: `© 2024 ОТСФЕРА. Все права защищены.` → `© 2026 Промтехносфера. Все права защищены.`
- `layout.tsx:20` (PWA `appleWebApp.title` или аналог): `'ОТСФЕРА'` → `'Промтехносфера'`.
- `xlsx.ts:34`: `wb.creator = 'Промтехносфера';`
- `pdf.ts:136`: `'Сформировано в кабинете ОТСФЕРА'` → `'Сформировано в личном кабинете «Промтехносфера»'`.

- [ ] **Step 2: Проверка полноты + тесты**

Run: `rg "ОТСФЕРА" src`
Expected: 0 совпадений. Поправить ожидания в тестах, если ссылались (`rg "ОТСФЕРА" src/__tests__`).

Run: `npm run typecheck && npx vitest run --mode=unit src/__tests__/services.commission.pdf.test.ts src/__tests__/services.commission.xlsx.test.ts`
Expected: PASS.

- [ ] **Step 3: Commit**

```bash
git add -A
git commit -m "feat(brand): unify product name to «Промтехносфера»"
```

---

### Task 9: Человеческая лента событий админа

**Files:**
- Create: `src/lib/i18n/auditActions.ts`
- Modify: `src/app/admin/dashboard/page.tsx:68-78`
- Test: `src/__tests__/lib.i18n.auditActions.test.ts`

Сейчас: `Admin — commission_statement_approved (commission_statement #cmpsftsme0016…)`.

- [ ] **Step 1: Падающий тест**

```ts
// src/__tests__/lib.i18n.auditActions.test.ts
import { describe, it, expect } from 'vitest';
import { auditActionRu } from '@/lib/i18n/auditActions';

describe('auditActionRu', () => {
  it('переводит все TRACKED_ACTIONS админ-дашборда', () => {
    expect(auditActionRu('commission_statement_approved')).toBe('утвердил отчёт по комиссии');
    expect(auditActionRu('lead_created')).toBe('создал заявку');
    expect(auditActionRu('user_role_changed')).toBe('изменил роль пользователя');
    expect(auditActionRu('partner_created')).toBe('создал партнёра');
  });
  it('неизвестный код — подчёркивания в пробелы, не падает', () => {
    expect(auditActionRu('some_new_action')).toBe('some new action');
  });
});
```

- [ ] **Step 2: Прогнать — FAIL**

- [ ] **Step 3: Реализация**

```ts
// src/lib/i18n/auditActions.ts
/**
 * action-коды AuditLog → русская фраза для ленты «Последние события»
 * (admin-дашборд). Формат — глагольная фраза от лица актора:
 * «<Имя> утвердил отчёт по комиссии». Покрывает TRACKED_ACTIONS из
 * services/admin/dashboard.ts; новый код без перевода деградирует
 * в читаемый fallback (подчёркивания -> пробелы), не в cuid-кашу.
 */
const ACTION_RU: Record<string, string> = {
  commission_statement_calculated: 'рассчитал отчёт по комиссии',
  commission_statement_approved: 'утвердил отчёт по комиссии',
  commission_statement_paid: 'отметил отчёт по комиссии оплаченным',
  partner_commission_rate_changed: 'изменил ставку партнёра',
  lead_created: 'создал заявку',
  lead_withdrawn: 'отозвал заявку',
  partner_member_invited: 'пригласил сотрудника партнёра',
  partner_member_deactivated: 'деактивировал сотрудника партнёра',
  partner_member_scope_changed: 'изменил зону видимости сотрудника партнёра',
  partner_created: 'создал партнёра',
  partner_updated: 'обновил партнёра',
  partner_deactivated: 'деактивировал партнёра',
  user_role_changed: 'изменил роль пользователя',
  org_rate_override: 'назначил индивидуальную ставку организации',
  organization_rate_override: 'назначил индивидуальную ставку организации',
  lead_promoted: 'перевёл заявку в заказ',
  lead_promoted_to_order: 'перевёл заявку в заказ'
};

export function auditActionRu(action: string): string {
  return ACTION_RU[action] ?? action.replaceAll('_', ' ');
}
```

- [ ] **Step 4: Прогнать — PASS**

- [ ] **Step 5: Применить в админ-дашборде**

`src/app/admin/dashboard/page.tsx` — импорт `import { auditActionRu } from '@/lib/i18n/auditActions';`; внутри Events Feed (строки ~74-77) заменить

```tsx
<span className="font-medium">{e.actor}</span>
{' — '}
{e.verb}
{e.entity ? ` (${e.entity}` + (e.entityRef ? ` #${e.entityRef}` : '') + ')' : ''}
```

на

```tsx
<span className="font-medium">{e.actor}</span>{' '}
{auditActionRu(e.verb)}
```

(cuid-хвосты и сырые имена сущностей убраны; клик по строке по-прежнему ведёт в `/admin/audit` с фильтром — там полная техническая детализация.)

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "feat(admin): humanize dashboard event feed (no raw action codes / cuids)"
```

---

### Task 10: Кликабельные KPI-плитки

**Files:**
- Modify: `src/components/dashboard/stat-card.tsx`
- Modify: `src/components/partner/kpi-grid.tsx`
- Modify: дашборды — `src/app/admin/dashboard/page.tsx` (+`src/lib/services/admin/dashboard.ts` тип KpiTile), `src/app/manager/dashboard/page.tsx`, org-дашборд (`rg "<StatCard|KpisGrid" src/app/organization src/components/organization`)
- Test: `src/__tests__/components.stat-card.test.tsx`

- [ ] **Step 1: Падающий тест**

```tsx
// src/__tests__/components.stat-card.test.tsx
import { describe, it, expect, vi } from 'vitest';
import React from 'react';
import { renderToString } from 'react-dom/server';

vi.mock('next/link', () => ({
  default: ({ href, children, className }: { href: string; children: React.ReactNode; className?: string }) =>
    React.createElement('a', { href, className }, children)
}));

import { StatCard } from '@/components/dashboard/stat-card';

describe('StatCard', () => {
  it('без href рендерится как div (не ссылка)', () => {
    const html = renderToString(React.createElement(StatCard, { title: 'Заказы', value: 5 }));
    expect(html).not.toContain('<a ');
  });
  it('с href вся плитка — ссылка', () => {
    const html = renderToString(React.createElement(StatCard, { title: 'Заказы', value: 5, href: '/manager/orders' }));
    expect(html).toContain('href="/manager/orders"');
    expect(html).toContain('Заказы');
  });
});
```

- [ ] **Step 2: Прогнать — FAIL** (нет prop href)

- [ ] **Step 3: Реализация**

`src/components/dashboard/stat-card.tsx` заменить целиком:

```tsx
import React from 'react';
import Link from 'next/link';

export function StatCard({
  title,
  value,
  accent,
  href
}: {
  title: string;
  value: number | string;
  accent?: boolean;
  href?: string;
}) {
  const card = (
    <div className={`rounded-xl border p-5 flex flex-col gap-2 shadow-sm transition-shadow hover:shadow-md ${accent ? 'bg-[#F97316] border-[#EA580C] text-white' : 'bg-white border-gray-200'}`}>
      <div className={`text-xs font-medium uppercase tracking-wider ${accent ? 'text-orange-100' : 'text-gray-500'}`}>
        {title}
      </div>
      <div className={`text-3xl font-bold ${accent ? 'text-white' : 'text-[#111111]'}`}>
        {value}
      </div>
    </div>
  );
  if (!href) return card;
  return (
    <Link href={href} className='block focus-visible:outline-2 focus-visible:outline-[#F97316] rounded-xl'>
      {card}
    </Link>
  );
}
```

- [ ] **Step 4: Прогнать — PASS**

- [ ] **Step 5: Прокинуть href на дашбордах**

Карта адресов (плитка → список, который она считает):

- **Партнёр** `kpi-grid.tsx`: Открытые заказы→`/partner/deals`, К оплате→`/partner/finance`, Заявки в работе→`/partner/leads`, Комиссия за месяц→`/partner/finance`. Заодно: локальный `fmtMoney` удалить, импортировать из `@/lib/format`.
- **Админ** — тип `KpiTile` в `src/lib/services/admin/dashboard.ts` дополнить `href: string;` и проставить в тайлах: партнёры→`/admin/partners`, организации→`/admin/organizations`, закрытые заказы→`/admin/orders`, к выплате→`/admin/commission-statements`. Страница: `<StatCard key={tile.label} title={tile.label} value={tile.value} href={tile.href} />`. (Сервис уже отдаёт href в attention-items — прецедент есть.)
- **Менеджер** `src/app/manager/dashboard/page.tsx`: активные заказы→`/manager/orders`, требует внимания→`/manager/orders`, непрочитанные комментарии→`/manager/messages`, срочные дедлайны→`/manager/orders`.
- **Организация**: найти KPI-компонент (`rg "АКТИВНЫХ ЗАКАЗОВ" src`), проставить: заказы→`/organization/orders`, к оплате→`/organization/finance`, сотрудники на курсе→`/organization/students`, документы→`/organization/documents`.

- [ ] **Step 6: Прогон + commit**

Run: `npm run typecheck && npx vitest run --mode=unit src/__tests__/components.stat-card.test.tsx src/__tests__/services.admin.dashboard.test.ts`
Expected: PASS (тест admin dashboard поправить, если проверяет форму KpiTile).

```bash
git add -A
git commit -m "feat(dashboards): KPI tiles link to their lists"
```

---

### Task 11: BackLink, пустые состояния, кавычки

**Files:**
- Create: `src/components/ui/back-link.tsx` (+barrel `src/components/ui/index.ts`)
- Modify: детальные страницы с «← » (найти: `rg "← " src/app src/components -l`)
- Modify: `src/app/admin/organizations/page.tsx` (пустая таблица), компонент чата с «Нет переписок» (`rg "Нет переписок" src -l`), `src/components/manager/manager-finance-view.tsx:16`
- Modify: подзаголовки с двойными кавычками (`rg "«\{" src/app src/components -l`)
- Test: `src/__tests__/components.ui-back-link.test.tsx`

- [ ] **Step 1: Тест + компонент BackLink**

```tsx
// src/__tests__/components.ui-back-link.test.tsx
import { describe, it, expect, vi } from 'vitest';
import React from 'react';
import { renderToString } from 'react-dom/server';

vi.mock('next/link', () => ({
  default: ({ href, children, className }: { href: string; children: React.ReactNode; className?: string }) =>
    React.createElement('a', { href, className }, children)
}));

import { BackLink } from '@/components/ui/back-link';

describe('BackLink', () => {
  it('рендерит «← <label>» со ссылкой', () => {
    const html = renderToString(React.createElement(BackLink, { href: '/partner/deals', label: 'Все заказы' }));
    expect(html).toContain('href="/partner/deals"');
    expect(html).toContain('← Все заказы');
  });
});
```

```tsx
// src/components/ui/back-link.tsx
import React from 'react';
import Link from 'next/link';

/** Единая «назад»-ссылка детальных страниц. Текст — «Все <раздел во мн.ч.>». */
export function BackLink({ href, label }: { href: string; label: string }) {
  return (
    <Link href={href} className='inline-block text-sm text-gray-500 hover:text-[#F97316] transition-colors'>
      ← {label}
    </Link>
  );
}
```

Barrel: `export { BackLink } from './back-link';`

Run: `npx vitest run --mode=unit src/__tests__/components.ui-back-link.test.tsx` → PASS.

- [ ] **Step 2: Заменить рукописные «← »-ссылки**

`rg "← " src/app src/components -l` — в каждой детальной странице заменить `<Link ...>← ...</Link>` на `<BackLink href='...' label='...' />`. Канон текстов: «Все заказы», «Все заявки», «Все отчёты», «Все пользователи» (вместо «К списку»), «Все организации», «Все партнёры». Email-шаблоны и не-навигационные стрелки не трогать.

- [ ] **Step 3: Пустые состояния**

- `src/app/admin/organizations/page.tsx`: вместо `<td>Ничего не нашли</td>`-строки — при пустом результате рендерить над/вместо таблицы `<EmptyState icon='🏛' message='По заданному фильтру организаций не нашлось.' />` (импорт из `@/components/ui`).
- Чат: в компоненте с «Нет переписок» добавить подпись-подсказку второй строкой: `Переписка появится, когда вы напишете сообщение по заказу или вам напишет команда.` (стилем `text-xs text-gray-400 mt-1`).
- `src/components/manager/manager-finance-view.tsx:16`: текст → `Нет организаций в вашей зоне видимости. Обратитесь к администратору, чтобы вам назначили организации.`

- [ ] **Step 4: Кавычки-дубли**

`rg "«\{" src/app src/components` — каждое вхождение вида `«{name}»`, где `name` — название организации/партнёра из БД, заменить на `{name}` без кавычек (имена уже содержат «ООО «…»»). Известные места: `app/organization/finance/page.tsx:39` («Платежи и задолженность по…»), дашборд организации («Обзор по…»), счётчик в `app/organization/orders/page.tsx`.

- [ ] **Step 5: Прогон + commit**

Run: `npm run typecheck && npm run test:unit`
Expected: PASS (правки размазаны — гоняем весь unit-слой).

```bash
git add -A
git commit -m "feat(ui): BackLink primitive, EmptyState sweep, doubled-quotes fix"
```

---

### Task 12: Страница слушателя — человеческий текст и родная оболочка

**Files:**
- Modify: `src/app/student/page.tsx`

Сейчас орг-пользователь, кликнув «Кабинет слушателя», выпадает в чужой generic-шелл (другая шапка, меню без иконок) и читает «безопасный переход во внешний LMS».

- [ ] **Step 1: Переписать страницу**

```tsx
// src/app/student/page.tsx
import Link from 'next/link';
import { AppShell } from '@/components/dashboard/app-shell';
import { OrgAppShell } from '@/components/organization/org-app-shell';
import { getSession } from '@/lib/auth/session';
import { getOrgPageContext } from '@/lib/auth/orgPageContext';

const CONTENT = (
  <>
    <h1 className='text-2xl font-semibold text-[#111111] mb-3'>Кабинет слушателя</h1>
    <p className='text-sm text-gray-600 mb-4 max-w-prose'>
      Обучение проходит на отдельной учебной площадке. Нажмите кнопку — вход
      выполнится автоматически, отдельный пароль не понадобится.
    </p>
    <Link
      href='/student/redirect'
      className='inline-block rounded-lg bg-[#F97316] hover:bg-[#EA580C] text-white px-4 py-2 text-sm font-semibold transition-colors'
    >
      Перейти к обучению
    </Link>
  </>
);

export default async function StudentPage({
  searchParams
}: {
  searchParams: Promise<{ org?: string }>;
}) {
  const session = await getSession();

  // Орг-пользователь должен остаться в родной оболочке организации,
  // а не выпадать в generic-шелл с другой шапкой и меню.
  if (session?.role === 'organization') {
    const ctx = await getOrgPageContext(await searchParams);
    return (
      <OrgAppShell
        userEmail={ctx.session.email}
        activeOrgName={ctx.activeOrgName}
        memberships={ctx.memberships}
        activeOrgId={ctx.activeOrgId}
        viewerRole={ctx.viewerRole}
      >
        {CONTENT}
      </OrgAppShell>
    );
  }

  return <AppShell>{CONTENT}</AppShell>;
}
```

Перед коммитом проверь сигнатуру `getOrgPageContext` (`src/lib/auth/orgPageContext.ts`) — если она сама читает searchParams иначе, адаптировать вызов по образцу `src/app/organization/finance/page.tsx`.

- [ ] **Step 2: Проверка в браузере (dev)**

Зайти под `org@demo.local` → «Кабинет слушателя»: оболочка организации с иконками сохраняется, текст без «LMS», кнопка оранжевая. Под `manager@demo.local` `/student` — generic-шелл, страница работает.

- [ ] **Step 3: Commit**

```bash
git add src/app/student/page.tsx
git commit -m "fix(student): native shell for org users + plain-language copy"
```

---

### Task 13: Seed-студент и кнопка «Студент» в демо-входе

**Files:**
- Modify: `prisma/seed.ts` (после блока manager-демо, ~строка 230)
- Modify: `src/app/(auth)/login/page.tsx:8-14`

- [ ] **Step 1: Seed**

В `prisma/seed.ts` после блока `manager@demo.local` добавить:

```ts
  // ─── Demo: student user (для кнопки «Студент» на /login) ────────────
  await prisma.user.upsert({
    where: { email: 'student@demo.local' },
    update: { role: 'student', isActive: true, passwordHash, name: 'Demo Student' },
    create: {
      email: 'student@demo.local',
      name: 'Demo Student',
      passwordHash,
      role: 'student'
    }
  });
```

И строку в итоговый вывод перед `[seed] done`:

```ts
  console.log('  - student@demo.local (role=student, лендинг /student)');
```

- [ ] **Step 2: Кнопка демо-входа**

`src/app/(auth)/login/page.tsx` — в `DEMO_LOGINS` добавить последним:

```ts
  { label: 'Студент', email: 'student@demo.local', password: 'Password123!' }
```

- [ ] **Step 3: Прогнать сид и проверить вход**

Run: `npm run prisma:seed` (внимание: процесс не завершается сам из-за BullMQ-хэндла — после строки `[seed] done` убить вручную/Ctrl+C; это известная особенность).
Затем dev-вход кнопкой «Студент» → должен открыться `/student`.

- [ ] **Step 4: Commit**

```bash
git add prisma/seed.ts "src/app/(auth)/login/page.tsx"
git commit -m "feat(demo): student demo account + login shortcut"
```

---

### Task 14: Sweep форматтеров (деньги/даты в одном стиле)

**Files:**
- Modify: `src/lib/services/manager/dashboard/events.ts:115,122`
- Modify: `src/components/manager/manager-events-feed.tsx:22`, `src/app/admin/dashboard/page.tsx:80`
- Modify: `src/components/partner/kpi-grid.tsx` (если не сделано в Task 10), `src/components/manager/manager-finance-view.tsx:7-10`
- Modify: места из `rg "toLocaleDateString\('ru-RU'\)|toLocaleString\('ru-RU'\)|toFixed\(0\)\} ?₽" src/app src/components src/lib/services -l`

- [ ] **Step 1: Точечные замены**

- `events.ts:115`: `${Number(p.amount).toFixed(0)} ₽` → `${fmtMoney(Number(p.amount))}` (итог: `Поступила оплата 250 000 ₽ …`; `Number()` обязателен — `p.amount` это Prisma Decimal, не string/number); `:122` аналогично. Импорт `import { fmtMoney } from '@/lib/format';`
- `manager-events-feed.tsx:22`: `{e.when.toLocaleString('ru-RU')}` → `{fmtDateTime(e.when)}`.
- `admin/dashboard/page.tsx:80`: `{e.timestamp.toLocaleString('ru-RU')}` → `{fmtDateTime(e.timestamp)}`.
- `manager-finance-view.tsx`: удалить локальный `fmtMoney`, импортировать из `@/lib/format` (поведение идентично).
- Остальные находки из rg: даты с `year: '2-digit'` перевести на `fmtDate` (полный год); деньги без разделителей — на `fmtMoney`. Места, где формат уже совпадает, можно оставить, но при касании файла — переводить на форматтеры.

- [ ] **Step 2: Обновить тесты**

`rg "toFixed|toLocaleString" src/__tests__ -l` + тесты manager dashboard events (`services.manager.*` / `services.partner.dashboard.events.test.ts`): ожидания «250000 ₽» → `250 000 ₽`.

Run: `npm run typecheck && npm run test:unit`
Expected: PASS.

- [ ] **Step 3: Commit**

```bash
git add -A
git commit -m "refactor(ui): route user-visible money/dates through shared formatters"
```

---

### Task 15: Мёртвый код входных точек + финальная верификация

**Files:**
- Modify: `src/app/page.tsx`, `src/app/forbidden/page.tsx` (только комментарий)

- [ ] **Step 1: page.tsx — страховка вместо хардкода**

Middleware уже редиректит `/` и `/dashboard` на `roleHome[role]` ([middleware.ts:69](../../src/middleware.ts)); серверный `page.tsx` исполняется только если matcher изменят. Заменить содержимое:

```tsx
import { redirect } from 'next/navigation';

// Боевой редирект корня живёт в middleware (`/` и `/dashboard` -> roleHome[role],
// см. src/middleware.ts). Эта страница — страховка на случай изменения matcher:
// не угадываем роль, просто отправляем на вход.
export default function Home() {
  redirect('/login');
}
```

- [ ] **Step 2: forbidden/page.tsx — комментарий-указатель**

Ссылку `/dashboard` оставить (это рабочий middleware-алиас «домой по роли»), но над `<Link href='/dashboard'` добавить:

```tsx
        {/* /dashboard — виртуальный алиас: middleware редиректит его на
            roleHome[role] (src/middleware.ts). Реальной страницы нет — не «чинить». */}
```

- [ ] **Step 3: Полная верификация**

Run по очереди, всё должно быть зелёным:

```bash
npm run typecheck
npm run lint
npm run test:unit
npm run build
```

- [ ] **Step 4: Ручной smoke (dev-сервер + сид)**

За каждую из 6 демо-кнопок (включая новую «Студент»):
1. Вход → попадаешь в свой кабинет; меню совпадает с каноном (у админа 13 пунктов в 3 группах, у менеджера есть «Финансы»).
2. «Выйти» → оказываешься на `/login` (не на JSON).
3. Один заказ открыть: статус не противоречит цифрам оплаты; нет «supply/service/wire»; KPI-плитка кликается.
4. `/forbidden` руками (зайти менеджером на `/admin/users`) → кнопка возвращает в свой кабинет.

- [ ] **Step 5: E2E visual baselines**

Меню и шапки изменились во всех кабинетах — снапшоты устарели. При наличии сил прогнать `npm run e2e:visual:update` (нужен dev на :3000 + сид) и закоммитить новые baselines отдельным коммитом; если стенд недоступен — зафиксировать в close-out как known debt.

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "chore(entry): align root page with middleware redirect + final verification"
```

---

## Definition of Done

- `rg "[Сс]делк|ОТСФЕРА" src -g '!__tests__'` → пусто.
- `rg "api/auth/logout" src/components` → только `logout-button.tsx`.
- Меню всех кабинетов рендерится из `navByRole` (греп `const ITEMS|const GROUPS` по `src/components/*/`*sidebar* → пусто).
- Организация нигде не видит комиссию; менеджер-не-руководитель — тоже.
- `typecheck` + `lint` + `test:unit` + `build` зелёные; ручной smoke по 6 ролям пройден.
