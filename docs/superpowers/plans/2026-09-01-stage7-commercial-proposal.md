# Этап 7 «Коммерческое предложение» — план

Спека — [2026-08-31-stage7-commercial-proposal-design.md](../specs/2026-08-31-stage7-commercial-proposal-design.md)
(предъявлена 31.08.2026, PR [#461](https://github.com/aiprocadm/lk_otsfera/pull/461);
**подтверждена 01.09.2026** мержем спеки; умолчания §6 действуют).
Требования `У-161`…`У-166` действующего
[ТЗ кабинетов, документов и интеграций](../../tz/2026-08-21-tz-cabinets-documents-integrations.md).

REQUIRED SUB-SKILL: superpowers:subagent-driven-development

## Разбивка

| PR | Что | Требования | Статус |
|---|---|---|---|
| PR-1 «Тип и модель» | `commercial_proposal`, необязательный контрагент с тремя ограничениями, `leadId`/`dealId`, `validUntil`, `rejectedAt`/`rejectReason`, `DocumentLine.vatIncluded`, `Company.proposalValidDays`, ключ имени файла | `У-161` (модель) | ✅ [#462](https://github.com/aiprocadm/lk_otsfera/pull/462) |
| PR-2 «Статусы КП» | `PROPOSAL_FLOW`, расширение `LIFECYCLE_TYPES`, отправка из `draft`, страж на `updateMany` | `У-164` (статусы) | ✅ [#463](https://github.com/aiprocadm/lk_otsfera/pull/463) |
| PR-3a «Тексты и печать» | Слоты текстов КП, переименование реестра, шаблон PDF, симметричный страж вёрстки | `У-162`, `У-163` | ✅ [#464](https://github.com/aiprocadm/lk_otsfera/pull/464) |
| PR-3b «Выпуск» | Начальный статус, пятый набор реквизитов, номер `КП-{год}-{N}`, цель-лид | `У-161`, `У-162`, `У-163` | ✅ [#465](https://github.com/aiprocadm/lk_otsfera/pull/465) |
| PR-4a «Дверь для КП» | Схема входа, гейт лида, панель формы для лида, поля предложения в форме, кнопка и список КП на карточке лида, скрытие черновика от клиента | `У-161`, `У-162`, `У-164` | ✅ [#467](https://github.com/aiprocadm/lk_otsfera/pull/467) |
| PR-4b «Вход из сделки» | Кнопка из сделки без организации, `dealId` у КП организации, стадия «КП отправлено» | `У-161`, `У-164`, `У-166` | ✅ [#469](https://github.com/aiprocadm/lk_otsfera/pull/469) |
| PR-4c «КП из обращения» | «Принять и выставить КП» на карточке обращения; ответственный у лида; закрыта дыра скачивания черновика | `У-161`, `У-164` | ✅ [#471](https://github.com/aiprocadm/lk_otsfera/pull/471) |
| PR-4d «Организация из лида» | Сервис «создать организацию из лида» с переносом уже выпущенных КП | `У-161` | ✅ [#472](https://github.com/aiprocadm/lk_otsfera/pull/472) |
| PR-5 «Срок действия» | Расчёт «истекло» одной функцией, ежедневная задача `docs.expireProposals` | `У-164` (срок) | ✅ [#474](https://github.com/aiprocadm/lk_otsfera/pull/474) |
| PR-6 «Принятие» | Четыре сценария, перенос строк в `OrderLine`, перевод сделки, уведомления | `У-164` (принятие) | ✅ [#476](https://github.com/aiprocadm/lk_otsfera/pull/476) |
| PR-7 «Кабинет заказчика» | «Принять»/«Отклонить» с обязательным комментарием, скрытие черновиков | `У-165` | ✅ [#478](https://github.com/aiprocadm/lk_otsfera/pull/478) |
| PR-8 «Блоки и конверсия» | Блок КП в карточке сделки и организации, конверсия «отправлено → принято»; попутно дефект `У-145` (вкладка «Документы» теряла бумаги без заказа) | `У-166` | ✅ [#480](https://github.com/aiprocadm/lk_otsfera/pull/480) |

**Порядок:** PR-1 первым — на нём стоит всё. PR-2 до PR-3: выпуск пишет статус.
**PR-3 разделён надвое** (решение исполнителя 01.09.2026): в спеке он один, но
получается PR на восемь файлов с новым печатным шаблоном И правкой генератора
на тысячу строк — такой диff не ревьюится. Порядок внутри пары обязателен:
сначала печать, потом выпуск, потому что выпуск обязан чем-то печатать.
PR-6 после PR-3 и PR-4: принимать нечего, пока КП не выпускается.

## PR-1 «Тип и модель» — `У-161` (модель)

- [x] `commercial_proposal` в `enum DocumentType` — **отдельной** миграцией
      `20260901090000_stage7_proposal_type`. Вместе с остальным не получается:
      Postgres не разрешает использовать новое значение перечисления в той же
      транзакции, где оно заведено («unsafe use of new value of enum type»).
- [x] Контрагент документа становится **необязательным**, но с тремя
      ограничениями целостности в базе: заполнен «оба или никак»; пустой
      допустим **только у КП**; у КП без контрагента обязателен `leadId`.
- [x] `Document.leadId` и `Document.dealId` — новые необязательные связи.
- [x] `Document.validUntil` — срок действия КП (`У-162`).
- [x] `Document.rejectedAt` и `rejectReason` — отказ клиента отдельно от
      аннулирования сотрудником (`У-164`).
- [x] `DocumentLine.vatIncluded` — иначе перенос строк в заказ (`У-164`)
      разошёлся бы с суммой КП ровно на ставку налога.
- [x] `Company.proposalValidDays` со значением 14 (`У-162`).
- [x] Чинится унаследованная опечатка: ключ `commercial_offer` в справочнике
      имён файлов заменяется на существующий тип.
- [x] Три страницы с исчерпывающей картой типов документа получают «КП»;
      фильтры типов в кабинетах дописываются руками — они не типизированы
      исчерпывающе и промолчали бы.
- [x] Мутации: пустой контрагент у счёта · контрагент заполнен наполовину ·
      КП без контрагента и без лида · строка документа без признака НДС.

### Появилось по ходу PR-1 (плана не было, но без этого он неверен)

- [x] **Связь с лидом переставлена на `RESTRICT`** третьей миграцией
      `20260901110000_stage7_proposal_lead_restrict`. Задуманный `SET NULL`
      противоречит собственной третьей проверке: обнулив `leadId` у КП без
      контрагента, база тут же нарушает `Document_proposal_needs_lead`, и
      удаление лида падает с сообщением, по которому непонятно ничего.
      Обнаружено пробой на живой базе. Правка уже накаченной миграции
      запрещена (§2 CLAUDE.md) — исправление ушло отдельным файлом.
- [x] **Доступ к документу без контрагента описан явно** в
      [policy.ts](../../../src/lib/auth/policy.ts) и
      [customFields/access.ts](../../../src/lib/services/customFields/access.ts).
      Прежнее «нет контрагента — нет доступа» было безобидным, пока таких
      строк не существовало; теперь оно означало бы, что сотрудник не может
      скачать собственное предложение. Читают свои сотрудники своей компании.
      Проверка роли в этой ветке **несущая**: клиентские сессии тоже несут
      `companyId`.
- [x] Термин «Коммерческое предложение» заведён в
      [глоссарии](../../glossary.md) (§15 CLAUDE.md: новое слово интерфейса
      попадает в глоссарий тем же PR).
- [x] Мутации доведены до девяти: шесть на ограничения базы (включая
      `RESTRICT`, умолчание НДС и умолчание срока) и три на политику доступа.
      Умолчания колонок мутируются **в схеме**, а не в базе: Prisma
      подставляет их сама, и правка `DEFAULT` в Postgres страж не тревожит.


## PR-2 «Статусы КП» — `У-164` (статусы)

- [x] `PROPOSAL_FLOW` — **отдельный** поток, а не ссылка на `ISSUED_FLOW`:
      `draft → sent`, `sent → accepted | rejected | expired | cancelled`,
      конечные пустые. У КП три отличия от общего потока (рождается
      черновиком; отклонить и дать истечь можно только отправленное; отказ и
      истечение необратимы), и общий поток пришлось бы обвешать оговоркой
      «кроме КП» в каждой строке — а разъезжаются именно оговорки.
- [x] `issued` у КП недостижим с ОБЕИХ сторон: попасть в него неоткуда и уйти
      некуда. Состояние, куда нельзя попасть, но откуда можно уйти, — ловушка
      для будущей правки.
- [x] `commercial_proposal` в `LIFECYCLE_TYPES` — вместе с ним КП получает
      право уходить письмом и панель статуса.
- [x] **Правило «откуда можно отправить» выведено из матрицы**, а не записано
      вторым списком, и переехало в `statusMatrix.ts`: его спрашивают и сервис
      (пускать ли), и экран (рисовать ли кнопку). Прежний литерал
      `['issued','sent','accepted']` жил в двух местах и прятал бы кнопку у
      КП-черновика, хотя сервис отправку разрешает.
- [x] **«Первая отправка» определяется матрицей**, а не сравнением с `issued`.
      Прежнее условие сочло бы первую отправку КП повторной: документ остался
      бы черновиком навсегда, а заказчик — с бумагой, которой по системе не
      отправляли.
- [x] Страж статусов расширен на `updateMany`. Проверено сравнением: тот же
      обход старый страж пропускал, новый ловит.
- [x] Мутации: КП пускают в `issued` · черновик КП можно отклонить · отказ КП
      обратим · КП убран из `LIFECYCLE_TYPES` · отправка разрешена из любого
      статуса · «первая отправка» снова по `issued` · обход через `updateMany`.

## PR-3a «Тексты и печать КП» — `У-162`, `У-163`

- [x] Реестр слотов переименован: `contractTemplate.ts` → `documentTemplate.ts`
      вместе с типами и `resolveDocumentClauses`. Он перестал быть договорным
      ещё до КП — просто это не было видно по имени.
- [x] Два слота КП: «Вводный текст» и «Условия», со своими группами экрана.
      Номера пункта у них ПУСТЫЕ: у предложения нет нумерованных разделов, и
      выдуманный «1.1» делал бы вид, что это договор.
- [x] Своя подстановка `proposal.validUntil` — **дата**, а не кусок фразы, как
      `contract.term` у договора. Одна вместо другой напечатала бы «до
      15.09.2026» там, где ждут «один год», и наоборот.
- [x] Срок в «Условиях» **обязателен**: предложение без даты «до когда» — это
      прайс-лист. Сохранить текст без него нельзя.
- [x] `proposalDocumentPdf.ts` — отдельный печатный шаблон. Без банковских
      реквизитов (по КП не платят), без подписи заказчика (он ещё ничего не
      подписывает) и без печати (она создаёт видимость обязательства).
- [x] Абзацы КП печатаются **по ключу**, а не по номеру пункта: фильтр по
      префиксу номера не нашёл бы ничего и оставил бы письмо без единого
      слова — молча.
- [x] Срок печатается вёрсткой ОТДЕЛЬНОЙ строкой помимо текста «Условий»:
      менеджер вправе переписать условия своими словами и выкинуть дату.
      Формулировка строки нарочно не совпадает с текстом слота — иначе страж
      чистоты вёрстки не отличил бы подпись к значению от вернувшегося в код
      готового текста.
- [x] **Страж вёрстки стал двусторонним.** Он проверял ровно один файл печати
      и для нового типа молчал бы, оставаясь зелёным. Теперь слот проверяется
      в ТОМ файле, где печатается, есть проверка «текст не просочился в чужой
      файл» и смок «каждый тип знает свой файл печати».
- [x] Экран настроек и карточка раздела перестали говорить только про договор;
      у абзаца без номера подпись «Пункт  · …» с дырой заменена на название
      места печати.
- [x] Мутации: срок КП не обязателен · условия КП печатают срок договора ·
      абзац КП уехал в договор · вводный текст не печатается · срок пропал из
      вёрстки · текст слота вернулся в вёрстку.

## PR-3b «Выпуск КП» — `У-161`, `У-162`, `У-163`

- [x] `commercial_proposal` в `GenerateDocType`; свой префикс номера `КП` и
      **своя последовательность** `kind='proposal'`. Общий счётчик с договором
      разогнал бы номера договоров до сотен за квартал: предложений выставляют
      кратно больше. Исторических КП с номерами из общей последовательности
      нет — столкнуться не с чем, в отличие от акта (`У-151`).
- [x] **Начальный статус — `draft`**, а не `issued`. Отдельного пути выпуска не
      завели: номер, версия, снимок строк, компенсирующее удаление файла и
      аудит остаются общими, различается одно поле.
- [x] **Третья цель выпуска — лид** (`leadId`), взаимоисключимая с заказом и
      организацией. Лид разрешён ТОЛЬКО коммерческому предложению: иначе счёт
      можно было бы выставить клиенту без организации и без реквизитов, и он
      ушёл бы в никуда.
- [x] **Компания КП лида берётся из СЕССИИ** — первый и единственный такой
      случай в проекте (у лида компании-исполнителя в модели нет). Записано
      комментарием как исключение, потому что `issueScope.ts` гарантирует
      обратное для всех прочих путей. Сотруднику без компании отвечаем
      нехваткой реквизитов, а не «нет прав»: искать у себя недостающий доступ
      он будет напрасно.
- [x] **Лид, у которого уже есть организация, выпускает КАК ОРГАНИЗАЦИИ.**
      Иначе у одного клиента появились бы две нити бумаг: одна в карточке
      организации, вторая невидимая — на лиде.
- [x] Заказчик для проверок и печати у КП лида — **синтетический** блок с одним
      названием. Это вместо `PartyBlock | null`: почти весь код ниже хочет от
      заказчика ровно название, а необязательное поле обросло бы веткой «а если
      пусто» в каждом месте — причём молча, пропущенная давала бы не падение, а
      пустое место в бумаге.
- [x] Пятый набор проверки реквизитов: исполнителю — без банка, заказчику —
      только название. Именно набор, а не отключение проверки.
- [x] Контрагент пишется «оба поля или ни одного»; у КП лида проставляются
      `leadId` и `dealId`, а срок действия ложится **в поле** `validUntil`:
      считать «истекло» по напечатанным словам невозможно.
- [x] Файл КП лида кладётся под `leads/<id>/…`: `organizations/null/…` сделал
      бы разбор хранилища по префиксу бессмысленным.
- [x] Поиск «соседних» документов у КП лида идёт **по лиду**: `counterpartyId:
      null` в Prisma означает «IS NULL» и собрал бы предложения ВСЕХ лидов
      компании.
- [x] Черновик клиенту **не анонсируется письмом**: уведомление уезжает в
      момент отправки (`У-164`).
- [x] Тексты компании читаются и для КП — без этой строки компания правила бы
      их в настройках впустую.
- [x] **Страж «абзацы собираются одним вызовом» переформулирован.** Он считал
      вызовы `resolveDocumentClauses` и держался на том, что тип с текстами
      один; с приходом КП веток стало две, и счётчик покраснел, хотя инвариант
      цел. Инвариант на самом деле: сборка живёт в общем `renderDocument`, а
      предпросмотр и выпуск оба зовут ЕГО. Проверено обеими мутациями.
- [x] Мутации: КП выпускается выставленным · КП делит счётчик с договором ·
      черновик анонсируется письмом · счёт лиду разрешён · лид с организацией
      заводит вторую нить · контрагент заполняется наполовину · срок действия
      не пишется в поле · КП требует банк исполнителя · КП требует реквизиты
      заказчика.

## PR-4a «Дверь для КП» — `У-161`, `У-162`, `У-164`

**Почему PR-4 разделён.** Точек входа пять, и они лежат в разных частях кода.
Но главное — до этого PR выпуск КП был НЕДОСТИЖИМ из интерфейса вовсе: схема
входа не принимала ни тип `commercial_proposal`, ни цель-лид. Сначала дверь,
потом остальные входы в неё.

- [x] Схема входа: тип КП, поле `leadId`, правило «ровно одна цель из трёх».
      Прежнее `!!orderId !== !!organizationId` на трёх полях молча пропускало
      бы «все три сразу»: два `true` дают `false`, и третье поле не смотрят.
- [x] **Гейт лида переехал в `issueScope.ts`** — туда же, где гейт организации,
      и по той же причине: форма выпуска и сам выпуск обязаны спрашивать ОДНО
      правило. Заодно закрыта дыра: `loadLeadTarget` из PR-3b не звал
      `canSeeLead`, и менеджер с профилем «только свои» мог выпустить КП на
      чужого лида, зная лишь его идентификатор — имя и контакт чужого клиента
      напечатались бы в его бумаге.
- [x] То же и на карточке лида: `getManagerLead` охват профиля не проверял
      вовсе — чужой лид открывался прямым адресом вместе с телефоном и почтой.
- [x] Лид с закрытой судьбой (`rejected`, `promoted_to_order`) адресатом быть
      не может; лид, ставший сделкой, — может: сделка и есть место, где
      выставляют КП.
- [x] Панель формы для лида (`getLeadDocumentIssuePanel`) и её серверный экшен
      со своим гейтом. Тип панели тот же, что у организации: форма одна, и
      второй почти-такой-же тип заставил бы её ветвиться в каждом поле.
- [x] У лида с уже заведённой организацией форма целится в ОРГАНИЗАЦИЮ и
      говорит об этом: сервис подменяет цель молча, и форма обязана называть
      настоящего адресата.
- [x] Поля предложения в форме: «О чём предложение» и «Действительно до» с
      умолчанием `сегодня + Company.proposalValidDays`. Сервер отдаёт ЧИСЛО
      ДНЕЙ, дату собирает браузер: сервер живёт в UTC, и готовая дата
      разошлась бы на сутки у части страны.
- [x] **Умолчание срока продублировано в СЕРВИСЕ**: форма — удобство, а вход
      открыт и напрямую (тот же разбор у предпросмотра). Без этого по API
      выпускалось бы предложение с «действительно до —».
- [x] Свой текст успеха для КП: «Черновик предложения № … готов». Общий
      «Коммерческое предложение № … выпущен» неверен дважды — и род, и смысл:
      КП создаётся черновиком, а «выпущено» обещает, что клиент его уже видит.
- [x] `revalidatePath` разложен на три ветки по цели. Тернарник на две давал
      бы `/manager/organizations/undefined`.
- [x] Кнопка «Выставить КП» и блок «Коммерческие предложения» на карточке
      лида. Без списка кнопка вела бы в никуда: у лида нет ни организации, ни
      заказа, и выпущенную бумагу можно было бы найти только поиском по номеру.
- [x] `missingByType` собирается ЛИТЕРАЛОМ, а не циклом по списку видов:
      массив с `satisfies` гарантирует «каждый элемент валиден», но не «каждый
      вид на месте» — проверено, пропуск вида собирался без единой ошибки.
- [x] **КП по заказу запрещено в сервисе** (`proposal_needs_no_order`).
      Предложение делают ДО заказа; иначе у него появился бы контрагент заказа,
      и оно уехало бы в портфель партнёра вместе с ценами и скидками.

### Найдено критиком полноты и починено здесь же

- [x] **Черновик КП утекал бы клиенту.** Канальные выборки статус не смотрели
      вовсе — и не должны были: до КП черновиков не существовало. Первая же
      кнопка «Выставить КП» организации показала бы клиенту неотправленную
      бумагу с ценой, которую ему ещё не предлагали. Фильтр добавлен во все
      три канальных строителя и в гейт чтения (прямая ссылка и скачивание).
      План ставил это в PR-7 — на три PR позже, чем появлялась дыра.
- [x] Фильтр написан ТОЛЬКО про КП, а не «спрятать все черновики»: общий
      запрет молча прятал бы любой будущий тип со стадией черновика.
- [x] **Тип и состояние обязан присылать вызывающий гейта** — иначе каждый
      вызов ходил бы в базу второй раз. Обязанность вынесена в страж
      `security.document-read-fields.guardrail`, который сразу нашёл вызов,
      про который забыли (`server-actions/documents/reissue.ts`).
- [x] Заведён ПДн-контекст `proposal_issue_lead` (§12): в бумагу печатается
      имя контактного лица лида — персональные данные физлица, и карточку
      лида человек мог не открывать вовсе.

## PR-4b «Вход из сделки» — `У-161`, `У-164`, `У-166`

- [x] `Deal.leadId` доезжает до карточки доски: без него экран не мог решить,
      кому выставлять предложение у сделки без организации.
- [x] Блок «Документы» карточки сделки говорит РАЗНОЕ в трёх случаях: есть
      организация — весь набор; есть только лид — одно предложение (счёт
      клиенту без реквизитов выставлять не на что); нет ни того, ни другого —
      кнопки нет, но сказано, что сделать. Пустой блок без объяснения был бы
      дефектом приёмки (§15).
- [x] У сделки без организации открывается форма ЛИДА, а не «та же с пустой
      организацией»: у лида нет ни реквизитов, ни договоров-оснований.
- [x] **`Document.dealId` ставится и у КП организации.** Раньше связь
      появлялась только на пути через лид, а самый частый случай — клиент уже
      заведён, переговоры идут по сделке — оставлял её пустой, и блок
      предложений в карточке сделки (`У-166`, PR-8) оказался бы пустым.
- [x] Связь со сделкой приходит из формы, поэтому **сверяется на сервере**:
      сделка обязана быть своей компании и про того же клиента, что цель
      выпуска. Без проверки документ появился бы в карточке ЧУЖОЙ сделки —
      не утечка наружу, но чужая бумага с ценами в чужих переговорах.
- [x] Дефолтная стадия «Предложение» переименована в **«КП отправлено»**
      (`У-164`): стадия называется тем, что на ней произошло, а «Предложение»
      не отвечало на вопрос «в каком мы состоянии» — его могли ещё готовить.
- [x] Мутации: сделка не сверяется с компанией · сделка не сверяется с
      клиентом · связь со сделкой не пишется · кнопка КП у сделки с лидом
      убрана · объяснение «чего не хватает» убрано · форма лида не
      открывается с доски · стадия снова «Предложение» · `leadId` не доезжает
      до карточки.

## PR-4c «КП из обращения» — `У-161`, `У-164`

**Почему PR-4 разделён ещё раз.** «Создать организацию из лида» — это новый
путь создания сущности со своими ловушками (ИНН у организации уникален
ГЛОБАЛЬНО, а не по компании; образец из админского экрана заводит новую
компанию на каждую организацию; перенос мгновенно открывает бумагу клиенту).
Смешивать его с простым UI-склеиванием двух готовых шагов — значит утопить
и то, и другое в одном ревью.

- [x] **Ни одного нового сервиса и роута.** «Принять и выставить КП» — это два
      СУЩЕСТВУЮЩИХ действия, склеенных в интерфейсе: `convertToLead` уже
      возвращает `leadId`, а форма предложения — готовый компонент. Состав и
      цены человек набирает руками, одним серверным вызовом это не выполнить.
      У каждого шага остаётся свой гейт, в журнале — две записи: общая врала
      бы, что действие атомарное.
- [x] **Порядок обязателен: тост со ссылкой на лид — ДО формы.** Если форма не
      откроется (нет прав, нет компании), человек всё равно знает, что лид
      создан, и знает, куда идти. Обратный порядок оставил бы его с одной
      непонятной ошибкой и без следа сделанного — а обращение уже закрыто.
- [x] **`router.refresh()` — только при закрытии формы.** Обновить список
      сразу значит перерисовать обращение закрытым: компонент уходит в ранний
      выход, и форма умирает на глазах вместе с набранным составом. По той же
      причине форма смонтирована ВЫШЕ раннего выхода.
- [x] Кнопка только на карточке обращения, не в очереди и не в «Входящих».
      Решение исполнителя: открывать форму с каталогом и строками из строки
      списка — тяжёлое взаимодействие, а обе очереди и так дают ссылку на лид,
      где кнопка есть.

### Починено по дороге

- [x] **Созданный из обращения лид теперь закреплён за тем, кто его принял.**
      `convertToLead` не ставил ответственного вовсе, а `canSeeLead` при
      профиле «вижу только свои» сравнивает именно это поле: менеджер создавал
      лид, которого сам же не видел — ни карточку открыть, ни предложение
      выставить. Причину пришлось бы искать в настройках доступа.
- [x] **Закрыта дыра скачивания черновика КП.** `getOrgDocumentForDownload`
      сверяет только КАНАЛ — ни состояния, ни гейта чтения он не спрашивает.
      PR-4a закрыл списки и карточку, но эта дверь осталась открытой: клиент,
      знающий идентификатор документа, скачал бы неотправленное предложение со
      своей же ценой. Запрет вынесен в общую функцию `isHiddenFromClient`
      рядом с канальным фильтром — два одинаковых условия в разных файлах
      расходятся при первой правке, ровно так дыра и появилась.
- [x] Мутации: ответственный не ставится · список обновляется сразу · форма
      внутри раннего выхода · кнопка без гейта флага · черновик скачивается.

## PR-4d «Организация из лида» — `У-161`

Последняя часть PR-4. Места, где лид получает организацию, в проекте не было
вовсе: `lead.organizationId` ставился только при создании лида. Поэтому
«навесить перенос на существующий переход» не на что — заводим действие.

- [x] Сервис `leads/toOrganization.ts`. Гейт — тот же
      `resolveLeadIssueScope`, что у выпуска предложения: второе правило рядом
      с первым обязано разъехаться.
- [x] **Организация создаётся в компании СОТРУДНИКА.** Образец из админского
      экрана берёт нельзя: он заводит новую `Company` на каждую организацию, и
      она сразу оказалась бы «чужой компании» — перенос отказал бы сам себе.
- [x] **ИНН у организации уникален ГЛОБАЛЬНО**, а не по компании. Значит
      «просто создать» нельзя: тёзка в СВОЕЙ компании привязывается (это тот
      же клиент, заведённый раньше), тёзка в ЧУЖОЙ — отказ. Привязать чужую
      значило бы отдать чужому клиенту наши бумаги: канал видимости строится
      по контрагенту и про компанию не знает.
- [x] **Переезжают ВСЕ предложения лида этой компании, независимо от
      состояния.** Не только черновики: отправленное вручную предложение —
      ровно та история, ради которой клиенту и заводят карточку; оставить его
      на лиде значило бы сказать «мы вам ничего не предлагали». Заменённые
      версии тоже — цепочка версий не должна рваться пополам.
- [x] **Фильтр по компании в переносе обязателен:** лиды single-tenant, и на
      одном лиде могут висеть предложения РАЗНЫХ компаний-исполнителей.
- [x] Не трогаются: `leadId` (запрещено проверкой `Document_proposal_needs_lead`
      и нужно для истории), состояние документа (перенос — смена адресата, а
      не событие жизненного цикла), `companyId` (держит уникальность номера, а
      счётчик чужой компании о занятом номере не узнает).
- [x] Контрагент проставляется ОДНИМ оператором: половина нарушает
      `Document_counterparty_both_or_none`, а двумя запросами промежуточное
      состояние неизбежно.
- [x] Сделка лида получает ту же организацию — иначе выигрыш сделки упрётся в
      «нужна организация», хотя клиент уже заведён.
- [x] Всё одной транзакцией; сбой журнала не отменяет сделанного (§3).
- [x] Мутации: чужая компания привязывается · фильтр компании снят у переноса ·
      переносятся только черновики · `leadId` обнуляется · повторная привязка
      разрешена · сделка не получает организацию · сбой журнала рушит операцию.

## PR-5 «Срок действия» — `У-164`

- [x] **Одно правило, два выражения:** чистая функция `isProposalExpired` и
      парный `expiredProposalsWhere` — четыре условия, тот же порядок, та же
      граница. Требование просит и проверку при чтении, и ночную задачу;
      разъедься они, экран сказал бы «ещё действует», а задача в ту же секунду
      перевела бы бумагу в «истёк срок». Совпадение проверяется тестом,
      прогоняющим обе через один набор краевых случаев.
- [x] **Граница — начало суток по МОСКВЕ.** Сервер работает в UTC, и
      `setHours(0,0,0,0)` дал бы разные ответы на машине разработчика и на
      проде — тот же дефект уже ловили у года номера документа (`Д-22`).
- [x] **Предложение действует ВЕСЬ последний день.** В бумаге напечатано «до
      16.09.2026»; клиент, открывший письмо утром 16-го, не должен получить
      «истекло» — это выглядит обманом. Ошибиться в сторону «ещё действует»
      стоит одного дня, в другую — сделки.
- [x] **`status === 'sent'` — часть правила, а не деталь выборки.** Срок стоит
      уже у черновика: без этого условия на карточке черновика было бы «Истёк
      срок» (неправда — его не отправляли), а задача каждую ночь ломилась бы в
      переход `draft → expired`, которого нет в матрице.
- [x] Прямое сравнение дат, а не счёт дней: формула «разница в сутках < 0»
      даёт `-0` для вчерашнего срока с ненулевым временем, а `-0 < 0` — это
      `false`; функция сказала бы «действует», а выборка — «истекло».
- [x] **Показ опережает задачу:** карточка документа и список КП на карточке
      лида считают состояние на лету. Считается на СЕРВЕРЕ — клиентский
      компонент, взявший «сейчас» сам, дал бы разные значения при отрисовке на
      сервере и в браузере, то есть мигающий статус.
- [x] Задача `docs.expireProposals`, ночью в 01:00 по Москве. Час, а не 07:00
      «как у остальных дневных»: расчёт на экране переворачивается в
      московскую полночь, и чем позже задача, тем дольше карточка и база
      говорят разное. Заодно не толкается с 07:00 (там уже две задачи) и с
      03:00 (тяжёлая сверка с 1С).
- [x] **По одному через дверь статусов, а не `updateMany`.** Матрица переходов
      проверяется по каждому документу, и по каждому же пишется строка в
      журнал; одним `updateMany` в журнале не осталось бы ни записи. Запрет
      держит страж, заведённый в PR-2 ровно ради этой задачи.
- [x] **Актёр — отправитель** (`Document.sentById`): `AuditLog.userId`
      обязателен и ссылается на настоящего пользователя, системного в проекте
      нет, а срок пошёл именно с отправки. Бумага без отправителя пропускается
      со счётчиком — выдумать автора в журнале хуже.
- [x] Одна плохая строка не рушит ночной заход: отказ двери и исключение
      считаются отдельно и пишутся в лог.
- [x] Отдельного поля-«захвата» нет: сама смена статуса и есть захват —
      выборка берёт только `sent`, обработанная строка в следующий заход не
      попадёт.
- [x] Мутации: граница по поясу процесса · истекает в последний день · черновик
      истекает · условие статуса выпало из выборки · счёт тоже истекает · показ
      не опережает задачу · бумага без отправителя подписывается кем попало ·
      одна ошибка рушит заход · расписание днём.

## PR-6 «Принятие» — `У-164`

- [x] **Отдельный сервис `acceptProposal`, а не ветка в `accept.ts`.** У них
      разные актёры (приёмка акта — дверь заказчика по построению, а
      предложение чаще принимают со слов клиента, и нажимает сотрудник),
      разные последствия (смена одного поля против создания заказа с составом)
      и разные отказы, которые у акта были бы мёртвыми ветками.
- [x] Все четыре сценария требования: организация без сделки → заказ со
      строками; сделка без заказа → то же плюс связь; сделка с ПУСТЫМ заказом
      → строки в него; сделка с ЗАПОЛНЕННЫМ → состав НЕ перезаписывается.
- [x] «Состав не тронут» — поле УСПЕХА, а не ошибка: предложение принято, и
      перезапись стёрла бы работу менеджера. Экран говорит об этом человеку.
- [x] Сделке ставится РОВНО одно поле — ссылка на заказ, и только если оно
      пусто. Выигрыш остаётся ручным решением менеджера (спека прямо
      запрещает звать `winDeal`).
- [x] Ответственный заказа — менеджер сделки, иначе отправитель предложения.
      НЕ инициатор: принять может сам заказчик, и его пользователь стал бы
      менеджером заказа.
- [x] Партнёр организации попадает в заказ — иначе посредник молча теряет
      вознаграждение. Компания берётся у ДОКУМЕНТА: у организации это поле
      необязательное.
- [x] Заказ из 1С не дополняется: его состав ведёт учётная система.
- [x] Предложение лида принять нечем (у заказа организация обязательна) —
      отдельный отказ со ссылкой на «завести организацию из лида».
- [x] Статус меняется ПОСЛЕ заказа и через единственную дверь; если он не
      сменился, заказ НЕ откатывается, а его идентификатор возвращается в
      ошибке — откат выбросил бы уже полезную работу.

### Два дефекта, найденных разведкой и починенных здесь

- [x] **`snapshotLines` не записывал `vatIncluded`.** Колонка заведена в PR-1
      ровно ради этого переноса, а генератор её не заполнял — у всех строк
      стояло значение по умолчанию «с НДС». Печать была верной (она считает по
      форме), а снимок врал: заказ по предложению с ценами «без НДС»
      разошёлся бы с ним ровно на ставку налога. Единственный тест про это
      поле проверял умолчание БАЗЫ, поэтому дефект был зелёным.
- [x] **`winDeal` безусловно перезаписывал `Deal.orderId`.** Как только
      принятие начало ставить это поле, кнопка «Выиграна» создала бы ВТОРОЙ
      заказ, а первый — со строками из предложения — навсегда потерял бы связь
      со сделкой. Теперь при существующем заказе выигрыш ничего не создаёт, а
      только отмечает сделку; в результате появилось поле `orderCreated`,
      чтобы экран не говорил «заказ создан», когда он был раньше. Запрещать
      выигрыш в таком случае нельзя — это нормальный путь.
- [x] Мутации: состав заказа перезаписывается · признак НДС теряется при
      переносе · менеджером становится нажавший · партнёр не попадает в заказ ·
      КП лида принимается · заказ из 1С дополняется · заказ откатывается при
      сбое статуса · «Выиграна» создаёт второй заказ · снимок строк теряет
      признак НДС.

## PR-7 «Кабинет заказчика» — `У-165`

Скрытие черновиков от клиента сделано раньше — в **PR-4a**, где оно и стало
опасным: первая же кнопка «Выставить КП» организации показала бы клиенту
неотправленную бумагу. Здесь остаётся отказ.

- [x] `rejectProposal` — отдельный сервис и **дверь заказчика по построению**.
      У сотрудника для «клиент отказался» есть аннулирование, и смешивать их
      нельзя: иначе в отчёте о причинах отказов КЛИЕНТОВ окажутся наши
      собственные опечатки в бумагах.
- [x] **Комментарий обязателен.** Отказ без причины сообщает менеджеру только
      то, что он и так видит по статусу. «Дорого», «сроки не подходят» и
      «выбрали другого» ведут к разным следующим шагам — ради этого действие и
      существует. Пустая строка проверяется ДО похода в базу.
- [x] **Отказ пишется в свои поля** `rejectedAt`/`rejectReason`, а не в
      `cancelReason`: «аннулировал сотрудник» и «клиент сказал нет» — разные
      события, и в одном поле их не различить.
- [x] Причина показывается в карточке документа — иначе то, ради чего отказ и
      просят пояснить, не видно нигде, кроме базы. У документа без отказа
      строки нет вовсе: пустое «Причина отказа: —» только сбивает с толку.
- [x] Кнопки «Принять предложение» и «Отклонить» — у отправленного КП. У
      черновика, принятого, отклонённого и истёкшего их нет: предлагать
      действие, которое система отвергнет, значит обещать несбыточное.
- [x] **Письма менеджеру при отказе НЕТ, и это решение.** Подходящего события
      в реестре не существует, а ближайшее («документ принят заказчиком»)
      сказало бы прямо противоположное. Заводить новое событие ТЗ не просит;
      врать в письме хуже, чем промолчать — отказ и причина видны в карточке.
- [x] Мутации: причина необязательна · отклоняет кто угодно · отклоняется любой
      документ · отказ пишется в поле аннулирования · «Отклонить» видна
      сотруднику · причина отказа не показывается.

## PR-8 «Блоки и конверсия» — `У-166`

Последний PR этапа. Требование состоит из двух половин: **блок КП** на карточке
сделки и карточке организации и **конверсия** «КП отправлено → принято» в
аналитике руководителя.

- [x] Блок рисует **один компонент** `ProposalsBlock` на оба места. Разъехавшись,
      два списка нарушили бы правило зеркала (§0.2 ТЗ): один объект должен
      выглядеть и называться одинаково везде. Название состояния берётся из
      общего словаря документов, а не пишется рядом своими словами.
- [x] Данные даёт **отдельный сервис** `proposalBlocks.ts`, а не расширение
      карточки организации. Три самостоятельные причины: карточку рисует один
      сервис на пять кабинетов, и всё, что попадёт в её тип, уедет партнёру и
      заказчику в ответе сервера (а §3.9 ТЗ прямо запрещает показывать
      предложения партнёру); страж действующих версий инвентаризует выборки с
      точностью до функции — второй `findMany` внутри той же функции он бы не
      увидел, и фильтр заменённых версий перестал бы проверяться молча; карточке
      сделки список нужен ленивым — она модалка на доске, где карточек десятки.
- [x] **Заменённые перевыпуском версии скрыты** (`supersededAt: null`): у них тот
      же номер, и две строки подряд читались бы как два разных предложения.
- [x] **Показ опережает ночную задачу:** состояние прогоняется через тот же
      расчёт «истекло», что и карточка (`proposalDisplayStatus`). Иначе до
      прихода задачи блок показывал бы «Отправлен» у предложения, срок которого
      кончился вчера.
- [x] **Граница компании берётся у ДОКУМЕНТА**, а не у организации: у документа
      это поле обязательное, а у организации — нет. Администратор освобождён —
      у него компании в сессии может не быть вовсе.
- [x] Пустой список рисуется **ничем**: заголовок с пустотой под ним занимает
      место и ничего не сообщает. Про «что делать дальше» (§15) отвечает кнопка
      выпуска рядом — она есть и без блока.
- [x] Конверсия считается **когортой, а не двумя счётчиками**: знаменатель —
      предложения, ОТПРАВЛЕННЫЕ за месяц; числитель — те же самые бумаги,
      которые клиент принял, когда бы он это ни сделал. Наивный вариант
      («принято за месяц» ÷ «отправлено за месяц») делит числа из разных
      множеств и умеет давать больше ста процентов.
- [x] «Ждут ответа» считается **остатком**, а не своим фильтром: сумма корзин
      обязана сходиться с целым, иначе появление нового состояния молча
      разъедется с общим числом.
- [x] **Ноль отправленных — прочерк, а не «0 %».** «Не предлагали» и
      «предлагали, но никто не купил» — разные вещи, и ноль сказал бы второе.
      Сервис отдаёт `conversionPct: null`, панель печатает «—».
- [x] Подпись под блоком объясняет, что цифра за текущий месяц всегда занижена:
      по вчерашним предложениям клиент ещё думает. Без неё руководитель сделал
      бы вывод о падении продаж из свойства календаря.
- [x] Заодно чинится дефект `У-145`, найденный при разведке: вкладка «Документы»
      карточки организации отбирала бумаги условием «документы её ЗАКАЗОВ» —
      значит, всё выпущенное кнопкой прямо над этим списком в него не попадало.
      Человек выпускал счёт из карточки и не находил его там же.
- [x] Мутации (двенадцать, все пойманы): сделка не сверяется скоупом · заменённые
      версии показываются · блок открыт клиентским ролям · граница компании
      снята · показ не опережает задачу · ноль отправленных даёт 0 % ·
      знаменатель без фильтра версий · отказ сервиса рисуется пустым блоком ·
      за предложениями ходим на каждой вкладке · ссылка уводит в чужой кабинет ·
      пустой список рисует заголовок · незнакомое состояние пропадает.
