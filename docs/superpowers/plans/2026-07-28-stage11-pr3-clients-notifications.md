# Этап 11 PR-3 — клиентские кабинеты, уведомления, глоссарий (ФТ-15.4, 15.5, 15.7, 15.8, 15.9)

Дата: 2026-07-28 · Спека: [2026-07-27-stage11-navigation-ux-design.md](../specs/2026-07-27-stage11-navigation-ux-design.md)
(✅ подтверждена 27.07.2026) · Ветка от `main` (§14 CLAUDE.md).

**Последний PR программы ТЗ.**

## Задачи

### 1. Навигация клиентов (ФТ-15.4) + терминология (решение §5-1)

- [x] Партнёр: состав и порядок уже совпадают с ТЗ — только переименовать
      «Обращения» → **«Мои заявки»**.
- [x] Организация: переименовать и **переставить** в порядок ТЗ — Главная ·
      Заказы · Мои заявки · Заявки на обучение · Удостоверения · Документы ·
      Финансы · Сотрудники · Команда · Сообщения · Кабинет слушателя · Настройки.
- [x] Переименование затрагивает **только user-facing строки**: коды, роуты
      (`/partner/requests`), модель `ClientRequest` не трогаем (решение §5-1).
- [x] Риск смешения с «заявкой на обучение» снимаем формулировками на самих
      экранах (заголовок/`EmptyState` проговаривают, что это обращение в
      поддержку).

### 2. Мобильность организации (ФТ-15.5)

- [x] Sibling `organization/bottom-tab-bar.tsx` (Главная · Заказы · Заявки ·
      Документы) — **отдельный** компонент, а не общий с партнёрским
      (sibling-pattern, CLAUDE.md §4).
- [x] Монтируется в `OrgAppShell` (у org-кабинета нет общего AppShell —
      страницы оборачиваются сами), контент получает нижний отступ.
- [x] Аудит ключевых таблиц менеджера на мобильном — записать результат
      (что уже карточное, что нет) в close-out, без «на всякий случай» правок.

### 3. Единый реестр типов уведомлений (ФТ-15.7)

- [x] `lib/notifications/registry.ts` по образцу `PII_CONTEXTS`: ключ → русская
      подпись + аудитория + файл-продьюсер.
- [x] Guardrail-тест по образцу `pii.capture-coverage`: каждый тип реестра
      действительно отправляется из заявленного файла, и продьюсеры не шлют
      типов мимо реестра.
- [x] `new_client_request` из ТЗ **уже реализован** под именем
      `client_request_submitted` — фиксируем это в реестре псевдонимом, а не
      переименованием: переименование осиротит исторические строки
      `Notification.type` в БД.
- [x] Мёртвый `enum NotificationType` удалить из схемы (поле уже `String`,
      enum не упоминается в коде ни разу) — миграция `DROP TYPE`.

### 4. Глоссарий (ФТ-15.8)

- [x] `docs/glossary.md` — термины, на которые ссылается ТЗ, с явным
      разведением «обращение (заявка клиента)» vs «заявка на обучение».

### 5. Тесты

- [x] `navigation.cabinet` — состав и порядок пунктов клиентов по ФТ-15.4.
- [x] `notifications.registry` — полнота, уникальность подписей, псевдоним.
- [x] `notifications.registry.guardrail` — продьюсеры vs реестр.
- [x] `components.organization-bottom-tab-bar` — состав вкладок, активная.
- [x] Миграция: `prisma migrate status` чистый.

### 6. Визуальные baseline (ФТ-15.9)

- [x] Оценить, задевают ли правки существующие snapshot'ы, и записать решение.
      Перегенерация требует dev-сервера + сида на :3000 — если недоступно,
      честно вынести в остаток, а не молча оставить красный e2e.

## Отгрузка

- [x] typecheck/lint/unit/integration зелёные; CHANGELOG, STATUS, PR `base: main`.
