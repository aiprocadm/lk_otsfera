import { permanentRedirect } from 'next/navigation';

/**
 * Старый адрес раздела «Заказы» партнёра (`У-109`).
 *
 * Раздел назывался «Сделки», хотя показывал заказы: одноимённая сущность
 * `Deal` в системе есть, и это была не синонимия, а прямая ошибка — человек
 * читал «Сделки» и думал про воронку продаж.
 *
 * Редирект **постоянный** (308): у людей остались закладки и ссылки в письмах,
 * и адрес не вернётся.
 */
export default function PartnerDealsRedirect() {
  permanentRedirect('/partner/orders');
}
