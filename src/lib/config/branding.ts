/**
 * Предел размера изображений оформления документов (`У-138`): логотип,
 * подпись, печать.
 *
 * Отдельный чистый модуль, а не константа сервиса: цифру показывает
 * клиентская форма, а `services/admin/companyBranding` тянет server-only
 * хранилище и очереди — value-импорт оттуда утащил бы их в браузерный бандл.
 * Дублировать число нельзя (§12b, страж `components.upload-size-hint`):
 * подсказка на экране обязана приходить из той же константы, что и проверка.
 */
export const BRANDING_MAX_FILE_MB = 1;

export const BRANDING_MAX_BYTES = BRANDING_MAX_FILE_MB * 1024 * 1024;
