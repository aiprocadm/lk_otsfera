/**
 * Предел размера файла выгрузки 1С (ТЗ починки импорта, Т-5).
 *
 * ЕДИНСТВЕННЫЙ источник правды: до этого предел был назван тремя разными
 * числами — `bodySizeLimit` 10 МБ в `next.config.mjs`, 20 МБ в обоих серверных
 * действиях и «20 МБ» в текстах форм. Получалось, что приложение обещает 20, а
 * Next режет тело на 10 — причём ДО входа в наш код, поэтому вежливо отказать
 * было некому и пользователь видел пустой экран.
 *
 * 25 МБ — размер «Карточки счёта 51» за год (решение заказчика 05.08.2026).
 *
 * Число продублировано в `next.config.mjs` (конфиг читается до сборки и TS
 * импортировать не может); от расхождения защищает тест
 * `lib.config.import-limits`.
 */
export const IMPORT_MAX_FILE_MB = 25;

export const IMPORT_MAX_FILE_BYTES = IMPORT_MAX_FILE_MB * 1024 * 1024;

/**
 * Миграция из Битрикс24 (`У-189` file): за один раз принимается не больше
 * файлов, чем сущностей в выгрузке — компании, контакты, лиды, сделки, задачи.
 * Лимит размера каждого файла — тот же `IMPORT_MAX_FILE_BYTES`.
 */
export const BITRIX_UPLOAD_MAX_FILES = 5;
